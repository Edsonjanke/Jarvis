/** The explanatory line under a list — what is missing, where copies live. */
export interface PanelNoteProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export declare function PanelNote(props: PanelNoteProps): JSX.Element;
