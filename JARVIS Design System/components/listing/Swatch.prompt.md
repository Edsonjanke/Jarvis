Marks the type of a note anywhere it is named — rows, hits, chips, the inspector header.

```jsx
<Swatch type="invoice" />
<Swatch colour="var(--warn)" />
```

Never draw an icon instead. Never tint the dot to show selection — selection is the accent ring around the node on the canvas and the accent text in the row.
