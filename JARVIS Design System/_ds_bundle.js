/* @ds-bundle: {"format":4,"namespace":"JARVISDesignSystem_ad8200","components":[{"name":"AnswerBlock","sourcePath":"components/answer/AnswerBlock.jsx"},{"name":"Cite","sourcePath":"components/answer/Cite.jsx"},{"name":"FactRow","sourcePath":"components/answer/FactRow.jsx"},{"name":"ResultRow","sourcePath":"components/answer/ResultRow.jsx"},{"name":"TurnRow","sourcePath":"components/answer/TurnRow.jsx"},{"name":"Alert","sourcePath":"components/chrome/Alert.jsx"},{"name":"Eyebrow","sourcePath":"components/chrome/Eyebrow.jsx"},{"name":"OpenLink","sourcePath":"components/chrome/OpenLink.jsx"},{"name":"Panel","sourcePath":"components/chrome/Panel.jsx"},{"name":"PanelHead","sourcePath":"components/chrome/PanelHead.jsx"},{"name":"PanelNote","sourcePath":"components/chrome/PanelNote.jsx"},{"name":"AskField","sourcePath":"components/controls/AskField.jsx"},{"name":"Button","sourcePath":"components/controls/Button.jsx"},{"name":"SearchInput","sourcePath":"components/controls/SearchInput.jsx"},{"name":"ThreadBar","sourcePath":"components/controls/ThreadBar.jsx"},{"name":"HudFrame","sourcePath":"design_handoff_jarvis_console_v2/components/hud/HudFrame.jsx"},{"name":"Icon","sourcePath":"design_handoff_jarvis_console_v2/components/hud/Icon.jsx"},{"name":"Meter","sourcePath":"design_handoff_jarvis_console_v2/components/hud/Meter.jsx"},{"name":"RadialGauge","sourcePath":"design_handoff_jarvis_console_v2/components/hud/RadialGauge.jsx"},{"name":"Reactor","sourcePath":"design_handoff_jarvis_console_v2/components/hud/Reactor.jsx"},{"name":"TerminalLog","sourcePath":"design_handoff_jarvis_console_v2/components/hud/TerminalLog.jsx"},{"name":"Tile","sourcePath":"design_handoff_jarvis_console_v2/components/hud/Tile.jsx"},{"name":"BrainRow","sourcePath":"components/listing/BrainRow.jsx"},{"name":"MetaGrid","sourcePath":"components/listing/MetaGrid.jsx"},{"name":"Row","sourcePath":"components/listing/Row.jsx"},{"name":"StatList","sourcePath":"components/listing/StatList.jsx"},{"name":"Swatch","sourcePath":"components/listing/Swatch.jsx"},{"name":"TypeRow","sourcePath":"components/listing/TypeRow.jsx"},{"name":"ReactorDial","sourcePath":"components/status/ReactorDial.jsx"}],"sourceHashes":{"components/answer/AnswerBlock.jsx":"59bfd795dccb","components/answer/Cite.jsx":"f4156299a828","components/answer/FactRow.jsx":"a5536b20ecc0","components/answer/ResultRow.jsx":"2f791ce851e9","components/answer/TurnRow.jsx":"f988227ff49d","components/chrome/Alert.jsx":"ed57c432d550","components/chrome/Eyebrow.jsx":"f7aefa695a05","components/chrome/OpenLink.jsx":"d4e1b91e308a","components/chrome/Panel.jsx":"1a9f93d47f04","components/chrome/PanelHead.jsx":"2614df10f15d","components/chrome/PanelNote.jsx":"4d6d26e1b264","components/controls/AskField.jsx":"36b43f1798b8","components/controls/Button.jsx":"328593647207","components/controls/SearchInput.jsx":"f25046b57b14","components/controls/ThreadBar.jsx":"8da73f1cf784","components/hud/HudFrame.jsx":"0490791060f9","components/hud/Icon.jsx":"7a49ac0a6550","components/hud/Meter.jsx":"01d680e61aa5","components/hud/RadialGauge.jsx":"354c243774f8","components/hud/Reactor.jsx":"a8184b901e82","components/hud/TerminalLog.jsx":"fee30a480b27","components/hud/Tile.jsx":"350270875e9b","components/listing/BrainRow.jsx":"f0f1ce12d23f","components/listing/MetaGrid.jsx":"380162db224b","components/listing/Row.jsx":"1c7e55608c4f","components/listing/StatList.jsx":"a40d080cb52b","components/listing/Swatch.jsx":"d6efb7c5ab3b","components/listing/TypeRow.jsx":"83ba11b783ba","components/status/ReactorDial.jsx":"61b8018be4d2","design_handoff_jarvis_console_v2/components/hud/HudFrame.jsx":"0490791060f9","design_handoff_jarvis_console_v2/components/hud/Icon.jsx":"7a49ac0a6550","design_handoff_jarvis_console_v2/components/hud/Meter.jsx":"01d680e61aa5","design_handoff_jarvis_console_v2/components/hud/RadialGauge.jsx":"354c243774f8","design_handoff_jarvis_console_v2/components/hud/Reactor.jsx":"a8184b901e82","design_handoff_jarvis_console_v2/components/hud/TerminalLog.jsx":"fee30a480b27","design_handoff_jarvis_console_v2/components/hud/Tile.jsx":"350270875e9b","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/CenterStage.jsx":"2319582df058","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/GraphStage.jsx":"be993bf2f57e","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/JarvisConsole.jsx":"b192dc3608f7","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/Overlays.jsx":"f83ca91b86c4","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailLeft.jsx":"75fad86af86b","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailRight.jsx":"e707fa19a5f4","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/telemetry.js":"5a8976b2872a","design_handoff_jarvis_console_v2/ui_kits/jarvis-console/vault.js":"7fad36aaec38","ui_kits/jarvis-console/CenterStage.jsx":"2319582df058","ui_kits/jarvis-console/GraphStage.jsx":"be993bf2f57e","ui_kits/jarvis-console/JarvisConsole.jsx":"b192dc3608f7","ui_kits/jarvis-console/Overlays.jsx":"f83ca91b86c4","ui_kits/jarvis-console/RailLeft.jsx":"75fad86af86b","ui_kits/jarvis-console/RailRight.jsx":"e707fa19a5f4","ui_kits/jarvis-console/telemetry.js":"5a8976b2872a","ui_kits/jarvis-console/vault.js":"7fad36aaec38"},"inlinedExternals":[],"duplicateExports":[{"name":"HudFrame","paths":["components/hud/HudFrame.jsx","design_handoff_jarvis_console_v2/components/hud/HudFrame.jsx"]},{"name":"Icon","paths":["components/hud/Icon.jsx","design_handoff_jarvis_console_v2/components/hud/Icon.jsx"]},{"name":"Meter","paths":["components/hud/Meter.jsx","design_handoff_jarvis_console_v2/components/hud/Meter.jsx"]},{"name":"RadialGauge","paths":["components/hud/RadialGauge.jsx","design_handoff_jarvis_console_v2/components/hud/RadialGauge.jsx"]},{"name":"Reactor","paths":["components/hud/Reactor.jsx","design_handoff_jarvis_console_v2/components/hud/Reactor.jsx"]},{"name":"TerminalLog","paths":["components/hud/TerminalLog.jsx","design_handoff_jarvis_console_v2/components/hud/TerminalLog.jsx"]},{"name":"Tile","paths":["components/hud/Tile.jsx","design_handoff_jarvis_console_v2/components/hud/Tile.jsx"]}],"unexposedExports":[{"name":"typeColour","sourcePath":"components/listing/Swatch.jsx"}]} */

(() => {

const __ds_ns = (window.JARVISDesignSystem_ad8200 = window.JARVISDesignSystem_ad8200 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/answer/FactRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Something JARVIS decided was worth keeping. It writes these itself, which is
   only acceptable if you can see all of them and throw any of them away. */
function FactRow({
  when,
  text,
  onForget,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-4)",
      padding: "7px var(--space-4)",
      borderRadius: "var(--r)",
      background: hover ? "rgba(255,255,255,0.05)" : "transparent",
      fontFamily: "var(--font-mono)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      fontSize: "var(--size-tick)",
      color: "var(--text-muted)",
      fontVariantNumeric: "tabular-nums"
    }
  }, when), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-base)",
      lineHeight: "var(--lh-row)",
      color: "var(--text-body)"
    }
  }, text), onForget ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onForget,
    style: {
      flex: "none",
      font: "inherit",
      fontSize: "var(--size-tick)",
      letterSpacing: "var(--track-label)",
      textTransform: "uppercase",
      color: "var(--control-fg)",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      padding: "var(--ghost-pad)",
      cursor: "pointer",
      opacity: hover ? 1 : 0,
      transition: "opacity var(--t-row)"
    }
  }, "Forget") : null);
}
Object.assign(__ds_scope, { FactRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/answer/FactRow.jsx", error: String((e && e.message) || e) }); }

// components/answer/TurnRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* One past question. Reopening it also puts you back in its conversation, so
   the next question continues from there. */
function TurnRow({
  when,
  question,
  meta,
  onOpen,
  onDelete,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "grid",
      gridTemplateColumns: "auto 1fr auto auto",
      gap: "var(--space-3)",
      alignItems: "baseline",
      padding: "var(--space-2) 0",
      borderTop: "1px solid rgba(255,255,255,0.05)",
      fontFamily: "var(--font-mono)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-tick)",
      color: "var(--text-muted)",
      fontVariantNumeric: "tabular-nums"
    }
  }, when), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onOpen,
    style: {
      textAlign: "left",
      border: 0,
      background: "transparent",
      color: "var(--text-strong)",
      font: "inherit",
      fontSize: "var(--size-base)",
      cursor: "pointer",
      padding: 0
    }
  }, question), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-tick)",
      color: "var(--text-muted)",
      whiteSpace: "nowrap"
    }
  }, meta), onDelete ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onDelete,
    style: {
      font: "inherit",
      fontSize: "var(--size-tick)",
      letterSpacing: "var(--track-label)",
      textTransform: "uppercase",
      color: "var(--control-fg)",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      padding: "var(--ghost-pad)",
      cursor: "pointer",
      opacity: hover ? 1 : 0,
      transition: "opacity var(--t-row)"
    }
  }, "Apagar") : null);
}
Object.assign(__ds_scope, { TurnRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/answer/TurnRow.jsx", error: String((e && e.message) || e) }); }

// components/chrome/Alert.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Present only when something is genuinely broken. Full-width bed at the top
   of the screen, stacked, never dismissible by decoration. */
const BED = {
  warn: {
    background: "var(--alert-warn-bg)",
    color: "var(--alert-warn-fg)"
  },
  crit: {
    background: "var(--alert-crit-bg)",
    color: "var(--alert-crit-fg)"
  },
  info: {
    background: "var(--alert-info-bg)",
    color: "var(--alert-info-fg)"
  }
};
function Alert({
  level = "warn",
  label,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-4)",
      padding: "9px 18px",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-row)",
      borderBottom: "1px solid var(--border-hairline)",
      ...(BED[level] || BED.warn),
      ...style
    }
  }), /*#__PURE__*/React.createElement("b", {
    style: {
      fontWeight: "var(--weight-strong)",
      letterSpacing: "var(--track-label)",
      textTransform: "uppercase",
      fontSize: "var(--size-tick)",
      opacity: 0.75,
      flex: "none"
    }
  }, label), /*#__PURE__*/React.createElement("span", null, children));
}
Object.assign(__ds_scope, { Alert });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/Alert.jsx", error: String((e && e.message) || e) }); }

// components/chrome/Eyebrow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Ten uppercase pixels behind a 6x1px dash. The only ornament in the product. */
function Eyebrow({
  children,
  count,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "7px",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      letterSpacing: "var(--track-eyebrow)",
      textTransform: "uppercase",
      color: "var(--text-label)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "6px",
      height: "1px",
      background: "currentColor",
      flex: "none"
    }
  }), children, count !== undefined && count !== null ? /*#__PURE__*/React.createElement("span", {
    style: {
      letterSpacing: 0,
      color: "var(--ink-3)"
    }
  }, count) : null);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/answer/AnswerBlock.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* An answer, with the notes it stands on. The body is the one place in the
   ask bar where the proportional face appears — a model writing prose is
   standing in for the person, not for the machine. */
function AnswerBlock({
  kind = "ask",
  meta,
  metaTitle,
  body,
  truncated,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      padding: "var(--space-4)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-4)",
      marginBottom: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, kind), /*#__PURE__*/React.createElement("span", {
    title: metaTitle,
    style: {
      fontSize: "var(--size-tick)",
      color: "var(--text-muted)",
      marginLeft: "auto"
    }
  }, meta)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-base)",
      lineHeight: "var(--lh-prose)",
      color: "var(--text-body)",
      whiteSpace: "pre-wrap"
    }
  }, body), truncated ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "var(--size-fine)",
      color: "var(--warn)",
      marginTop: "var(--space-3)"
    }
  }, "Cut off at the token limit \u2014 the answer above is incomplete.") : null, children ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      flexWrap: "wrap",
      marginTop: "var(--space-5)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--border-hairline)"
    }
  }, children) : null);
}
Object.assign(__ds_scope, { AnswerBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/answer/AnswerBlock.jsx", error: String((e && e.message) || e) }); }

// components/chrome/OpenLink.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Opening the real document. Deliberately not styled as a primary action —
   the extracted text is still the fast path; this is for when you need to see
   the page itself. */
function OpenLink({
  children,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", _extends({
    target: "_blank",
    rel: "noopener noreferrer"
  }, rest, {
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "inline-block",
      margin: "2px 0 10px",
      padding: "5px var(--space-4)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-fine)",
      color: "var(--accent)",
      background: "var(--accent-soft)",
      borderRadius: "var(--r)",
      textDecoration: "none",
      filter: hover ? "var(--lift-hover)" : "none",
      ...style
    }
  }), children, /*#__PURE__*/React.createElement("span", {
    style: {
      opacity: 0.6
    }
  }, " \u2197"));
}
Object.assign(__ds_scope, { OpenLink });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/OpenLink.jsx", error: String((e && e.message) || e) }); }

// components/chrome/Panel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The floating plate. Translucent, blurred, hairline border with a lit top
   edge. Three of these float over the canvas; nothing else in the product is
   a surface. */
function Panel({
  side = "free",
  children,
  scroll = true,
  style,
  ...rest
}) {
  const fixed = {
    left: {
      position: "fixed",
      zIndex: 3,
      top: "var(--gutter)",
      left: "var(--gutter)",
      width: "var(--panel-left-w)",
      bottom: "var(--panel-bottom)"
    },
    right: {
      position: "fixed",
      zIndex: 3,
      top: "var(--gutter)",
      right: "var(--gutter)",
      width: "var(--panel-right-w)",
      bottom: "var(--panel-bottom)"
    },
    free: {}
  }[side] || {};
  return /*#__PURE__*/React.createElement("aside", _extends({}, rest, {
    style: {
      background: "var(--surface-panel)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid var(--border-hairline)",
      borderTopColor: "var(--border-lit)",
      borderRadius: "var(--r)",
      padding: "var(--pad)",
      color: "var(--text-body)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-base)",
      overflowY: scroll ? "auto" : "visible",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      ...fixed,
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/Panel.jsx", error: String((e && e.message) || e) }); }

// components/chrome/PanelHead.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Section header inside a plate: an eyebrow on the left, one control or count
   on the right. There are no <h2>s in this product. */
function PanelHead({
  label,
  children,
  rule = false,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("header", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-3)",
      marginBottom: "var(--space-5)",
      paddingBottom: rule ? "var(--space-4)" : 0,
      borderBottom: rule ? "1px solid var(--border-hairline)" : "none",
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, null, label), children);
}
Object.assign(__ds_scope, { PanelHead });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/PanelHead.jsx", error: String((e && e.message) || e) }); }

// components/chrome/PanelNote.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Why a thing is unavailable, or where the backups went. Small, dim, and on
   the page rather than hidden in a title attribute where only a mouse finds it. */
function PanelNote({
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("p", _extends({}, rest, {
    style: {
      margin: "var(--space-2) var(--space-3) 0",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      lineHeight: 1.45,
      color: "var(--text-muted)",
      opacity: 0.75,
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { PanelNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/chrome/PanelNote.jsx", error: String((e && e.message) || e) }); }

// components/controls/AskField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The ask bar's field. It does two things at once and they do not collide:
   typing runs the instant file search, Enter sends the question to the model. */
function AskField({
  mode = "ask",
  value,
  onChange,
  onSubmit,
  placeholder,
  hint = "enter",
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-5)",
      padding: "0 var(--field-pad-x)",
      height: "var(--field-h)",
      background: "var(--surface-raised)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid " + (focus ? "var(--edge-lit)" : "var(--border-hairline)"),
      borderTopColor: focus ? "var(--edge-lit)" : "var(--border-lit)",
      borderRadius: "var(--r)",
      transition: "border-color var(--t-field)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-micro)",
      letterSpacing: "var(--track-mode)",
      textTransform: "uppercase",
      color: "var(--text-muted)",
      borderRight: "1px solid var(--border-hairline)",
      paddingRight: "var(--space-5)",
      flex: "none"
    }
  }, mode), /*#__PURE__*/React.createElement("input", {
    type: "text",
    autoComplete: "off",
    spellCheck: false,
    value: value,
    placeholder: placeholder,
    onChange: e => onChange && onChange(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    onKeyDown: e => {
      if (e.key === "Enter" && onSubmit) {
        e.preventDefault();
        onSubmit(value);
      }
    },
    style: {
      flex: 1,
      minWidth: 0,
      background: "transparent",
      border: 0,
      outline: 0,
      color: "var(--text-strong)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-input)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-micro)",
      letterSpacing: "var(--track-label)",
      textTransform: "uppercase",
      color: "var(--text-muted)",
      flex: "none"
    },
    "aria-hidden": "true"
  }, hint));
}
Object.assign(__ds_scope, { AskField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/AskField.jsx", error: String((e && e.message) || e) }); }

// components/controls/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Outlined, never filled: on a dark instrument panel a filled button reads as
   a web page, an etched one reads as hardware. Hover and pressed resolve to
   the same accent triple; disabled keeps its label and takes a dashed border. */
function Button({
  size = "key",
  pressed,
  disabled,
  children,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const lit = hover && !disabled || pressed;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    disabled: disabled,
    "aria-pressed": pressed === undefined ? undefined : String(!!pressed),
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      font: "inherit",
      fontFamily: "var(--font-mono)",
      fontSize: size === "ghost" ? "var(--size-tick)" : "var(--size-key)",
      letterSpacing: "var(--track-label)",
      textTransform: "uppercase",
      padding: size === "ghost" ? "var(--ghost-pad)" : "var(--key-pad)",
      borderRadius: "var(--r)",
      background: disabled ? "transparent" : lit ? "var(--control-bg-active)" : "transparent",
      color: disabled ? "var(--control-fg-disabled)" : lit ? "var(--control-fg-active)" : "var(--control-fg)",
      border: "1px solid " + (disabled ? "var(--control-border-disabled)" : lit ? "var(--control-border-active)" : "var(--control-border)"),
      borderStyle: disabled ? "dashed" : "solid",
      cursor: disabled ? "not-allowed" : "pointer",
      transition: "var(--transition-control)",
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/Button.jsx", error: String((e && e.message) || e) }); }

// components/controls/SearchInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The secondary field: filtering a list that is already on screen. */
function SearchInput({
  value,
  onChange,
  onSubmit,
  placeholder,
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  return /*#__PURE__*/React.createElement("input", _extends({
    type: "search"
  }, rest, {
    value: value,
    placeholder: placeholder,
    onChange: e => onChange && onChange(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    onKeyDown: e => {
      if (e.key === "Enter" && onSubmit) onSubmit(e.currentTarget.value);
    },
    style: {
      width: "100%",
      margin: "var(--space-2) 0 var(--space-4)",
      padding: "7px 9px",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-base)",
      color: "var(--text-strong)",
      background: "rgba(255, 255, 255, 0.04)",
      border: "1px solid " + (focus ? "var(--accent)" : "rgba(255, 255, 255, 0.08)"),
      borderRadius: "var(--r)",
      outline: "none",
      ...style
    }
  }));
}
Object.assign(__ds_scope, { SearchInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/SearchInput.jsx", error: String((e && e.message) || e) }); }

// components/controls/ThreadBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* "This question will be read alongside the last one" changes what you should
   type, so the indicator sits in the button row rather than in a panel. */
function ThreadBar({
  label = "conversa em andamento",
  onNew,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginLeft: "var(--space-2)",
      padding: "4px var(--space-3)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      color: "var(--accent)",
      background: "var(--accent-soft)",
      borderRadius: "var(--r)",
      ...style
    }
  }), label, /*#__PURE__*/React.createElement(__ds_scope.Button, {
    size: "ghost",
    onClick: onNew
  }, "nova conversa"));
}
Object.assign(__ds_scope, { ThreadBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/controls/ThreadBar.jsx", error: String((e && e.message) || e) }); }

// components/hud/HudFrame.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* v2's panel. Not a box — a labelled rule across the top whose right end
   cuts away on a diagonal, with the content hanging beneath it. Set foot to
   close the block with a mirrored rule. */
function HudFrame({
  label,
  right,
  foot = false,
  children,
  style,
  ...rest
}) {
  const rule = flip => /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--hud-frame-gap)",
      pointerEvents: "none"
    }
  }, !flip && /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--hud-rule)",
      height: "var(--hud-cap)",
      background: "var(--hud-line)",
      flex: "none"
    }
  }), !flip && label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      letterSpacing: "var(--track-eyebrow)",
      textTransform: "uppercase",
      color: "var(--text-label)",
      whiteSpace: "nowrap",
      flex: "none"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: "var(--hud-rule)",
      background: flip ? "var(--hud-line-dim)" : "var(--hud-line)"
    }
  }), right && !flip ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      letterSpacing: "0.06em",
      color: "var(--text-muted)",
      flex: "none"
    }
  }, right) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--hud-cut)",
      height: "var(--hud-cut-drop)",
      flex: "none",
      background: `linear-gradient(to bottom ${flip ? "left" : "right"}, transparent calc(50% - 0.5px), ${flip ? "var(--hud-line-dim)" : "var(--hud-line)"} calc(50% - 0.5px), ${flip ? "var(--hud-line-dim)" : "var(--hud-line)"} calc(50% + 0.5px), transparent calc(50% + 0.5px))`
    }
  }));
  return /*#__PURE__*/React.createElement("section", _extends({}, rest, {
    style: {
      fontFamily: "var(--font-mono)",
      color: "var(--text-body)",
      ...style
    }
  }), rule(false), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--hud-body-pad)"
    }
  }, children), foot ? rule(true) : null);
}
Object.assign(__ds_scope, { HudFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/HudFrame.jsx", error: String((e && e.message) || e) }); }

// components/hud/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* SUBSTITUTION — the source codebase ships no icons at all (see readme,
   ICONOGRAPHY). v2's tile grids need glyphs, so Lucide (1.5px stroke, round
   caps, 24px box, MIT) stands in. The path data is copied into this file and
   into assets/icons/*.svg rather than linked from a CDN, so a glyph renders
   with no network and survives thumbnail capture. Stroke is currentColor, so
   an icon takes the same state colour as the control around it. */
const GLYPHS = {
  "mic": "<path d=\"M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z\" /> <path d=\"M19 10v2a7 7 0 0 1-14 0v-2\" /> <line x1=\"12\" x2=\"12\" y1=\"19\" y2=\"22\" />",
  "volume-x": "<path d=\"M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z\" /> <line x1=\"22\" x2=\"16\" y1=\"9\" y2=\"15\" /> <line x1=\"16\" x2=\"22\" y1=\"9\" y2=\"15\" />",
  "scan-line": "<path d=\"M3 7V5a2 2 0 0 1 2-2h2\" /> <path d=\"M17 3h2a2 2 0 0 1 2 2v2\" /> <path d=\"M21 17v2a2 2 0 0 1-2 2h-2\" /> <path d=\"M7 21H5a2 2 0 0 1-2-2v-2\" /> <path d=\"M7 12h10\" />",
  "file-search": "<path d=\"M14 2v4a2 2 0 0 0 2 2h4\" /> <path d=\"M4.268 21a2 2 0 0 0 1.727 1H18a2 2 0 0 0 2-2V7l-5-5H6a2 2 0 0 0-2 2v3\" /> <path d=\"m9 18-1.5-1.5\" /> <circle cx=\"5\" cy=\"14\" r=\"3\" />",
  "brain-circuit": "<path d=\"M12 5a3 3 0 1 0-5.997.125 4 4 0 0 0-2.526 5.77 4 4 0 0 0 .556 6.588A4 4 0 1 0 12 18Z\" /> <path d=\"M9 13a4.5 4.5 0 0 0 3-4\" /> <path d=\"M6.003 5.125A3 3 0 0 0 6.401 6.5\" /> <path d=\"M3.477 10.896a4 4 0 0 1 .585-.396\" /> <path d=\"M6 18a4 4 0 0 1-1.967-.516\" /> <path d=\"M12 13h4\" /> <path d=\"M12 18h6a2 2 0 0 1 2 2v1\" /> <path d=\"M12 8h8\" /> <path d=\"M16 8V5a2 2 0 0 1 2-2\" /> <circle cx=\"16\" cy=\"13\" r=\".5\" /> <circle cx=\"18\" cy=\"3\" r=\".5\" /> <circle cx=\"20\" cy=\"21\" r=\".5\" /> <circle cx=\"20\" cy=\"8\" r=\".5\" />",
  "square-terminal": "<path d=\"m7 11 2-2-2-2\" /> <path d=\"M11 13h4\" /> <rect width=\"18\" height=\"18\" x=\"3\" y=\"3\" rx=\"2\" ry=\"2\" />",
  "database": "<ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\" /> <path d=\"M3 5V19A9 3 0 0 0 21 19V5\" /> <path d=\"M3 12A9 3 0 0 0 21 12\" />",
  "network": "<rect x=\"16\" y=\"16\" width=\"6\" height=\"6\" rx=\"1\" /> <rect x=\"2\" y=\"16\" width=\"6\" height=\"6\" rx=\"1\" /> <rect x=\"9\" y=\"2\" width=\"6\" height=\"6\" rx=\"1\" /> <path d=\"M5 16v-3a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v3\" /> <path d=\"M12 12V8\" />",
  "workflow": "<rect width=\"8\" height=\"8\" x=\"3\" y=\"3\" rx=\"2\" /> <path d=\"M7 11v4a2 2 0 0 0 2 2h4\" /> <rect width=\"8\" height=\"8\" x=\"13\" y=\"13\" rx=\"2\" />",
  "settings": "<path d=\"M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z\" /> <circle cx=\"12\" cy=\"12\" r=\"3\" />",
  "cloud-rain": "<path d=\"M4 14.899A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 2.5 8.242\" /> <path d=\"M16 14v6\" /> <path d=\"M8 14v6\" /> <path d=\"M12 16v6\" />",
  "arrow-right": "<path d=\"M5 12h14\" /> <path d=\"m12 5 7 7-7 7\" />",
  "x": "<path d=\"M18 6 6 18\" /> <path d=\"m6 6 12 12\" />"
};
function Icon({
  name,
  size = 15,
  strokeWidth = 1.7,
  style,
  ...rest
}) {
  const inner = GLYPHS[name];
  if (!inner) return null;
  return /*#__PURE__*/React.createElement("svg", _extends({
    "aria-hidden": "true",
    viewBox: "0 0 24 24",
    width: size,
    height: size,
    fill: "none",
    stroke: "currentColor",
    strokeWidth: strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, rest, {
    style: {
      display: "block",
      flex: "none",
      ...style
    },
    dangerouslySetInnerHTML: {
      __html: inner
    }
  }));
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/Icon.jsx", error: String((e && e.message) || e) }); }

// components/hud/Meter.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A labelled bar with a value at the end. Segmented by default — the bar is
   cut into 3px ticks, which is how the reference panel reads a load. */
function Meter({
  label,
  value = 0,
  display,
  tone = "accent",
  segmented = true,
  style,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, value));
  const colour = tone === "warn" ? "var(--warn)" : tone === "crit" ? "var(--crit)" : tone === "muted" ? "var(--ink-3)" : "var(--accent)";
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "9px",
      padding: "3px 0",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--meter-label-w)",
      flex: "none",
      color: "var(--text-label)",
      letterSpacing: "0.08em",
      textTransform: "uppercase"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: "var(--meter-h)",
      background: "var(--wash)",
      position: "relative",
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      right: 100 - pct + "%",
      background: colour,
      transition: "right 400ms linear",
      ...(segmented ? {
        WebkitMaskImage: "repeating-linear-gradient(90deg, #000 0 var(--meter-seg), transparent var(--meter-seg) calc(var(--meter-seg) + 2px))",
        maskImage: "repeating-linear-gradient(90deg, #000 0 var(--meter-seg), transparent var(--meter-seg) calc(var(--meter-seg) + 2px))"
      } : null)
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      minWidth: "30px",
      textAlign: "right",
      color: "var(--text-strong)",
      fontVariantNumeric: "tabular-nums"
    }
  }, display !== undefined ? display : pct + "%"));
}
Object.assign(__ds_scope, { Meter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/Meter.jsx", error: String((e && e.message) || e) }); }

// components/hud/RadialGauge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A ring gauge with its value in the middle. Stroke is a dasharray, so the
   arc animates by colour crossfade only — nothing rotates. */
function RadialGauge({
  value = 0,
  size = 54,
  label,
  caption,
  style,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, value));
  const r = (size - 6) / 2;
  const c = 2 * Math.PI * r;
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "11px",
      fontFamily: "var(--font-mono)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size,
      flex: "none"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--wash-2)",
    strokeWidth: "var(--gauge-stroke)"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--accent)",
    strokeWidth: "var(--gauge-stroke)",
    strokeDasharray: `${c * pct / 100} ${c}`,
    strokeLinecap: "butt",
    style: {
      transition: "stroke-dasharray 400ms linear"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "1px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-base)",
      color: "var(--accent)",
      textShadow: "var(--hud-glow-soft)",
      fontVariantNumeric: "tabular-nums"
    }
  }, pct, "%"), label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "8px",
      letterSpacing: "0.14em",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, label) : null)), caption ? /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, caption) : null);
}
Object.assign(__ds_scope, { RadialGauge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/RadialGauge.jsx", error: String((e && e.message) || e) }); }

// components/hud/Reactor.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* v2's reactor. Concentric rings around a triangle core, drawn on canvas.
   v1's ReactorDial was a single bezel with one state arc; this is the
   reference's stack — a tick bezel, gapped rings, an arc pair and a slow
   orbit — and it still carries state only: idle crawls, thinking sweeps.

   Runs on the same 33ms interval the rest of this system uses rather than
   requestAnimationFrame, which stops dead when the frame is off screen. */
const RINGS = [{
  r: 0.115,
  w: 1,
  a: 0.42,
  gaps: 0,
  spin: 0
}, {
  r: 0.175,
  w: 1,
  a: 0.20,
  gaps: 3,
  spin: 0.10,
  gapArc: 0.30
}, {
  r: 0.245,
  w: 1,
  a: 0.30,
  ticks: 48,
  tick: 4
}, {
  r: 0.325,
  w: 1.4,
  a: 0.16,
  gaps: 2,
  spin: -0.06,
  gapArc: 0.55
}, {
  r: 0.395,
  w: 1,
  a: 0.10,
  ticks: 96,
  tick: 2.5
}, {
  r: 0.475,
  w: 1,
  a: 0.13,
  gaps: 4,
  spin: 0.04,
  gapArc: 0.18
}, {
  r: 0.60,
  w: 1,
  a: 0.07,
  gaps: 0
}];
function Reactor({
  size = 420,
  state = "idle",
  level = 0.5,
  tilt = 0.46,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  const live = React.useRef({
    state,
    level,
    tilt
  });
  live.current = {
    state,
    level,
    tilt
  };
  const RS = 0.78; /* the ring stack fills 94% of the box, corners left free for readouts */

  React.useEffect(() => {
    const canvas = ref.current;
    const ctx = canvas.getContext("2d");
    const css = getComputedStyle(document.documentElement);
    const accent = css.getPropertyValue("--accent").trim() || "#3ce88f";
    const rgb = (css.getPropertyValue("--accent-rgb").trim() || "60,232,143").replace(/\s/g, "");
    const dots = Array.from({
      length: 26
    }, (_, i) => ({
      a: i / 26 * Math.PI * 2 + i * 0.7,
      r: 0.035 + i * 37 % 100 / 100 * 0.10,
      s: 0.15 + i * 53 % 100 / 100 * 0.5
    }));
    let t = 0,
      raf = null;
    const draw = () => {
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      const s = canvas.clientWidth || size;
      if (canvas.width !== Math.round(s * dpr)) {
        canvas.width = Math.round(s * dpr);
        canvas.height = Math.round(s * dpr);
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      ctx.clearRect(0, 0, s, s);
      const cx = s / 2,
        cy = s / 2;
      const busy = live.current.state !== "idle";
      const lv = Math.max(0, Math.min(1, live.current.level));
      const ty = Math.max(0.12, Math.min(1, live.current.tilt));
      const rate = busy ? 4.2 : 1;
      const gain = 0.55 + lv * 0.45 + (busy ? 0.25 : 0);

      /* The whole stack lies on a plane seen at an angle: every ring is an
         ellipse of the same radius squashed to `tilt` vertically, and every
         mark sits on that plane. Only the core stands upright. */
      const px = (a, R) => cx + Math.cos(a) * R;
      const py = (a, R) => cy + Math.sin(a) * R * ty;
      for (const ring of RINGS) {
        const R = ring.r * s * RS;
        ctx.lineWidth = ring.w;
        ctx.strokeStyle = `rgba(${rgb},${ring.a * gain})`;
        if (ring.ticks) {
          for (let i = 0; i < ring.ticks; i++) {
            const a = i / ring.ticks * Math.PI * 2 + t * 0.00004 * rate;
            const major = i % 4 === 0;
            const len = major ? ring.tick * 1.9 : ring.tick;
            ctx.beginPath();
            ctx.moveTo(px(a, R), py(a, R));
            ctx.lineTo(px(a, R + len), py(a, R + len));
            ctx.stroke();
          }
        } else if (ring.gaps) {
          const step = Math.PI * 2 / ring.gaps;
          const arc = step * (1 - ring.gapArc);
          for (let i = 0; i < ring.gaps; i++) {
            const a0 = i * step + t * 0.00012 * ring.spin * rate * 12;
            ctx.beginPath();
            ctx.ellipse(cx, cy, R, R * ty, 0, a0, a0 + arc);
            ctx.stroke();
          }
        } else {
          ctx.beginPath();
          ctx.ellipse(cx, cy, R, R * ty, 0, 0, Math.PI * 2);
          ctx.stroke();
        }
      }

      /* the state arc — a crawl at idle, a sweep when the model is working */
      const period = busy ? 800 : 6000;
      const head = t % period / period * Math.PI * 2;
      const arcR = 0.245 * s * RS;
      ctx.lineWidth = 1.6;
      ctx.strokeStyle = accent;
      ctx.shadowColor = accent;
      ctx.shadowBlur = 10;
      ctx.beginPath();
      ctx.ellipse(cx, cy, arcR, arcR * ty, 0, head, head + (busy ? 1.15 : 0.42));
      ctx.stroke();
      ctx.shadowBlur = 0;

      /* drifting motes, on the same plane */
      for (const d of dots) {
        const a = d.a + t * 0.00022 * d.s * rate;
        const R = d.r * s * RS * (1 + Math.sin(t * 0.0006 + d.a) * 0.08);
        ctx.fillStyle = `rgba(${rgb},${0.30 + Math.sin(t * 0.001 + d.a) * 0.18})`;
        ctx.beginPath();
        ctx.arc(px(a, R), py(a, R), 1.15, 0, Math.PI * 2);
        ctx.fill();
      }

      /* core: a soft bloom and the triangle */
      const bloom = ctx.createRadialGradient(cx, cy, 0, cx, cy, 0.09 * s);
      bloom.addColorStop(0, `rgba(${rgb},${0.30 * gain})`);
      bloom.addColorStop(1, `rgba(${rgb},0)`);
      ctx.fillStyle = bloom;
      ctx.beginPath();
      ctx.arc(cx, cy, 0.09 * s, 0, Math.PI * 2);
      ctx.fill();
      const tri = 0.035 * s;
      const spin = t * 0.00008 * rate;
      ctx.fillStyle = accent;
      ctx.shadowColor = accent;
      ctx.shadowBlur = 16;
      ctx.beginPath();
      for (let i = 0; i < 3; i++) {
        const a = spin + i / 3 * Math.PI * 2;
        const x = cx + Math.cos(a) * tri,
          y = cy + Math.sin(a) * tri;
        if (i === 0) ctx.moveTo(x, y);else ctx.lineTo(x, y);
      }
      ctx.closePath();
      ctx.fill();
      ctx.shadowBlur = 0;
    };
    const loop = setInterval(() => {
      t += 33;
      draw();
    }, 33);
    draw();
    return () => {
      clearInterval(loop);
      if (raf) cancelAnimationFrame(raf);
    };
  }, [size]);
  return /*#__PURE__*/React.createElement("canvas", _extends({
    ref: ref,
    role: "img",
    "aria-label": "Reator — " + state,
    style: {
      width: size,
      height: size,
      display: "block",
      ...style
    }
  }, rest));
}
Object.assign(__ds_scope, { Reactor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/Reactor.jsx", error: String((e && e.message) || e) }); }

// components/hud/TerminalLog.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TONE = {
  sys: "var(--accent)",
  ok: "var(--good)",
  info: "var(--ink-2)",
  err: "var(--crit)",
  warn: "var(--warn)"
};

/* The live log. Timestamp, a three-letter tag, then the line. Scrolls itself
   to the newest entry; the prompt sits under it and does not blink when the
   process is busy. */
function TerminalLog({
  lines = [],
  prompt = "jarvis@os:~$",
  busy = false,
  height = 150,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [lines.length]);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      lineHeight: 1.5,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: {
      height,
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      display: "flex",
      flexDirection: "column",
      gap: "3px"
    }
  }, lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      gap: "6px",
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      flex: "none"
    }
  }, "[", l.at, "]"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      minWidth: "26px",
      color: TONE[l.tag] || "var(--ink-3)",
      textTransform: "uppercase",
      letterSpacing: "0.06em"
    }
  }, l.tag), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)",
      minWidth: 0,
      wordBreak: "break-word"
    }
  }, l.text)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "7px",
      alignItems: "center",
      marginTop: "6px",
      color: "var(--accent)"
    }
  }, /*#__PURE__*/React.createElement("span", null, prompt), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "6px",
      height: "11px",
      background: "var(--accent)",
      opacity: busy ? 1 : 0.45,
      animation: busy ? "none" : "hud-caret 1.1s steps(1,end) infinite"
    }
  })));
}
Object.assign(__ds_scope, { TerminalLog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/TerminalLog.jsx", error: String((e && e.message) || e) }); }

// components/hud/Tile.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A square action in the rail grid: glyph over a two-line label. Outlined,
   never filled — pressed and hover resolve to the same accent triple, as
   every other control in this system does. */
function Tile({
  icon,
  label,
  active = false,
  disabled = false,
  onClick,
  title,
  style,
  ...rest
}) {
  const [hot, setHot] = React.useState(false);
  const lit = hot && !disabled || active;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    title: title,
    disabled: disabled,
    "aria-pressed": active,
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false)
  }, rest, {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "5px",
      minHeight: "var(--tile-h)",
      padding: "7px 3px",
      background: lit ? "var(--control-bg-active)" : "transparent",
      border: "1px solid " + (disabled ? "var(--control-border-disabled)" : lit ? "var(--control-border-active)" : "var(--control-border)"),
      borderStyle: disabled ? "dashed" : "solid",
      borderRadius: "var(--r)",
      color: disabled ? "var(--control-fg-disabled)" : lit ? "var(--control-fg-active)" : "var(--control-fg)",
      fontFamily: "var(--font-mono)",
      fontSize: "7.5px",
      letterSpacing: "0.06em",
      textTransform: "uppercase",
      lineHeight: 1.35,
      textAlign: "center",
      cursor: disabled ? "default" : "pointer",
      transition: "var(--transition-control)",
      ...style
    }
  }), icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 15
  }) : null, /*#__PURE__*/React.createElement("span", null, label));
}
Object.assign(__ds_scope, { Tile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/hud/Tile.jsx", error: String((e && e.message) || e) }); }

// components/listing/BrainRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A picker row with a reason under it: which model, which tool server, which
   skill, which edit mode. The second line says why it can or cannot be used. */
function BrainRow({
  name,
  note,
  pressed,
  disabled,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const interactive = !!onClick && !disabled;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    disabled: disabled,
    "aria-pressed": String(!!pressed),
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "block",
      width: "100%",
      textAlign: "left",
      border: 0,
      borderRadius: "var(--r)",
      padding: "var(--space-2) var(--space-3)",
      fontFamily: "var(--font-mono)",
      background: pressed ? "var(--accent-soft)" : hover && interactive ? "rgba(255,255,255,0.05)" : "transparent",
      boxShadow: pressed ? "var(--edge-inset)" : "none",
      color: "var(--text-muted)",
      opacity: disabled ? 0.5 : 1,
      cursor: interactive ? "pointer" : "default",
      transition: "var(--transition-row)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      fontSize: "var(--size-base)",
      color: pressed ? "var(--accent)" : "var(--text-muted)"
    }
  }, name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      fontSize: "var(--size-tick)",
      color: "var(--text-muted)"
    }
  }, note));
}
Object.assign(__ds_scope, { BrainRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/BrainRow.jsx", error: String((e && e.message) || e) }); }

// components/listing/MetaGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Note metadata: uppercase keys, values that may wrap. Ruled top and bottom,
   never boxed. */
function MetaGrid({
  rows = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ul", _extends({}, rest, {
    style: {
      listStyle: "none",
      margin: "0 0 var(--space-6)",
      padding: "var(--space-5) 0",
      borderTop: "1px solid var(--border-hairline)",
      borderBottom: "1px solid var(--border-hairline)",
      display: "grid",
      gridTemplateColumns: "auto 1fr",
      gap: "5px var(--space-6)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-fine)",
      ...style
    }
  }), rows.map(([k, v], i) => [/*#__PURE__*/React.createElement("li", {
    key: "k" + i,
    style: {
      color: "var(--text-muted)",
      textTransform: "uppercase",
      letterSpacing: "var(--track-label)",
      fontSize: "var(--size-micro)"
    }
  }, k), /*#__PURE__*/React.createElement("li", {
    key: "v" + i,
    style: {
      color: k === "warning" ? "var(--warn)" : "var(--text-body)",
      wordBreak: "break-word"
    }
  }, v)]));
}
Object.assign(__ds_scope, { MetaGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/MetaGrid.jsx", error: String((e && e.message) || e) }); }

// components/listing/StatList.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The vault readout. Right-aligned, tabular, and honest: a capability that is
   absent says "missing" in --warn rather than being left out. */
function StatList({
  rows = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("dl", _extends({}, rest, {
    style: {
      display: "grid",
      gridTemplateColumns: "auto 1fr",
      gap: "5px var(--space-5)",
      margin: "var(--space-4) 0 0",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-key)",
      ...style
    }
  }), rows.map(([k, v], i) => [/*#__PURE__*/React.createElement("dt", {
    key: "k" + i,
    style: {
      color: "var(--text-muted)",
      textTransform: "uppercase",
      letterSpacing: "var(--track-label)",
      fontSize: "var(--size-micro)"
    }
  }, k), /*#__PURE__*/React.createElement("dd", {
    key: "v" + i,
    style: {
      margin: 0,
      textAlign: "right",
      fontVariantNumeric: "tabular-nums",
      color: v === "missing" ? "var(--warn)" : "var(--text-body)"
    }
  }, v)]));
}
Object.assign(__ds_scope, { StatList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/StatList.jsx", error: String((e && e.message) || e) }); }

// components/listing/Swatch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The only icon in the product: a 9px dot in a node-type colour. Hue says
   what a thing is; it never carries state. */
const TYPES = ["client", "project", "meeting", "invoice", "person", "note", "reference"];
function typeColour(type) {
  return TYPES.indexOf(type) >= 0 ? "var(--t-" + type + ")" : "var(--t-other)";
}
function Swatch({
  type = "other",
  colour,
  size = 9,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    style: {
      width: size + "px",
      height: size + "px",
      borderRadius: "50%",
      background: colour || typeColour(type),
      flex: "none",
      display: "inline-block",
      ...style
    }
  }));
}
Object.assign(__ds_scope, { typeColour, Swatch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/Swatch.jsx", error: String((e && e.message) || e) }); }

// components/answer/Cite.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The note an answer stands on. The only pill-shaped thing in the product. */
function Cite({
  type = "other",
  title,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-2)",
      maxWidth: "240px",
      border: "1px solid " + (hover ? "var(--control-border-active)" : "var(--border-hairline)"),
      borderRadius: "var(--r-pill)",
      background: hover ? "var(--control-bg-active)" : "transparent",
      color: hover ? "var(--control-fg-active)" : "var(--text-body)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-fine)",
      padding: "var(--chip-pad)",
      cursor: "pointer",
      transition: "var(--transition-control)",
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Swatch, {
    type: type
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, title));
}
Object.assign(__ds_scope, { Cite });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/answer/Cite.jsx", error: String((e && e.message) || e) }); }

// components/answer/ResultRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* An instant file hit: title and location on one line, the matching text
   under it in the proportional face. */
function ResultRow({
  type = "other",
  title,
  where,
  snippet,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "block",
      width: "100%",
      textAlign: "left",
      border: 0,
      background: hover ? "rgba(255,255,255,0.05)" : "transparent",
      color: "var(--text-body)",
      fontFamily: "var(--font-mono)",
      padding: "9px var(--space-4)",
      borderRadius: "var(--r)",
      cursor: "pointer",
      transition: "var(--transition-row)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginBottom: "4px"
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Swatch, {
    type: type
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-strong)",
      fontSize: "var(--size-base)"
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-muted)",
      fontSize: "var(--size-tick)",
      marginLeft: "auto"
    }
  }, where)), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-row)",
      lineHeight: "var(--lh-row)",
      color: "var(--text-muted)"
    }
  }, snippet));
}
Object.assign(__ds_scope, { ResultRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/answer/ResultRow.jsx", error: String((e && e.message) || e) }); }

// components/listing/Row.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The list row: direction, dot, label, tail. Bleeds 6px past the panel padding
   so its hover wash reaches the inner edge. Rows never move. */
function Row({
  type = "other",
  label,
  tail,
  dir,
  step,
  selected,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const lit = hover || selected;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--row-gap)",
      width: "100%",
      padding: "var(--row-pad-y) var(--row-pad-x)",
      margin: "0 calc(-1 * var(--row-pad-x))",
      border: 0,
      borderRadius: "var(--r)",
      background: lit ? "var(--surface-hover)" : "transparent",
      color: lit ? "var(--text-strong)" : "var(--text-body)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-row)",
      textAlign: "left",
      cursor: "pointer",
      transition: "var(--transition-row)",
      ...style
    }
  }), dir ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      fontSize: "9px",
      width: "12px",
      flex: "none"
    }
  }, dir) : null, step !== undefined ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      fontSize: "9.5px",
      width: "12px",
      flex: "none"
    }
  }, step) : null, /*#__PURE__*/React.createElement(__ds_scope.Swatch, {
    type: type
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, label), tail !== undefined && tail !== null ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      fontSize: "var(--size-tick)",
      flex: "none"
    }
  }, tail) : null);
}
Object.assign(__ds_scope, { Row });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/Row.jsx", error: String((e && e.message) || e) }); }

// components/listing/TypeRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A filter and a histogram in one row: switch a type off and the graph drops
   it; the bar shows its share of the vault. */
function TypeRow({
  type,
  name,
  count,
  share = 0,
  on = true,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button"
  }, rest, {
    "aria-pressed": String(!!on),
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--row-gap)",
      width: "100%",
      padding: "var(--row-pad-x)",
      margin: "0 calc(-1 * var(--row-pad-x))",
      border: 0,
      borderRadius: "var(--r)",
      background: hover ? "var(--surface-hover)" : "transparent",
      color: "var(--text-body)",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-row)",
      opacity: on ? 1 : "var(--dim-filter)",
      cursor: "pointer",
      transition: "background-color var(--t-row), opacity var(--t-row)",
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Swatch, {
    type: type
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      textTransform: "capitalize",
      letterSpacing: "var(--track-name)",
      textAlign: "left"
    }
  }, name || type), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      width: "100%",
      height: "var(--bar-h)",
      background: "var(--wash-2)",
      borderRadius: "1px",
      overflow: "hidden",
      marginTop: "3px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      height: "100%",
      width: Math.round(share * 100) + "%",
      background: on ? __ds_scope.typeColour(type) : "var(--ink-3)"
    }
  }))), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      fontSize: "var(--size-key)",
      fontVariantNumeric: "tabular-nums"
    }
  }, count));
}
Object.assign(__ds_scope, { TypeRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/listing/TypeRow.jsx", error: String((e && e.message) || e) }); }

// components/status/ReactorDial.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The reactor. Twelve index marks like a dial face, a 64-bin polar level meter
   with phosphor decay, and one arc that carries state: a slow crawl at idle, a
   fast sweep when live. Driven on a timer, not requestAnimationFrame — RAF
   stops dead in a backgrounded tab. */
const BINS = 64;
const TAU = Math.PI * 2;
const LIVE = ["listening", "thinking", "speaking"];
function ReactorDial({
  state = "idle",
  level = 0,
  sub = "",
  size = 176,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  const bins = React.useRef(new Float32Array(BINS));
  const sweep = React.useRef(0);
  const live = LIVE.indexOf(state) >= 0;
  const liveRef = React.useRef(live);
  const levelRef = React.useRef(level);
  liveRef.current = live;
  levelRef.current = level;
  const speaking = state === "speaking";
  const speakRef = React.useRef(speaking);
  speakRef.current = speaking;
  React.useEffect(() => {
    const canvas = ref.current;
    if (!canvas) return undefined;
    const ctx = canvas.getContext("2d");
    const draw = () => {
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      if (canvas.width !== size * dpr) {
        canvas.width = size * dpr;
        canvas.height = size * dpr;
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      ctx.clearRect(0, 0, size, size);
      const cx = size / 2,
        cy = size / 2,
        k = size / 176;
      const css = getComputedStyle(document.documentElement);
      const accent = css.getPropertyValue("--accent").trim() || "#93b4ff";
      const idle = css.getPropertyValue("--ink-3").trim() || "#5f6874";
      const on = liveRef.current;
      const colour = on ? accent : idle;
      ctx.strokeStyle = idle;
      ctx.lineWidth = 1;
      for (let i = 0; i < 12; i++) {
        const angle = i / 12 * TAU - Math.PI / 2;
        const long = i % 3 === 0;
        ctx.globalAlpha = long ? 0.36 : 0.16;
        ctx.beginPath();
        ctx.moveTo(cx + Math.cos(angle) * 84 * k, cy + Math.sin(angle) * 84 * k);
        ctx.lineTo(cx + Math.cos(angle) * (long ? 74 : 79) * k, cy + Math.sin(angle) * (long ? 74 : 79) * k);
        ctx.stroke();
      }
      const rIn = 56 * k,
        rOut = 79 * k;
      for (let i = 0; i < BINS; i++) {
        bins.current[i] *= 0.9;
        const angle = i / BINS * TAU - Math.PI / 2;
        const len = 1.5 + bins.current[i] * (rOut - rIn);
        ctx.strokeStyle = colour;
        ctx.globalAlpha = 0.2 + bins.current[i] * 0.75;
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(cx + Math.cos(angle) * rIn, cy + Math.sin(angle) * rIn);
        ctx.lineTo(cx + Math.cos(angle) * (rIn + len), cy + Math.sin(angle) * (rIn + len));
        ctx.stroke();
      }
      ctx.globalAlpha = on ? 0.5 : 0.2;
      ctx.strokeStyle = colour;
      ctx.lineWidth = 1;
      ctx.beginPath();
      ctx.arc(cx, cy, 46 * k, 0, TAU);
      ctx.stroke();
      const t = performance.now() / (on ? 800 : 6000);
      const head = t % TAU;
      ctx.strokeStyle = colour;
      ctx.globalAlpha = on ? 1 : 0.55;
      ctx.lineWidth = 2.5;
      ctx.lineCap = "round";
      if (on) {
        ctx.shadowColor = accent;
        ctx.shadowBlur = 10;
      }
      ctx.beginPath();
      ctx.arc(cx, cy, 46 * k, head, head + (on ? 1.35 : 0.5));
      ctx.stroke();
      ctx.shadowBlur = 0;
      ctx.lineCap = "butt";
      ctx.globalAlpha = 1;
    };
    const tick = () => {
      if (!liveRef.current) {
        sweep.current = (sweep.current + 0.6) % BINS;
        const at = Math.floor(sweep.current);
        bins.current[at] = Math.max(bins.current[at], 0.22);
      } else {
        const spread = speakRef.current ? 0.5 : 1;
        for (let i = 0; i < BINS; i++) {
          bins.current[i] = Math.max(bins.current[i], Math.random() * levelRef.current * spread);
        }
      }
      draw();
    };
    const id = setInterval(tick, 33);
    tick();
    return () => clearInterval(id);
  }, [size]);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      position: "relative",
      display: "grid",
      placeItems: "center",
      margin: "4px auto var(--space-9)",
      width: size + "px",
      height: size + "px",
      ...style
    }
  }), /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: 0,
      width: size + "px",
      height: size + "px"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      display: "grid",
      placeItems: "center",
      gap: "4px",
      textAlign: "center",
      pointerEvents: "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-key)",
      letterSpacing: "var(--track-state)",
      textTransform: "uppercase",
      color: live ? "var(--accent)" : "var(--text-body)"
    }
  }, state), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-micro)",
      letterSpacing: "0.08em",
      color: "var(--text-muted)",
      maxWidth: Math.round(size * 0.74) + "px"
    }
  }, sub)));
}
Object.assign(__ds_scope, { ReactorDial });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/status/ReactorDial.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/HudFrame.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* v2's panel. Not a box — a labelled rule across the top whose right end
   cuts away on a diagonal, with the content hanging beneath it. Set foot to
   close the block with a mirrored rule. */
function HudFrame({
  label,
  right,
  foot = false,
  children,
  style,
  ...rest
}) {
  const rule = flip => /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--hud-frame-gap)",
      pointerEvents: "none"
    }
  }, !flip && /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--hud-rule)",
      height: "var(--hud-cap)",
      background: "var(--hud-line)",
      flex: "none"
    }
  }), !flip && label && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      letterSpacing: "var(--track-eyebrow)",
      textTransform: "uppercase",
      color: "var(--text-label)",
      whiteSpace: "nowrap",
      flex: "none"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: "var(--hud-rule)",
      background: flip ? "var(--hud-line-dim)" : "var(--hud-line)"
    }
  }), right && !flip ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      letterSpacing: "0.06em",
      color: "var(--text-muted)",
      flex: "none"
    }
  }, right) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--hud-cut)",
      height: "var(--hud-cut-drop)",
      flex: "none",
      background: `linear-gradient(to bottom ${flip ? "left" : "right"}, transparent calc(50% - 0.5px), ${flip ? "var(--hud-line-dim)" : "var(--hud-line)"} calc(50% - 0.5px), ${flip ? "var(--hud-line-dim)" : "var(--hud-line)"} calc(50% + 0.5px), transparent calc(50% + 0.5px))`
    }
  }));
  return /*#__PURE__*/React.createElement("section", _extends({}, rest, {
    style: {
      fontFamily: "var(--font-mono)",
      color: "var(--text-body)",
      ...style
    }
  }), rule(false), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--hud-body-pad)"
    }
  }, children), foot ? rule(true) : null);
}
Object.assign(__ds_scope, { HudFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/HudFrame.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* SUBSTITUTION — the source codebase ships no icons at all (see readme,
   ICONOGRAPHY). v2's tile grids need glyphs, so Lucide (1.5px stroke, round
   caps, 24px box, MIT) stands in. The path data is copied into this file and
   into assets/icons/*.svg rather than linked from a CDN, so a glyph renders
   with no network and survives thumbnail capture. Stroke is currentColor, so
   an icon takes the same state colour as the control around it. */
const GLYPHS = {
  "mic": "<path d=\"M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z\" /> <path d=\"M19 10v2a7 7 0 0 1-14 0v-2\" /> <line x1=\"12\" x2=\"12\" y1=\"19\" y2=\"22\" />",
  "volume-x": "<path d=\"M11 4.702a.705.705 0 0 0-1.203-.498L6.413 7.587A1.4 1.4 0 0 1 5.416 8H3a1 1 0 0 0-1 1v6a1 1 0 0 0 1 1h2.416a1.4 1.4 0 0 1 .997.413l3.383 3.384A.705.705 0 0 0 11 19.298z\" /> <line x1=\"22\" x2=\"16\" y1=\"9\" y2=\"15\" /> <line x1=\"16\" x2=\"22\" y1=\"9\" y2=\"15\" />",
  "scan-line": "<path d=\"M3 7V5a2 2 0 0 1 2-2h2\" /> <path d=\"M17 3h2a2 2 0 0 1 2 2v2\" /> <path d=\"M21 17v2a2 2 0 0 1-2 2h-2\" /> <path d=\"M7 21H5a2 2 0 0 1-2-2v-2\" /> <path d=\"M7 12h10\" />",
  "file-search": "<path d=\"M14 2v4a2 2 0 0 0 2 2h4\" /> <path d=\"M4.268 21a2 2 0 0 0 1.727 1H18a2 2 0 0 0 2-2V7l-5-5H6a2 2 0 0 0-2 2v3\" /> <path d=\"m9 18-1.5-1.5\" /> <circle cx=\"5\" cy=\"14\" r=\"3\" />",
  "brain-circuit": "<path d=\"M12 5a3 3 0 1 0-5.997.125 4 4 0 0 0-2.526 5.77 4 4 0 0 0 .556 6.588A4 4 0 1 0 12 18Z\" /> <path d=\"M9 13a4.5 4.5 0 0 0 3-4\" /> <path d=\"M6.003 5.125A3 3 0 0 0 6.401 6.5\" /> <path d=\"M3.477 10.896a4 4 0 0 1 .585-.396\" /> <path d=\"M6 18a4 4 0 0 1-1.967-.516\" /> <path d=\"M12 13h4\" /> <path d=\"M12 18h6a2 2 0 0 1 2 2v1\" /> <path d=\"M12 8h8\" /> <path d=\"M16 8V5a2 2 0 0 1 2-2\" /> <circle cx=\"16\" cy=\"13\" r=\".5\" /> <circle cx=\"18\" cy=\"3\" r=\".5\" /> <circle cx=\"20\" cy=\"21\" r=\".5\" /> <circle cx=\"20\" cy=\"8\" r=\".5\" />",
  "square-terminal": "<path d=\"m7 11 2-2-2-2\" /> <path d=\"M11 13h4\" /> <rect width=\"18\" height=\"18\" x=\"3\" y=\"3\" rx=\"2\" ry=\"2\" />",
  "database": "<ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\" /> <path d=\"M3 5V19A9 3 0 0 0 21 19V5\" /> <path d=\"M3 12A9 3 0 0 0 21 12\" />",
  "network": "<rect x=\"16\" y=\"16\" width=\"6\" height=\"6\" rx=\"1\" /> <rect x=\"2\" y=\"16\" width=\"6\" height=\"6\" rx=\"1\" /> <rect x=\"9\" y=\"2\" width=\"6\" height=\"6\" rx=\"1\" /> <path d=\"M5 16v-3a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v3\" /> <path d=\"M12 12V8\" />",
  "workflow": "<rect width=\"8\" height=\"8\" x=\"3\" y=\"3\" rx=\"2\" /> <path d=\"M7 11v4a2 2 0 0 0 2 2h4\" /> <rect width=\"8\" height=\"8\" x=\"13\" y=\"13\" rx=\"2\" />",
  "settings": "<path d=\"M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z\" /> <circle cx=\"12\" cy=\"12\" r=\"3\" />",
  "cloud-rain": "<path d=\"M4 14.899A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 2.5 8.242\" /> <path d=\"M16 14v6\" /> <path d=\"M8 14v6\" /> <path d=\"M12 16v6\" />",
  "arrow-right": "<path d=\"M5 12h14\" /> <path d=\"m12 5 7 7-7 7\" />",
  "x": "<path d=\"M18 6 6 18\" /> <path d=\"m6 6 12 12\" />"
};
function Icon({
  name,
  size = 15,
  strokeWidth = 1.7,
  style,
  ...rest
}) {
  const inner = GLYPHS[name];
  if (!inner) return null;
  return /*#__PURE__*/React.createElement("svg", _extends({
    "aria-hidden": "true",
    viewBox: "0 0 24 24",
    width: size,
    height: size,
    fill: "none",
    stroke: "currentColor",
    strokeWidth: strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, rest, {
    style: {
      display: "block",
      flex: "none",
      ...style
    },
    dangerouslySetInnerHTML: {
      __html: inner
    }
  }));
}
Object.assign(__ds_scope, { Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/Icon.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/Meter.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A labelled bar with a value at the end. Segmented by default — the bar is
   cut into 3px ticks, which is how the reference panel reads a load. */
function Meter({
  label,
  value = 0,
  display,
  tone = "accent",
  segmented = true,
  style,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, value));
  const colour = tone === "warn" ? "var(--warn)" : tone === "crit" ? "var(--crit)" : tone === "muted" ? "var(--ink-3)" : "var(--accent)";
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "9px",
      padding: "3px 0",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--meter-label-w)",
      flex: "none",
      color: "var(--text-label)",
      letterSpacing: "0.08em",
      textTransform: "uppercase"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: "var(--meter-h)",
      background: "var(--wash)",
      position: "relative",
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      right: 100 - pct + "%",
      background: colour,
      transition: "right 400ms linear",
      ...(segmented ? {
        WebkitMaskImage: "repeating-linear-gradient(90deg, #000 0 var(--meter-seg), transparent var(--meter-seg) calc(var(--meter-seg) + 2px))",
        maskImage: "repeating-linear-gradient(90deg, #000 0 var(--meter-seg), transparent var(--meter-seg) calc(var(--meter-seg) + 2px))"
      } : null)
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      minWidth: "30px",
      textAlign: "right",
      color: "var(--text-strong)",
      fontVariantNumeric: "tabular-nums"
    }
  }, display !== undefined ? display : pct + "%"));
}
Object.assign(__ds_scope, { Meter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/Meter.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/RadialGauge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A ring gauge with its value in the middle. Stroke is a dasharray, so the
   arc animates by colour crossfade only — nothing rotates. */
function RadialGauge({
  value = 0,
  size = 54,
  label,
  caption,
  style,
  ...rest
}) {
  const pct = Math.max(0, Math.min(100, value));
  const r = (size - 6) / 2;
  const c = 2 * Math.PI * r;
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "11px",
      fontFamily: "var(--font-mono)",
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size,
      flex: "none"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--wash-2)",
    strokeWidth: "var(--gauge-stroke)"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--accent)",
    strokeWidth: "var(--gauge-stroke)",
    strokeDasharray: `${c * pct / 100} ${c}`,
    strokeLinecap: "butt",
    style: {
      transition: "stroke-dasharray 400ms linear"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "1px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-base)",
      color: "var(--accent)",
      textShadow: "var(--hud-glow-soft)",
      fontVariantNumeric: "tabular-nums"
    }
  }, pct, "%"), label ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "8px",
      letterSpacing: "0.14em",
      textTransform: "uppercase",
      color: "var(--text-muted)"
    }
  }, label) : null)), caption ? /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, caption) : null);
}
Object.assign(__ds_scope, { RadialGauge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/RadialGauge.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/Reactor.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* v2's reactor. Concentric rings around a triangle core, drawn on canvas.
   v1's ReactorDial was a single bezel with one state arc; this is the
   reference's stack — a tick bezel, gapped rings, an arc pair and a slow
   orbit — and it still carries state only: idle crawls, thinking sweeps.

   Runs on the same 33ms interval the rest of this system uses rather than
   requestAnimationFrame, which stops dead when the frame is off screen. */
const RINGS = [{
  r: 0.115,
  w: 1,
  a: 0.42,
  gaps: 0,
  spin: 0
}, {
  r: 0.175,
  w: 1,
  a: 0.20,
  gaps: 3,
  spin: 0.10,
  gapArc: 0.30
}, {
  r: 0.245,
  w: 1,
  a: 0.30,
  ticks: 48,
  tick: 4
}, {
  r: 0.325,
  w: 1.4,
  a: 0.16,
  gaps: 2,
  spin: -0.06,
  gapArc: 0.55
}, {
  r: 0.395,
  w: 1,
  a: 0.10,
  ticks: 96,
  tick: 2.5
}, {
  r: 0.475,
  w: 1,
  a: 0.13,
  gaps: 4,
  spin: 0.04,
  gapArc: 0.18
}, {
  r: 0.60,
  w: 1,
  a: 0.07,
  gaps: 0
}];
function Reactor({
  size = 420,
  state = "idle",
  level = 0.5,
  tilt = 0.46,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  const live = React.useRef({
    state,
    level,
    tilt
  });
  live.current = {
    state,
    level,
    tilt
  };
  const RS = 0.78; /* the ring stack fills 94% of the box, corners left free for readouts */

  React.useEffect(() => {
    const canvas = ref.current;
    const ctx = canvas.getContext("2d");
    const css = getComputedStyle(document.documentElement);
    const accent = css.getPropertyValue("--accent").trim() || "#3ce88f";
    const rgb = (css.getPropertyValue("--accent-rgb").trim() || "60,232,143").replace(/\s/g, "");
    const dots = Array.from({
      length: 26
    }, (_, i) => ({
      a: i / 26 * Math.PI * 2 + i * 0.7,
      r: 0.035 + i * 37 % 100 / 100 * 0.10,
      s: 0.15 + i * 53 % 100 / 100 * 0.5
    }));
    let t = 0,
      raf = null;
    const draw = () => {
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      const s = canvas.clientWidth || size;
      if (canvas.width !== Math.round(s * dpr)) {
        canvas.width = Math.round(s * dpr);
        canvas.height = Math.round(s * dpr);
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      ctx.clearRect(0, 0, s, s);
      const cx = s / 2,
        cy = s / 2;
      const busy = live.current.state !== "idle";
      const lv = Math.max(0, Math.min(1, live.current.level));
      const ty = Math.max(0.12, Math.min(1, live.current.tilt));
      const rate = busy ? 4.2 : 1;
      const gain = 0.55 + lv * 0.45 + (busy ? 0.25 : 0);

      /* The whole stack lies on a plane seen at an angle: every ring is an
         ellipse of the same radius squashed to `tilt` vertically, and every
         mark sits on that plane. Only the core stands upright. */
      const px = (a, R) => cx + Math.cos(a) * R;
      const py = (a, R) => cy + Math.sin(a) * R * ty;
      for (const ring of RINGS) {
        const R = ring.r * s * RS;
        ctx.lineWidth = ring.w;
        ctx.strokeStyle = `rgba(${rgb},${ring.a * gain})`;
        if (ring.ticks) {
          for (let i = 0; i < ring.ticks; i++) {
            const a = i / ring.ticks * Math.PI * 2 + t * 0.00004 * rate;
            const major = i % 4 === 0;
            const len = major ? ring.tick * 1.9 : ring.tick;
            ctx.beginPath();
            ctx.moveTo(px(a, R), py(a, R));
            ctx.lineTo(px(a, R + len), py(a, R + len));
            ctx.stroke();
          }
        } else if (ring.gaps) {
          const step = Math.PI * 2 / ring.gaps;
          const arc = step * (1 - ring.gapArc);
          for (let i = 0; i < ring.gaps; i++) {
            const a0 = i * step + t * 0.00012 * ring.spin * rate * 12;
            ctx.beginPath();
            ctx.ellipse(cx, cy, R, R * ty, 0, a0, a0 + arc);
            ctx.stroke();
          }
        } else {
          ctx.beginPath();
          ctx.ellipse(cx, cy, R, R * ty, 0, 0, Math.PI * 2);
          ctx.stroke();
        }
      }

      /* the state arc — a crawl at idle, a sweep when the model is working */
      const period = busy ? 800 : 6000;
      const head = t % period / period * Math.PI * 2;
      const arcR = 0.245 * s * RS;
      ctx.lineWidth = 1.6;
      ctx.strokeStyle = accent;
      ctx.shadowColor = accent;
      ctx.shadowBlur = 10;
      ctx.beginPath();
      ctx.ellipse(cx, cy, arcR, arcR * ty, 0, head, head + (busy ? 1.15 : 0.42));
      ctx.stroke();
      ctx.shadowBlur = 0;

      /* drifting motes, on the same plane */
      for (const d of dots) {
        const a = d.a + t * 0.00022 * d.s * rate;
        const R = d.r * s * RS * (1 + Math.sin(t * 0.0006 + d.a) * 0.08);
        ctx.fillStyle = `rgba(${rgb},${0.30 + Math.sin(t * 0.001 + d.a) * 0.18})`;
        ctx.beginPath();
        ctx.arc(px(a, R), py(a, R), 1.15, 0, Math.PI * 2);
        ctx.fill();
      }

      /* core: a soft bloom and the triangle */
      const bloom = ctx.createRadialGradient(cx, cy, 0, cx, cy, 0.09 * s);
      bloom.addColorStop(0, `rgba(${rgb},${0.30 * gain})`);
      bloom.addColorStop(1, `rgba(${rgb},0)`);
      ctx.fillStyle = bloom;
      ctx.beginPath();
      ctx.arc(cx, cy, 0.09 * s, 0, Math.PI * 2);
      ctx.fill();
      const tri = 0.035 * s;
      const spin = t * 0.00008 * rate;
      ctx.fillStyle = accent;
      ctx.shadowColor = accent;
      ctx.shadowBlur = 16;
      ctx.beginPath();
      for (let i = 0; i < 3; i++) {
        const a = spin + i / 3 * Math.PI * 2;
        const x = cx + Math.cos(a) * tri,
          y = cy + Math.sin(a) * tri;
        if (i === 0) ctx.moveTo(x, y);else ctx.lineTo(x, y);
      }
      ctx.closePath();
      ctx.fill();
      ctx.shadowBlur = 0;
    };
    const loop = setInterval(() => {
      t += 33;
      draw();
    }, 33);
    draw();
    return () => {
      clearInterval(loop);
      if (raf) cancelAnimationFrame(raf);
    };
  }, [size]);
  return /*#__PURE__*/React.createElement("canvas", _extends({
    ref: ref,
    role: "img",
    "aria-label": "Reator — " + state,
    style: {
      width: size,
      height: size,
      display: "block",
      ...style
    }
  }, rest));
}
Object.assign(__ds_scope, { Reactor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/Reactor.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/TerminalLog.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TONE = {
  sys: "var(--accent)",
  ok: "var(--good)",
  info: "var(--ink-2)",
  err: "var(--crit)",
  warn: "var(--warn)"
};

/* The live log. Timestamp, a three-letter tag, then the line. Scrolls itself
   to the newest entry; the prompt sits under it and does not blink when the
   process is busy. */
function TerminalLog({
  lines = [],
  prompt = "jarvis@os:~$",
  busy = false,
  height = 150,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [lines.length]);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      lineHeight: 1.5,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: {
      height,
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      display: "flex",
      flexDirection: "column",
      gap: "3px"
    }
  }, lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      gap: "6px",
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      flex: "none"
    }
  }, "[", l.at, "]"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      minWidth: "26px",
      color: TONE[l.tag] || "var(--ink-3)",
      textTransform: "uppercase",
      letterSpacing: "0.06em"
    }
  }, l.tag), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)",
      minWidth: 0,
      wordBreak: "break-word"
    }
  }, l.text)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "7px",
      alignItems: "center",
      marginTop: "6px",
      color: "var(--accent)"
    }
  }, /*#__PURE__*/React.createElement("span", null, prompt), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "6px",
      height: "11px",
      background: "var(--accent)",
      opacity: busy ? 1 : 0.45,
      animation: busy ? "none" : "hud-caret 1.1s steps(1,end) infinite"
    }
  })));
}
Object.assign(__ds_scope, { TerminalLog });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/TerminalLog.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/components/hud/Tile.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* A square action in the rail grid: glyph over a two-line label. Outlined,
   never filled — pressed and hover resolve to the same accent triple, as
   every other control in this system does. */
function Tile({
  icon,
  label,
  active = false,
  disabled = false,
  onClick,
  title,
  style,
  ...rest
}) {
  const [hot, setHot] = React.useState(false);
  const lit = hot && !disabled || active;
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    title: title,
    disabled: disabled,
    "aria-pressed": active,
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHot(true),
    onMouseLeave: () => setHot(false)
  }, rest, {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "5px",
      minHeight: "var(--tile-h)",
      padding: "7px 3px",
      background: lit ? "var(--control-bg-active)" : "transparent",
      border: "1px solid " + (disabled ? "var(--control-border-disabled)" : lit ? "var(--control-border-active)" : "var(--control-border)"),
      borderStyle: disabled ? "dashed" : "solid",
      borderRadius: "var(--r)",
      color: disabled ? "var(--control-fg-disabled)" : lit ? "var(--control-fg-active)" : "var(--control-fg)",
      fontFamily: "var(--font-mono)",
      fontSize: "7.5px",
      letterSpacing: "0.06em",
      textTransform: "uppercase",
      lineHeight: 1.35,
      textAlign: "center",
      cursor: disabled ? "default" : "pointer",
      transition: "var(--transition-control)",
      ...style
    }
  }), icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 15
  }) : null, /*#__PURE__*/React.createElement("span", null, label));
}
Object.assign(__ds_scope, { Tile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/components/hud/Tile.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/CenterStage.jsx
try { (() => {
/* Centre stage — greeting, the reactor with the vault's four headline
   figures pinned around it, and the ask bar. Everything the operator types
   happens here; the rails only report. */
const {
  Reactor,
  Icon,
  AnswerBlock,
  Cite,
  ResultRow,
  Button
} = window.JARVISDesignSystem_ad8200;
const readout = (label, value, pos) => /*#__PURE__*/React.createElement("div", {
  key: label,
  style: {
    position: "absolute",
    maxWidth: "46%",
    ...pos
  }
}, /*#__PURE__*/React.createElement("div", {
  style: {
    fontSize: "8px",
    letterSpacing: "0.16em",
    textTransform: "uppercase",
    color: "var(--text-label)",
    whiteSpace: "nowrap"
  }
}, label), /*#__PURE__*/React.createElement("div", {
  style: {
    fontSize: "15px",
    color: "var(--accent)",
    textShadow: "var(--hud-glow-soft)",
    marginTop: "4px",
    fontVariantNumeric: "tabular-nums",
    whiteSpace: "nowrap"
  }
}, value));
function CenterStage({
  greeting,
  phase,
  level,
  query,
  onQuery,
  onSubmit,
  results,
  onOpen,
  answer,
  onCite,
  onClear,
  vaultStats
}) {
  const inputRef = React.useRef(null);
  const stageRef = React.useRef(null);
  const [focus, setFocus] = React.useState(false);
  const [dial, setDial] = React.useState(0);
  const busy = phase === "thinking";
  const showResults = !busy && !answer && query.trim().length > 1 && results.length > 0;

  /* The reactor is the one thing here that must never collide with a rail or
     the greeting, so it is measured rather than fixed — and re-measured after
     every render, because opening the answer panel steals the stage's height
     without any window resize. */
  const fit = React.useCallback(() => {
    const el = stageRef.current;
    if (!el) return;
    const next = Math.max(0, Math.min(420, el.clientWidth - 24, el.clientHeight - 8));
    setDial(d => Math.abs(d - next) < 2 ? d : next);
  }, []);
  React.useLayoutEffect(fit);
  React.useEffect(() => {
    const el = stageRef.current;
    if (!el || typeof ResizeObserver === "undefined") return;
    const ro = new ResizeObserver(fit);
    ro.observe(el);
    window.addEventListener("resize", fit);
    return () => {
      ro.disconnect();
      window.removeEventListener("resize", fit);
    };
  }, [fit]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 2,
      top: 0,
      bottom: 0,
      left: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      right: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      pointerEvents: "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "58px",
      display: "flex",
      gap: "13px",
      alignItems: "flex-start",
      pointerEvents: "auto"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "26px",
      height: "26px",
      flex: "none",
      border: "1px solid var(--accent-line)",
      borderRadius: "var(--r)",
      background: "var(--accent-soft)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "9px",
      height: "9px",
      background: "var(--accent)",
      boxShadow: "var(--hud-glow)"
    }
  })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "19px",
      letterSpacing: "0.02em",
      color: "var(--accent)",
      textShadow: "var(--hud-glow-soft)"
    }
  }, greeting), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-base)",
      color: "var(--text-body)",
      marginTop: "3px"
    }
  }, "Pergunte qualquer coisa sobre o que est\xE1 no vault."))), /*#__PURE__*/React.createElement("div", {
    ref: stageRef,
    style: {
      flex: 1,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: "100%",
      minHeight: 0,
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: dial,
      height: dial,
      flex: "none"
    }
  }, /*#__PURE__*/React.createElement(Reactor, {
    size: dial,
    state: phase === "idle" ? "idle" : "thinking",
    level: level,
    style: {
      position: "absolute",
      inset: 0
    }
  }), dial >= 260 ? /*#__PURE__*/React.createElement(React.Fragment, null, readout("Índice", vaultStats.notes + " notas", {
    left: 0,
    top: "19%"
  }), readout("Cobertura", "93%", {
    right: 0,
    top: "19%",
    textAlign: "right"
  }), readout("Vínculos", vaultStats.links, {
    left: 0,
    bottom: "19%"
  }), readout("Varredura", "0,4s", {
    right: 0,
    bottom: "19%",
    textAlign: "right"
  })) : null)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "min(var(--ask-w), 100%)",
      paddingBottom: "26px",
      pointerEvents: "auto"
    }
  }, answer ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: "12px",
      maxHeight: "34vh",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      background: "var(--surface-panel)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid var(--border-hairline)",
      borderTopColor: "var(--border-lit)",
      borderRadius: "var(--r)",
      padding: "var(--pad)"
    }
  }, /*#__PURE__*/React.createElement(AnswerBlock, {
    kind: answer.kind,
    meta: answer.meta,
    body: answer.body
  }, answer.citations.map(id => /*#__PURE__*/React.createElement(Cite, {
    key: id,
    type: answer.types[id],
    title: answer.titles[id],
    onClick: () => onCite(id)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "12px",
      display: "flex",
      gap: "6px"
    }
  }, /*#__PURE__*/React.createElement(Button, {
    size: "ghost",
    onClick: onClear
  }, "nova conversa"))) : null, showResults ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: "12px",
      maxHeight: "30vh",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      background: "var(--surface-panel)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid var(--border-hairline)",
      borderTopColor: "var(--border-lit)",
      borderRadius: "var(--r)",
      padding: "10px var(--pad)"
    }
  }, results.map(r => /*#__PURE__*/React.createElement(ResultRow, {
    key: r.id,
    type: r.type,
    title: r.title,
    where: r.rel,
    snippet: r.snippet,
    onClick: () => onOpen(r.id)
  }))) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      marginBottom: "10px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "7px",
      fontSize: "var(--size-tick)",
      letterSpacing: "0.16em",
      textTransform: "uppercase",
      color: busy ? "var(--accent)" : "var(--text-label)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "5px",
      height: "5px",
      borderRadius: "50%",
      background: busy ? "var(--accent)" : "var(--good)",
      boxShadow: "var(--hud-glow-soft)"
    }
  }), busy ? "pensando…" : "sistemas operacionais")), /*#__PURE__*/React.createElement("form", {
    onSubmit: e => {
      e.preventDefault();
      onSubmit();
    },
    style: {
      display: "flex",
      alignItems: "center",
      gap: "10px",
      height: "var(--ask-h)",
      padding: "0 12px 0 14px",
      background: "var(--surface-raised)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid " + (focus ? "var(--accent-line)" : "var(--border-hairline)"),
      borderRadius: "var(--r)",
      transition: "border-color var(--t-field) linear"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "mic",
    size: 14,
    style: {
      color: "var(--ink-3)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "1px",
      height: "18px",
      background: "var(--hairline)",
      flex: "none"
    }
  }), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    value: query,
    onChange: e => onQuery(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    placeholder: "O que vamos construir hoje?",
    "aria-label": "Pergunta",
    disabled: busy,
    style: {
      flex: 1,
      minWidth: 0,
      background: "transparent",
      border: 0,
      outline: "none",
      color: "var(--text-strong)",
      fontFamily: "var(--font-mono)",
      fontSize: "13.5px",
      letterSpacing: "0.01em"
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "submit",
    disabled: busy || !query.trim(),
    "aria-label": "Enviar",
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: "28px",
      height: "28px",
      background: "transparent",
      border: "1px solid " + (query.trim() && !busy ? "var(--accent-line)" : "var(--control-border)"),
      borderRadius: "var(--r)",
      color: query.trim() && !busy ? "var(--accent)" : "var(--control-fg-disabled)",
      cursor: query.trim() && !busy ? "pointer" : "default",
      transition: "var(--transition-control)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "arrow-right",
    size: 14
  })))));
}
Object.assign(window, {
  CenterStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/CenterStage.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/GraphStage.jsx
try { (() => {
/* GraphStage — the canvas graph, ported from Jarvis/ui/graph.js.
   Same forces, same colours, same label placement, same dim/glow rules;
   the spatial-grid optimisation is dropped because 44 nodes do not need it. */
const {
  useRef,
  useEffect
} = React;
const REPULSION = 2000,
  CUTOFF = 240,
  SPRING_LEN = 58,
  SPRING_K = 0.036;
const GRAVITY = 0.0105,
  DAMPING = 0.83,
  ALPHA_DECAY = 0.014,
  BREATH = 0.0065;
const BREATH_MS = 11000,
  PRESETTLE = 450,
  R_MIN = 3.4,
  R_MAX = 17;
const LABEL_MIN_R = 6.5,
  LABEL_PAD = 4,
  LABEL_CAP = 26,
  DIM = 0.1;
const PULSE_EVERY = 3400,
  PULSE_MS = 1500;
const INSETS = {
  left: 300,
  right: 300,
  top: 130,
  bottom: 150
};
const TYPES = ["client", "project", "meeting", "invoice", "person", "note", "reference"];
function hash(str) {
  let h = 2166136261;
  for (let i = 0; i < str.length; i++) {
    h ^= str.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return (h >>> 0) / 4294967296;
}
const clamp = (v, lo, hi) => v < lo ? lo : v > hi ? hi : v;
function GraphStage({
  nodes,
  edges,
  hidden,
  focused,
  pathIds,
  onSelect,
  onHover,
  opacity = 1,
  sparseLabels = false
}) {
  const ref = useRef(null);
  const sim = useRef(null);
  const props = useRef({});
  props.current = {
    hidden,
    focused,
    pathIds,
    onSelect,
    onHover,
    sparseLabels
  };
  useEffect(() => {
    const canvas = ref.current;
    const ctx = canvas.getContext("2d", {
      alpha: true
    });
    const css = getComputedStyle(document.documentElement);
    const colour = {
      other: css.getPropertyValue("--t-other").trim()
    };
    TYPES.forEach(t => {
      colour[t] = css.getPropertyValue("--t-" + t).trim();
    });
    const accent = css.getPropertyValue("--accent").trim();
    const accentRgb = (css.getPropertyValue("--accent-rgb").trim() || "60,232,143").replace(/\s/g, "");
    const plateC = css.getPropertyValue("--plate").trim() || "rgba(4,9,7,.74)";
    const ink3 = css.getPropertyValue("--ink-3").trim();
    const colourFor = t => colour[t] || colour.other;

    /* Nodes are drawn as lit spheres, not flat dots: a wide halo, a body whose
       highlight sits up and to the left, and a cross flare on the hubs. Hue is
       still taxonomy and nothing else — only the rendering changed. */
    const rgbOf = hex => {
      const h = hex.replace("#", "");
      const v = h.length === 3 ? h.split("").map(c => c + c).join("") : h;
      return [parseInt(v.slice(0, 2), 16), parseInt(v.slice(2, 4), 16), parseInt(v.slice(4, 6), 16)];
    };
    const mix = (hex, target, k) => {
      const [r, g, b] = rgbOf(hex);
      return "rgb(" + Math.round(r + (target - r) * k) + "," + Math.round(g + (target - g) * k) + "," + Math.round(b + (target - b) * k) + ")";
    };
    const soft = (hex, a) => {
      const [r, g, b] = rgbOf(hex);
      return "rgba(" + r + "," + g + "," + b + "," + a + ")";
    };
    const size = () => ({
      w: canvas.clientWidth || 1,
      h: canvas.clientHeight || 1
    });
    const view = () => {
      const {
        w,
        h
      } = size();
      const x = INSETS.left,
        y = INSETS.top;
      const vw = Math.max(160, w - INSETS.left - INSETS.right);
      const vh = Math.max(160, h - INSETS.top - INSETS.bottom);
      return {
        x,
        y,
        w: vw,
        h: vh,
        cx: x + vw / 2,
        cy: y + vh / 2
      };
    };
    const v = view();
    const n = Math.max(1, nodes.length);
    const spread = Math.min(v.w, v.h) * 0.42;
    const pts = nodes.map((raw, i) => {
      const seed = hash(raw.id);
      const angle = i * 2.399963 + seed * 0.9;
      const radius = Math.sqrt((i + 0.5) / n) * spread;
      return {
        ...raw,
        x: v.cx + Math.cos(angle) * radius,
        y: v.cy + Math.sin(angle) * radius,
        vx: 0,
        vy: 0,
        fixed: false,
        r: clamp(R_MIN + Math.sqrt(raw.degree || 0) * 2.35, R_MIN, R_MAX)
      };
    });
    const byId = new Map(pts.map(p => [p.id, p]));
    const links = edges.map(e => ({
      a: byId.get(e.source),
      b: byId.get(e.target)
    })).filter(e => e.a && e.b);
    const state = {
      pts,
      links,
      byId,
      alpha: 1,
      scale: 1,
      tx: 0,
      ty: 0,
      hovered: null,
      pulses: [],
      lastPulse: 0
    };
    sim.current = state;
    const visible = p => !(props.current.hidden || new Set()).has(p.type);
    const edgeKey = (a, b) => a < b ? a + "\u0000" + b : b + "\u0000" + a;
    function tick(stepScale) {
      const active = state.pts.filter(visible);
      if (!active.length) return;
      state.alpha += (BREATH - state.alpha) * ALPHA_DECAY;
      const a = state.alpha * (stepScale || 1);
      const sway = 1 + Math.sin(performance.now() / BREATH_MS * Math.PI * 2) * 0.03;
      for (let i = 0; i < active.length; i++) {
        for (let j = i + 1; j < active.length; j++) {
          const p = active[i],
            q = active[j];
          let dx = p.x - q.x,
            dy = p.y - q.y,
            d2 = dx * dx + dy * dy;
          if (d2 > CUTOFF * CUTOFF) continue;
          if (d2 < 0.01) {
            dx = (hash(p.id) - 0.5) * 0.5;
            dy = (hash(q.id) - 0.5) * 0.5;
            d2 = dx * dx + dy * dy + 0.01;
          }
          const d = Math.sqrt(d2);
          const force = REPULSION * a * (p.r * q.r / 36) / d2;
          p.vx += dx / d * force;
          p.vy += dy / d * force;
          q.vx -= dx / d * force;
          q.vy -= dy / d * force;
        }
      }
      const rest = SPRING_LEN * sway;
      for (const {
        a: p,
        b: q
      } of state.links) {
        if (!visible(p) || !visible(q)) continue;
        const dx = q.x - p.x,
          dy = q.y - p.y;
        const d = Math.hypot(dx, dy) || 0.01;
        const f = (d - rest) * SPRING_K * a;
        p.vx += dx / d * f;
        p.vy += dy / d * f;
        q.vx -= dx / d * f;
        q.vy -= dy / d * f;
      }
      const c = view();
      for (const p of active) {
        p.vx += (c.cx - p.x) * GRAVITY * a;
        p.vy += (c.cy - p.y) * GRAVITY * a;
        if (p.fixed) {
          p.vx = 0;
          p.vy = 0;
          continue;
        }
        p.vx *= DAMPING;
        p.vy *= DAMPING;
        p.x += p.vx;
        p.y += p.vy;
      }
      for (let pass = 0; pass < 2; pass++) {
        for (let i = 0; i < active.length; i++) {
          for (let j = i + 1; j < active.length; j++) {
            const p = active[i],
              q = active[j];
            const min = p.r + q.r + 2.5;
            const dx = q.x - p.x,
              dy = q.y - p.y,
              d2 = dx * dx + dy * dy;
            if (d2 >= min * min || d2 === 0) continue;
            const d = Math.sqrt(d2),
              shift = (min - d) / d * 0.5;
            if (!q.fixed) {
              q.x += dx * shift;
              q.y += dy * shift;
            }
            if (!p.fixed) {
              p.x -= dx * shift;
              p.y -= dy * shift;
            }
          }
        }
      }
    }
    function fit(margin) {
      const shown = state.pts.filter(visible);
      if (!shown.length) return;
      const q = (vals, p) => vals[clamp(Math.round((vals.length - 1) * p), 0, vals.length - 1)];
      const xs = shown.map(p => p.x).sort((a, b) => a - b);
      const ys = shown.map(p => p.y).sort((a, b) => a - b);
      const pad = Math.max(...shown.map(p => p.r));
      const minX = q(xs, 0.01) - pad,
        maxX = q(xs, 0.99) + pad;
      const minY = q(ys, 0.01) - pad,
        maxY = q(ys, 0.99) + pad;
      const c = view();
      const sx = (c.w - margin * 2) / Math.max(1, maxX - minX);
      const sy = (c.h - margin * 2) / Math.max(1, maxY - minY);
      state.scale = clamp(Math.min(sx, sy), 0.15, 2.4);
      state.tx = c.cx - (minX + maxX) / 2 * state.scale;
      state.ty = c.cy - (minY + maxY) / 2 * state.scale;
    }
    function draw() {
      const {
        w,
        h
      } = size();
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      if (canvas.width !== Math.round(w * dpr)) {
        canvas.width = Math.round(w * dpr);
        canvas.height = Math.round(h * dpr);
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      ctx.clearRect(0, 0, w, h);
      ctx.save();
      ctx.translate(state.tx, state.ty);
      ctx.scale(state.scale, state.scale);
      const path = props.current.pathIds || new Set();
      const pathEdges = new Set();
      const list = Array.from(path);
      for (let i = 0; i < list.length - 1; i++) pathEdges.add(edgeKey(list[i], list[i + 1]));
      const spotlight = state.hovered || props.current.focused;
      const lit = new Set();
      if (spotlight) {
        for (const {
          a,
          b
        } of state.links) {
          if (a.id === spotlight) lit.add(b.id);
          if (b.id === spotlight) lit.add(a.id);
        }
      }
      const isLit = id => !spotlight || id === spotlight || lit.has(id);
      for (const {
        a,
        b
      } of state.links) {
        if (!visible(a) || !visible(b)) continue;
        const onPath = pathEdges.has(edgeKey(a.id, b.id));
        const touched = spotlight && (a.id === spotlight || b.id === spotlight);
        let alpha = 0.14,
          rgb = accentRgb;
        if (onPath) {
          alpha = 0.95;
          rgb = accentRgb;
        } else if (touched) {
          alpha = 0.40;
          rgb = accentRgb;
        } else if (spotlight) {
          alpha = 0.14 * DIM;
        }
        ctx.strokeStyle = "rgba(" + rgb + "," + alpha + ")";
        ctx.lineWidth = (onPath ? 1.6 : 1) / state.scale;
        ctx.beginPath();
        ctx.moveTo(a.x, a.y);
        ctx.lineTo(b.x, b.y);
        ctx.stroke();
      }
      for (const pulse of state.pulses) {
        const {
          a,
          b
        } = pulse.edge;
        if (!visible(a) || !visible(b)) continue;
        const fade = Math.sin(pulse.t * Math.PI);
        ctx.fillStyle = "rgba(" + accentRgb + "," + 0.5 * fade * (spotlight ? DIM : 1) + ")";
        ctx.beginPath();
        ctx.arc(a.x + (b.x - a.x) * pulse.t, a.y + (b.y - a.y) * pulse.t, 1.9 / state.scale, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalCompositeOperation = "lighter";
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        const dim = isLit(p.id) || onPath ? 1 : DIM;
        const r = p.r * (hot ? 1.32 : 1);
        const col = colourFor(p.type);
        const halo = ctx.createRadialGradient(p.x, p.y, r * 0.5, p.x, p.y, r * (hot ? 3.0 : 2.1));
        halo.addColorStop(0, soft(col, 0.20 * dim));
        halo.addColorStop(0.42, soft(col, 0.06 * dim));
        halo.addColorStop(1, soft(col, 0));
        ctx.fillStyle = halo;
        ctx.beginPath();
        ctx.arc(p.x, p.y, r * (hot ? 3.0 : 2.1), 0, Math.PI * 2);
        ctx.fill();

        /* the four-point flare the biggest hubs carry */
        if (p.r > 11 || hot || focus) {
          const reach = r * (hot ? 4.2 : 3.2);
          ctx.lineWidth = Math.max(0.5, r * 0.10) / state.scale;
          for (const [dx, dy] of [[1, 0], [0, 1]]) {
            const flare = ctx.createLinearGradient(p.x - dx * reach, p.y - dy * reach, p.x + dx * reach, p.y + dy * reach);
            flare.addColorStop(0, soft(col, 0));
            flare.addColorStop(0.5, soft(col, 0.18 * dim));
            flare.addColorStop(1, soft(col, 0));
            ctx.strokeStyle = flare;
            ctx.beginPath();
            ctx.moveTo(p.x - dx * reach, p.y - dy * reach);
            ctx.lineTo(p.x + dx * reach, p.y + dy * reach);
            ctx.stroke();
          }
        }
      }
      ctx.globalCompositeOperation = "source-over";
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        ctx.globalAlpha = isLit(p.id) || onPath ? 1 : DIM;
        const r = p.r * (hot ? 1.32 : 1);
        const col = colourFor(p.type);
        const body = ctx.createRadialGradient(p.x - r * 0.34, p.y - r * 0.38, r * 0.08, p.x, p.y, r);
        body.addColorStop(0, mix(col, 255, 0.78));
        body.addColorStop(0.38, mix(col, 255, 0.16));
        body.addColorStop(1, mix(col, 0, 0.34));
        ctx.fillStyle = body;
        ctx.beginPath();
        ctx.arc(p.x, p.y, r, 0, Math.PI * 2);
        ctx.fill();
        if (focus || onPath) {
          ctx.strokeStyle = accent;
          ctx.lineWidth = (focus ? 2 : 1.4) / state.scale;
          ctx.beginPath();
          ctx.arc(p.x, p.y, r + 4 / state.scale, 0, Math.PI * 2);
          ctx.stroke();
        }
        ctx.globalAlpha = 1;
      }
      ctx.restore();
      labels(spotlight, isLit, path);
    }
    function labels(spotlight, isLit, path) {
      const {
        w,
        h
      } = size();
      ctx.font = "11px " + getComputedStyle(document.body).getPropertyValue("--font-mono");
      ctx.textBaseline = "middle";
      const candidates = state.pts.filter(visible).sort((a, b) => b.r - a.r);
      const obstacles = [];
      for (const p of candidates) {
        const sr = p.r * state.scale;
        if (sr < 6) continue;
        obstacles.push({
          x: p.x * state.scale + state.tx - sr,
          y: p.y * state.scale + state.ty - sr,
          w: sr * 2,
          h: sr * 2
        });
      }
      const hits = box => obstacles.some(o => box.x < o.x + o.w && box.x + box.w > o.x && box.y < o.y + o.h && box.y + box.h > o.y);
      const always = new Set(candidates.slice(0, 5).map(p => p.id));
      let drawn = 0;
      for (const p of candidates) {
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        const forced = hot || focus || onPath || !props.current.sparseLabels && always.has(p.id);
        if (!forced && props.current.sparseLabels) continue;
        if (!forced && (p.r * state.scale < LABEL_MIN_R || drawn >= LABEL_CAP)) continue;
        const sx = p.x * state.scale + state.tx,
          sy = p.y * state.scale + state.ty;
        if (sx < -120 || sx > w + 120 || sy < -30 || sy > h + 30) continue;
        const text = p.title.length > 30 ? p.title.slice(0, 29) + "…" : p.title;
        const tw = ctx.measureText(text).width;
        const sr = p.r * state.scale,
          off = sr + 8;
        const slots = [[sx + off, sy], [sx - off - tw, sy], [sx - tw / 2, sy + off + 5], [sx - tw / 2, sy - off - 5], [sx + off * 0.72, sy - off * 0.72 - 4], [sx - off * 0.72 - tw, sy - off * 0.72 - 4], [sx + off * 0.72, sy + off * 0.72 + 4], [sx - off * 0.72 - tw, sy + off * 0.72 + 4]];
        let box = null;
        for (const [x, y] of slots) {
          const trial = {
            x: x - LABEL_PAD,
            y: y - 8,
            w: tw + LABEL_PAD * 2,
            h: 16
          };
          if (!hits(trial)) {
            box = trial;
            break;
          }
        }
        if (!box) {
          if (!forced) continue;
          box = {
            x: sx + sr + 7 - LABEL_PAD,
            y: sy - 8,
            w: tw + LABEL_PAD * 2,
            h: 16
          };
        }
        obstacles.push(box);
        drawn++;
        ctx.globalAlpha = spotlight && !isLit(p.id) && !onPath ? DIM : 1;
        ctx.fillStyle = plateC;
        ctx.fillRect(box.x, box.y, box.w, box.h);
        ctx.fillStyle = hot || focus || onPath ? accent : ink3;
        ctx.fillText(text, box.x + LABEL_PAD, box.y + 8.5);
        ctx.globalAlpha = 1;
      }
    }
    for (let i = 0; i < PRESETTLE; i++) tick(1);
    fit(46);
    draw();

    /* The product drives this with requestAnimationFrame. Here it runs on the
       same 33ms timer the reactor uses, for the same reason the reactor gives:
       RAF stops dead when the frame is not on screen, and this kit is often
       previewed inside one that isn't. */
    let last = performance.now();
    const frame = () => {
      const now = performance.now();
      const dt = Math.min(64, now - last);
      last = now;
      tick(1);
      if (now - state.lastPulse > PULSE_EVERY && state.links.length) {
        const vis = state.links.filter(e => visible(e.a) && visible(e.b));
        if (vis.length) state.pulses.push({
          edge: vis[Math.floor(Math.random() * vis.length)],
          t: 0
        });
        state.lastPulse = now;
      }
      for (const p of state.pulses) p.t += dt / PULSE_MS;
      state.pulses = state.pulses.filter(p => p.t < 1);
      draw();
    };
    const loop = setInterval(frame, 33);
    const nodeAt = (px, py) => {
      const x = (px - state.tx) / state.scale,
        y = (py - state.ty) / state.scale;
      let best = null,
        bestD = Infinity;
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const d = Math.hypot(p.x - x, p.y - y);
        const reach = Math.max(p.r + 5 / state.scale, 9 / state.scale);
        if (d < reach && d < bestD) {
          best = p;
          bestD = d;
        }
      }
      return best;
    };
    let panning = false,
      dragging = null,
      lastX = 0,
      lastY = 0,
      moved = 0;
    const down = ev => {
      canvas.setPointerCapture(ev.pointerId);
      lastX = ev.offsetX;
      lastY = ev.offsetY;
      moved = 0;
      const p = nodeAt(ev.offsetX, ev.offsetY);
      if (p) {
        dragging = p;
        p.fixed = true;
      } else {
        panning = true;
        canvas.style.cursor = "grabbing";
      }
    };
    const move = ev => {
      const dx = ev.offsetX - lastX,
        dy = ev.offsetY - lastY;
      lastX = ev.offsetX;
      lastY = ev.offsetY;
      moved += Math.abs(dx) + Math.abs(dy);
      if (dragging) {
        dragging.x += dx / state.scale;
        dragging.y += dy / state.scale;
        state.alpha = Math.max(state.alpha, 0.12);
        return;
      }
      if (panning) {
        state.tx += dx;
        state.ty += dy;
        return;
      }
      const p = nodeAt(ev.offsetX, ev.offsetY);
      const id = p ? p.id : null;
      if (id !== state.hovered) {
        state.hovered = id;
        canvas.style.cursor = id ? "pointer" : "grab";
        if (props.current.onHover) props.current.onHover(p);
      }
    };
    const up = ev => {
      const wasClick = moved < 5;
      const p = nodeAt(ev.offsetX, ev.offsetY);
      if (dragging) {
        dragging.fixed = false;
        dragging = null;
      }
      if (panning) {
        panning = false;
        canvas.style.cursor = "grab";
      }
      try {
        canvas.releasePointerCapture(ev.pointerId);
      } catch (e) {/* gone */}
      if (!wasClick) return;
      if (props.current.onSelect) props.current.onSelect(p || null, ev.shiftKey);
    };
    const wheel = ev => {
      ev.preventDefault();
      const factor = Math.exp(-ev.deltaY * 0.0016);
      const next = clamp(state.scale * factor, 0.15, 6);
      const k = next / state.scale;
      state.tx = ev.offsetX - (ev.offsetX - state.tx) * k;
      state.ty = ev.offsetY - (ev.offsetY - state.ty) * k;
      state.scale = next;
    };
    canvas.addEventListener("pointerdown", down);
    canvas.addEventListener("pointermove", move);
    canvas.addEventListener("pointerup", up);
    canvas.addEventListener("wheel", wheel, {
      passive: false
    });
    const onResize = () => fit(46);
    window.addEventListener("resize", onResize);
    return () => {
      clearInterval(loop);
      canvas.removeEventListener("pointerdown", down);
      canvas.removeEventListener("pointermove", move);
      canvas.removeEventListener("pointerup", up);
      canvas.removeEventListener("wheel", wheel);
      window.removeEventListener("resize", onResize);
    };
  }, []);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-label": "Knowledge graph. Use the inspector panel to read notes.",
    style: {
      position: "fixed",
      inset: 0,
      width: "100%",
      height: "100%",
      display: "block",
      touchAction: "none",
      cursor: "grab",
      opacity
    }
  });
}
Object.assign(window, {
  GraphStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/GraphStage.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/JarvisConsole.jsx
try { (() => {
/* JARVIS console — v2. Same product as v1 (one vault, one graph, one ask
   bar); the shell is the instrument HUD instead of three plates.

   State lives here and nowhere else: the rails report, the stage asks, the
   overlays read. */
const {
  useState,
  useEffect,
  useRef,
  useMemo,
  useCallback
} = React;
const V = window.VAULT,
  T = window.TELEMETRY;
const BY_ID = new Map(V.nodes.map(n => [n.id, n]));
const NEIGH = new Map(V.nodes.map(n => [n.id, []]));
V.edges.forEach(e => {
  NEIGH.get(e.source).push(e.target);
  NEIGH.get(e.target).push(e.source);
});
function shortestPath(a, b) {
  if (!a || !b || a === b) return null;
  const prev = new Map([[a, null]]);
  const queue = [a];
  while (queue.length) {
    const cur = queue.shift();
    if (cur === b) break;
    for (const next of NEIGH.get(cur) || []) {
      if (prev.has(next)) continue;
      prev.set(next, cur);
      queue.push(next);
    }
  }
  if (!prev.has(b)) return null;
  const out = [];
  for (let cur = b; cur !== null; cur = prev.get(cur)) out.unshift(cur);
  return out;
}
const clock = () => new Date().toLocaleTimeString("pt-BR", {
  hour12: false
});
const hhmm = () => new Date().toLocaleTimeString("pt-BR", {
  hour: "2-digit",
  minute: "2-digit"
});
const greetingFor = h => h < 5 ? "BOA MADRUGADA, EDSON" : h < 12 ? "BOM DIA, EDSON" : h < 18 ? "BOA TARDE, EDSON" : "BOA NOITE, EDSON";
const drift = v => Math.max(4, Math.min(97, Math.round(v + (Math.random() - 0.5) * 7)));
function JarvisConsoleV2() {
  const [metrics, setMetrics] = useState(T.system);
  const [feed, setFeed] = useState(T.feed);
  const [log, setLog] = useState(T.log);
  const [brain, setBrain] = useState(V.brains.find(b => b.current).id);
  const [tools, setTools] = useState(V.tools);
  const [edits, setEdits] = useState(V.edits);
  const [quickState, setQuickState] = useState({
    silencio: false,
    analisar: false
  });
  const [openPanel, setOpenPanel] = useState(null);
  const [focused, setFocused] = useState(null);
  const [anchor, setAnchor] = useState(null);
  const [pathIds, setPathIds] = useState(null);
  const [query, setQuery] = useState("");
  const [phase, setPhase] = useState("idle");
  const [answer, setAnswer] = useState(null);
  const [level, setLevel] = useState(0.5);
  const timer = useRef(null);
  const push = useCallback((tag, text) => setLog(l => [...l.slice(-40), {
    at: clock(),
    tag,
    text
  }]), []);
  const note = useCallback((what, tail, tone) => setFeed(f => [{
    at: hhmm(),
    what,
    tail,
    tone
  }, ...f].slice(0, 7)), []);
  useEffect(() => {
    const id = setInterval(() => {
      setMetrics(m => m.map(x => ({
        ...x,
        value: drift(x.value)
      })));
      setLevel(l => Math.max(0.25, Math.min(1, l + (Math.random() - 0.5) * 0.2)));
    }, 2400);
    return () => clearInterval(id);
  }, []);
  useEffect(() => () => clearTimeout(timer.current), []);
  const results = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (q.length < 2) return [];
    const out = [];
    for (const n of V.nodes) {
      const body = (V.bodies[n.id] || {}).body || "";
      const inTitle = n.title.toLowerCase().includes(q);
      const at = body.toLowerCase().indexOf(q);
      if (!inTitle && at < 0) continue;
      out.push({
        id: n.id,
        title: n.title,
        type: n.type,
        rel: n.rel,
        snippet: at >= 0 ? "…" + body.slice(Math.max(0, at - 34), at + 78).trim() + "…" : undefined
      });
      if (out.length === 6) break;
    }
    return out;
  }, [query]);
  const open = useCallback(id => {
    setFocused(id);
    setAnchor(id);
    setPathIds(null);
    setOpenPanel(null);
  }, []);
  const onSelect = useCallback((node, shift) => {
    if (!node) {
      setFocused(null);
      setPathIds(null);
      return;
    }
    if (shift && anchor && anchor !== node.id) {
      const p = shortestPath(anchor, node.id);
      setFocused(node.id);
      setPathIds(p ? new Set(p) : null);
      push(p ? "ok" : "warn", p ? "caminho traçado — " + (p.length - 1) + " passos" : "nenhum caminho entre as duas notas");
      return;
    }
    open(node.id);
  }, [anchor, open, push]);
  const ask = useCallback(() => {
    const q = query.trim();
    if (!q) return;
    setPhase("thinking");
    setAnswer(null);
    push("info", "consulta: " + q);
    timer.current = setTimeout(() => {
      const a = V.answers.ask;
      const titles = {},
        types = {};
      a.citations.forEach(id => {
        const n = BY_ID.get(id);
        titles[id] = n ? n.title : id;
        types[id] = n ? n.type : "other";
      });
      setAnswer({
        ...a,
        titles,
        types
      });
      setPhase("idle");
      push("ok", "resposta pronta · " + a.citations.length + " citações · 4,1s");
      note("Resposta gerada", a.citations.length + " citações", "accent");
    }, 1500);
  }, [query, push, note]);
  const onQuick = useCallback(q => {
    if (q.disabled) return;
    if (q.blocked) {
      push("err", q.note + " — nada foi ligado");
      note("Ação recusada", q.label, "warn");
      return;
    }
    if (q.id === "reindexar") {
      push("sys", "reindexando " + V.nodes.length + " notas…");
      note("Índice atualizado", "em curso");
      timer.current = setTimeout(() => {
        push("ok", "índice pronto — " + V.nodes.length + " notas, " + V.edges.length + " vínculos");
      }, 1200);
      return;
    }
    setQuickState(s => {
      const next = {
        ...s,
        [q.id]: !s[q.id]
      };
      push("info", q.label.toLowerCase() + ": " + (next[q.id] ? "ligado" : "desligado"));
      return next;
    });
  }, [push, note]);
  const onTool = useCallback(name => setTools(ts => ts.map(t => {
    if (t.name !== name) return t;
    push("info", t.label + ": " + (t.on ? "desligada" : "ligada"));
    return {
      ...t,
      on: !t.on
    };
  })), [push]);
  const onUndo = useCallback(id => setEdits(es => es.map(e => {
    if (e.id !== id || e.undone) return e;
    push("ok", "desfeito — " + e.path);
    note("Alteração desfeita", e.path.split("/").pop());
    return {
      ...e,
      undone: true
    };
  })), [push, note]);
  const focusedNote = focused ? {
    ...BY_ID.get(focused),
    ...(V.bodies[focused] || {})
  } : null;
  const links = focused ? (NEIGH.get(focused) || []).map(id => ({
    ...BY_ID.get(id),
    dir: "→"
  })) : [];
  const pathNodes = pathIds ? Array.from(pathIds).map(id => BY_ID.get(id)) : null;
  const hubs = V.hubs.map(id => BY_ID.get(id));
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(GraphStage, {
    nodes: V.nodes,
    edges: V.edges,
    hidden: new Set(),
    focused: focused,
    pathIds: pathIds,
    onSelect: onSelect,
    opacity: 0.68,
    sparseLabels: true
  }), /*#__PURE__*/React.createElement(RailLeft, {
    metrics: metrics,
    brains: V.brains,
    brain: brain,
    onBrain: id => {
      setBrain(id);
      push("info", "cérebro: " + id);
    },
    memory: T.memory,
    network: T.network,
    feed: feed
  }), /*#__PURE__*/React.createElement(CenterStage, {
    greeting: greetingFor(new Date().getHours()),
    phase: phase,
    level: level,
    query: query,
    onQuery: v => {
      setQuery(v);
      if (answer) setAnswer(null);
    },
    onSubmit: ask,
    results: results,
    onOpen: open,
    answer: answer,
    onCite: open,
    onClear: () => {
      setAnswer(null);
      setQuery("");
      push("sys", "nova conversa");
    },
    vaultStats: {
      notes: V.nodes.length,
      links: V.edges.length
    }
  }), /*#__PURE__*/React.createElement(RailRight, {
    weather: T.weather,
    jobs: T.jobs,
    quick: T.quick,
    quickState: quickState,
    onQuick: onQuick,
    shortcuts: T.shortcuts,
    onShortcut: id => setOpenPanel(p => {
      const next = p === id ? null : id;
      if (next) {
        setFocused(null);
        setPathIds(null);
      }
      return next;
    }),
    openPanel: openPanel,
    log: log,
    busy: phase === "thinking"
  }), /*#__PURE__*/React.createElement(Inspector, {
    note: focusedNote,
    links: links,
    path: pathNodes,
    hubs: hubs,
    onOpen: open,
    onClose: () => {
      setFocused(null);
      setPathIds(null);
    }
  }), /*#__PURE__*/React.createElement(SidePanel, {
    id: openPanel,
    vault: V,
    brain: brain,
    onBrain: id => {
      setBrain(id);
      push("info", "cérebro: " + id);
    },
    tools: tools,
    onTool: onTool,
    edits: edits,
    onUndo: onUndo,
    log: log,
    onClose: () => setOpenPanel(null)
  }));
}
Object.assign(window, {
  JarvisConsoleV2
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/JarvisConsole.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/Overlays.jsx
try { (() => {
/* The two overlays. Both are plates — the one place v2 still uses v1's
   translucent panel recipe, because they sit on top of the graph rather
   than on the field. */
const {
  HudFrame,
  MetaGrid,
  Row,
  BrainRow,
  Button,
  Eyebrow,
  PanelNote,
  OpenLink,
  TerminalLog,
  Icon
} = window.JARVISDesignSystem_ad8200;
const PLATE = {
  position: "fixed",
  zIndex: 6,
  top: "var(--rail-gutter)",
  background: "var(--surface-panel)",
  backdropFilter: "var(--blur-panel)",
  WebkitBackdropFilter: "var(--blur-panel)",
  border: "1px solid var(--border-hairline)",
  borderTopColor: "var(--border-lit)",
  borderRadius: "var(--r)",
  padding: "var(--pad)",
  maxHeight: "min(72vh, calc(100vh - var(--rail-gutter) * 2))",
  overflowY: "auto",
  overscrollBehavior: "contain",
  scrollbarWidth: "thin",
  scrollbarColor: "var(--ink-3) transparent",
  fontFamily: "var(--font-mono)",
  fontSize: "var(--size-base)",
  color: "var(--text-body)"
};
function CloseX({
  onClick
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    "aria-label": "Fechar",
    style: {
      position: "absolute",
      top: "10px",
      right: "10px",
      width: "20px",
      height: "20px",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      color: "var(--control-fg)",
      cursor: "pointer",
      transition: "var(--transition-control)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 11
  }));
}
function Inspector({
  note,
  links,
  path,
  hubs,
  onOpen,
  onClose
}) {
  if (!note) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...PLATE,
      left: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      width: "334px"
    }
  }, /*#__PURE__*/React.createElement(CloseX, {
    onClick: onClose
  }), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Inspector",
    right: note.type
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "9px",
      alignItems: "baseline",
      paddingRight: "22px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--swatch)",
      height: "var(--swatch)",
      borderRadius: "50%",
      flex: "none",
      background: "var(--t-" + note.type + ", var(--t-other))",
      transform: "translateY(2px)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-title)",
      color: "var(--text-strong)",
      lineHeight: 1.25
    }
  }, note.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "6px",
      fontSize: "var(--size-tick)",
      color: "var(--ink-3)"
    }
  }, note.rel), note.meta && Object.keys(note.meta).length ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "11px"
    }
  }, /*#__PURE__*/React.createElement(MetaGrid, {
    rows: Object.entries(note.meta)
  })) : null, note.warning ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "10px",
      color: "var(--warn)"
    }
  }, /*#__PURE__*/React.createElement(PanelNote, null, note.warning)) : null, note.body ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "12px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-prose)",
      lineHeight: 1.55,
      color: "var(--text-strong)",
      maxHeight: "var(--scroll-note)",
      overflowY: "auto",
      textWrap: "pretty"
    }
  }, note.body) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "12px"
    }
  }, /*#__PURE__*/React.createElement(OpenLink, {
    href: "file:///vault/" + note.rel,
    title: "abre fora do JARVIS"
  }, "Abrir o arquivo"))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "V\xEDnculos",
    right: String(links.length)
  }, links.length ? links.map(l => /*#__PURE__*/React.createElement(Row, {
    key: l.id,
    type: l.type,
    label: l.title,
    dir: l.dir,
    onClick: () => onOpen(l.id)
  })) : /*#__PURE__*/React.createElement(PanelNote, null, "Nenhum v\xEDnculo."))), path && path.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Caminho mais curto",
    right: path.length - 1 + " passos"
  }, path.map((p, i) => /*#__PURE__*/React.createElement(Row, {
    key: p.id,
    type: p.type,
    label: p.title,
    step: i + 1,
    onClick: () => onOpen(p.id)
  })))) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Maiores hubs",
    foot: true
  }, hubs.map(h => /*#__PURE__*/React.createElement(Row, {
    key: h.id,
    type: h.type,
    label: h.title,
    tail: h.degree,
    onClick: () => onOpen(h.id)
  })))));
}
function SidePanel({
  id,
  vault,
  brain,
  onBrain,
  tools,
  onTool,
  edits,
  onUndo,
  log,
  onClose
}) {
  if (!id) return null;
  const wrap = (label, right, children) => /*#__PURE__*/React.createElement("div", {
    style: {
      ...PLATE,
      right: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      width: "300px"
    }
  }, /*#__PURE__*/React.createElement(CloseX, {
    onClick: onClose
  }), /*#__PURE__*/React.createElement(HudFrame, {
    label: label,
    right: right,
    foot: true
  }, children));
  if (id === "cerebro") {
    return wrap("Cérebro", "modelo", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, vault.brains.map(b => /*#__PURE__*/React.createElement(BrainRow, {
      key: b.id,
      name: b.label,
      note: b.note,
      pressed: b.id === brain,
      onClick: () => onBrain(b.id)
    }))));
  }
  if (id === "ferramentas") {
    return wrap("Ferramentas", "fora do vault", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, tools.map(t => /*#__PURE__*/React.createElement(BrainRow, {
      key: t.name,
      name: t.label,
      note: t.authenticated ? t.on ? "ligada" : "desligada" : "falta " + t.needs,
      pressed: t.on,
      disabled: !t.authenticated,
      onClick: () => t.authenticated && onTool(t.name)
    }))));
  }
  if (id === "alteracoes") {
    return wrap("Alterações", edits.length + " no disco", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "9px"
      }
    }, /*#__PURE__*/React.createElement(PanelNote, null, "C\xF3pias em .jarvis/undo. Nada \xE9 sobrescrito sem uma."), edits.map(e => /*#__PURE__*/React.createElement("div", {
      key: e.id,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "4px",
        paddingBottom: "8px",
        borderBottom: "1px solid var(--hairline)"
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        justifyContent: "space-between",
        gap: "8px",
        fontSize: "var(--size-tick)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--text-body)"
      }
    }, e.action), /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--ink-3)"
      }
    }, e.when)), /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: "var(--size-tick)",
        color: "var(--text-strong)",
        wordBreak: "break-all"
      }
    }, e.path), /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        gap: "8px"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: "var(--size-tick)",
        color: "var(--ink-3)"
      }
    }, e.before, " \u2192 ", e.after, " bytes"), /*#__PURE__*/React.createElement(Button, {
      size: "ghost",
      disabled: e.undone,
      title: e.undone ? "já desfeito" : undefined,
      onClick: () => onUndo(e.id)
    }, e.undone ? "desfeito" : "desfazer"))))));
  }
  if (id === "habilidades") {
    return wrap("Habilidades", "como você trabalha", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, vault.skills.map(s => /*#__PURE__*/React.createElement(BrainRow, {
      key: s.name,
      name: s.name,
      note: s.problem ? "⚠ " + s.problem : s.description,
      pressed: s.always,
      disabled: !!s.problem
    }))));
  }
  return wrap("Terminal", "sessão", /*#__PURE__*/React.createElement(TerminalLog, {
    lines: log,
    height: 280
  }));
}
Object.assign(window, {
  Inspector,
  SidePanel
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/Overlays.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailLeft.jsx
try { (() => {
/* Left rail — the machine's own vital signs, top to bottom: load, the core
   that is answering, what it is holding in memory, the vault's shape, and
   the last things that happened. */
const {
  HudFrame,
  Meter,
  RadialGauge,
  StatList,
  BrainRow
} = window.JARVISDesignSystem_ad8200;
const kv = (k, v, tone) => /*#__PURE__*/React.createElement("div", {
  key: k,
  style: {
    display: "flex",
    justifyContent: "space-between",
    gap: "8px",
    padding: "2px 0",
    fontSize: "var(--size-tick)"
  }
}, /*#__PURE__*/React.createElement("span", {
  style: {
    color: "var(--text-label)",
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  }
}, k), /*#__PURE__*/React.createElement("span", {
  style: {
    color: tone || "var(--text-strong)",
    fontVariantNumeric: "tabular-nums"
  }
}, v));
function RailLeft({
  metrics,
  brains,
  brain,
  onBrain,
  memory,
  network,
  feed,
  onOpenFeed
}) {
  const [openBrains, setOpenBrains] = React.useState(false);
  const current = brains.find(b => b.id === brain) || brains[0];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 4,
      left: "var(--rail-gutter)",
      top: "var(--rail-gutter)",
      bottom: "var(--rail-gutter)",
      width: "var(--rail-w)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--rail-gap)",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Status do sistema",
    right: "local"
  }, metrics.map(m => /*#__PURE__*/React.createElement(Meter, {
    key: m.key,
    label: m.label,
    value: m.value,
    tone: m.value > 88 ? "warn" : "accent"
  }))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "N\xFAcleo de IA",
    right: openBrains ? "trocar" : null
  }, /*#__PURE__*/React.createElement("div", null, kv("modelo", current.label, "var(--accent)"), kv("modo", "ask"), kv("raciocínio", "87%"), kv("resposta", "4,1s")), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setOpenBrains(v => !v),
    style: {
      marginTop: "7px",
      width: "100%",
      padding: "4px 6px",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      color: "var(--control-fg)",
      fontFamily: "var(--font-mono)",
      fontSize: "8px",
      letterSpacing: "0.14em",
      textTransform: "uppercase",
      cursor: "pointer",
      transition: "var(--transition-control)"
    }
  }, openBrains ? "fechar cérebro" : "trocar cérebro"), openBrains ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "7px",
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, brains.map(b => /*#__PURE__*/React.createElement(BrainRow, {
    key: b.id,
    name: b.label,
    note: b.note,
    pressed: b.id === brain,
    onClick: () => {
      onBrain(b.id);
      setOpenBrains(false);
    }
  }))) : null), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Banco de mem\xF3ria"
  }, /*#__PURE__*/React.createElement(RadialGauge, {
    value: memory.pct,
    label: "mem\xF3ria",
    caption: /*#__PURE__*/React.createElement(StatList, {
      rows: memory.rows
    })
  })), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Rede"
  }, /*#__PURE__*/React.createElement(StatList, {
    rows: network
  })), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Feed ao vivo",
    foot: true
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "1px"
    }
  }, feed.map((f, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    onClick: () => onOpenFeed && onOpenFeed(f),
    style: {
      display: "flex",
      gap: "8px",
      alignItems: "baseline",
      width: "100%",
      textAlign: "left",
      padding: "var(--row-pad-y) var(--row-pad-x)",
      margin: "0 calc(var(--row-pad-x) * -1)",
      background: "transparent",
      border: 0,
      borderRadius: "var(--r)",
      cursor: "pointer",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      transition: "var(--transition-row)"
    },
    onMouseEnter: e => {
      e.currentTarget.style.background = "var(--wash)";
    },
    onMouseLeave: e => {
      e.currentTarget.style.background = "transparent";
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      flex: "none"
    }
  }, f.at), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)",
      flex: 1,
      minWidth: 0,
      whiteSpace: "nowrap",
      overflow: "hidden",
      textOverflow: "ellipsis"
    }
  }, f.what), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      color: f.tone === "warn" ? "var(--warn)" : f.tone === "accent" ? "var(--accent)" : "var(--ink-3)",
      maxWidth: "96px",
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, f.tail))))));
}
Object.assign(window, {
  RailLeft
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailLeft.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailRight.jsx
try { (() => {
/* Right rail — the world outside the vault (weather, scheduled work), the
   things you can press, and the log. */
const {
  HudFrame,
  Tile,
  TerminalLog,
  Icon
} = window.JARVISDesignSystem_ad8200;
function RailRight({
  weather,
  jobs,
  quick,
  quickState,
  onQuick,
  shortcuts,
  onShortcut,
  openPanel,
  log,
  busy
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 4,
      right: "var(--rail-gutter)",
      top: "var(--rail-gutter)",
      bottom: "var(--rail-gutter)",
      width: "var(--rail-w)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--rail-gap)",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Clima",
    right: weather.city
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "12px",
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: "none",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: "4px",
      paddingTop: "2px"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "cloud-rain",
    size: 26,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "20px",
      color: "var(--text-strong)",
      textShadow: "var(--hud-glow-soft)",
      lineHeight: 1
    }
  }, weather.temp, "\xB0")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, weather.rows.map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "8px",
      fontSize: "var(--size-tick)",
      padding: "1px 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-label)"
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)"
    }
  }, v))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "8px",
      fontSize: "var(--size-tick)",
      color: "var(--text-label)",
      textTransform: "uppercase",
      letterSpacing: "0.1em"
    }
  }, weather.sky), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "7px",
      display: "grid",
      gridTemplateColumns: "repeat(5,minmax(0,1fr))",
      gap: "4px"
    }
  }, weather.week.map(([d, t]) => /*#__PURE__*/React.createElement("div", {
    key: d,
    style: {
      textAlign: "center",
      fontSize: "8px",
      color: "var(--ink-3)",
      letterSpacing: "0.06em"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textTransform: "uppercase"
    }
  }, d), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--text-body)",
      marginTop: "3px"
    }
  }, t))))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Tarefas agendadas"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4,minmax(0,1fr))",
      gap: "4px"
    }
  }, jobs.map(([n, label], i) => /*#__PURE__*/React.createElement("div", {
    key: label,
    style: {
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "17px",
      color: i === 3 ? "var(--crit)" : "var(--accent)",
      textShadow: i === 3 ? "none" : "var(--hud-glow-soft)",
      lineHeight: 1.1
    }
  }, n), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "7.5px",
      letterSpacing: "0.1em",
      textTransform: "uppercase",
      color: "var(--text-label)",
      marginTop: "3px"
    }
  }, label))))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "A\xE7\xF5es r\xE1pidas"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(5,minmax(0,1fr))",
      gap: "var(--tile-gap)"
    }
  }, quick.map(q => /*#__PURE__*/React.createElement(Tile, {
    key: q.id,
    icon: q.icon,
    label: q.label,
    active: !!quickState[q.id],
    disabled: q.disabled,
    title: q.title || q.note,
    onClick: () => onQuick(q)
  })))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Atalhos"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3,minmax(0,1fr))",
      gap: "var(--tile-gap)"
    }
  }, shortcuts.map(s => /*#__PURE__*/React.createElement(Tile, {
    key: s.id,
    icon: s.icon,
    label: s.label,
    active: openPanel === s.id,
    onClick: () => onShortcut(s.id)
  })))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Terminal",
    right: busy ? "ocupado" : null,
    foot: true
  }, /*#__PURE__*/React.createElement(TerminalLog, {
    lines: log,
    busy: busy,
    height: 132
  })));
}
Object.assign(window, {
  RailRight
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/RailRight.jsx", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/telemetry.js
try { (() => {
/* Telemetry — the machine-side readouts v2 added to the console. Fixed
   values so every load draws the same picture; the drift in ConsoleV2 walks
   them a few points either way, which is all the movement the source's
   motion rules allow. */
(function () {
  window.TELEMETRY = {
    system: [{
      key: "cpu",
      label: "CPU",
      value: 17
    }, {
      key: "ram",
      label: "RAM",
      value: 39
    }, {
      key: "gpu",
      label: "GPU",
      value: 35
    }, {
      key: "dsk",
      label: "Disco",
      value: 52
    }],
    core: [["modelo", "sonnet-4.6"], ["modo", "ask"], ["raciocínio", "87%"], ["resposta", "4,1s"]],
    memory: {
      pct: 62,
      rows: [["contexto ativo", "32,4 GB"], ["longo prazo", "128,7 GB"], ["curto prazo", "18,6 GB"]]
    },
    network: [["nós do vault", String(window.VAULT.nodes.length)], ["vínculos", String(window.VAULT.edges.length)], ["entrada", "2,4 MB/s"], ["saída", "1,8 MB/s"]],
    feed: [{
      at: "22:41",
      what: "Índice atualizado",
      tail: "concluído"
    }, {
      at: "22:40",
      what: "Arquivo lido",
      tail: "fatura-4471.pdf"
    }, {
      at: "22:38",
      what: "Ferramenta negada",
      tail: "Google Drive",
      tone: "warn"
    }, {
      at: "22:35",
      what: "Nota escrita",
      tail: "client-health-board",
      tone: "accent"
    }, {
      at: "22:33",
      what: "Sessão aberta",
      tail: "edson@evo.local"
    }],
    weather: {
      city: "Rio do Sul, SC",
      temp: "18",
      sky: "chuva fraca",
      rows: [["sensação", "16°C"], ["umidade", "88%"], ["vento", "11 km/h"], ["ar", "bom"]],
      week: [["ter", "19/12"], ["qua", "21/13"], ["qui", "23/14"], ["sex", "20/15"], ["sáb", "18/12"]]
    },
    jobs: [["3", "rodando"], ["2", "agendadas"], ["5", "concluídas"], ["1", "falha"]],
    quick: [{
      id: "voz",
      icon: "mic",
      label: "Voz",
      note: "falta ELEVENLABS_API_KEY",
      blocked: true
    }, {
      id: "silencio",
      icon: "volume-x",
      label: "Silêncio"
    }, {
      id: "reindexar",
      icon: "scan-line",
      label: "Reindexar",
      action: true
    }, {
      id: "analisar",
      icon: "file-search",
      label: "Analisar"
    }, {
      id: "treinar",
      icon: "brain-circuit",
      label: "Treinar",
      disabled: true,
      title: "a etapa 5 liga isto"
    }],
    shortcuts: [{
      id: "terminal",
      icon: "square-terminal",
      label: "Terminal"
    }, {
      id: "cerebro",
      icon: "database",
      label: "Cérebro"
    }, {
      id: "ferramentas",
      icon: "network",
      label: "Ferramentas"
    }, {
      id: "alteracoes",
      icon: "workflow",
      label: "Alterações"
    }, {
      id: "habilidades",
      icon: "settings",
      label: "Habilidades"
    }],
    log: [{
      at: "22:36:58",
      tag: "sys",
      text: "índice aberto — " + window.VAULT.nodes.length + " notas, " + window.VAULT.edges.length + " vínculos"
    }, {
      at: "22:37:02",
      tag: "ok",
      text: "backend conectado — tudo operacional"
    }, {
      at: "22:37:44",
      tag: "info",
      text: "cérebro: sonnet-4.6"
    }, {
      at: "22:38:10",
      tag: "err",
      text: "falta ELEVENLABS_API_KEY — voz desligada"
    }]
  };
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/telemetry.js", error: String((e && e.message) || e) }); }

// design_handoff_jarvis_console_v2/ui_kits/jarvis-console/vault.js
try { (() => {
/* Demo vault — the profile block from Jarvis/data/generate.py, shrunk to 44
   notes so the graph reads at card size. Same shapes, same seed idea: fixed
   data, identical picture every load. */
(function () {
  const clients = [["Harrow & Vane", "furniture retail", "Leeds"], ["Northsound Audio", "hardware", "Glasgow"], ["Pellworth Legal", "professional services", "London"], ["Bramble Bakery", "food & drink", "Bristol"], ["Kestrel Fitness", "health", "Manchester"], ["Orlin Freight", "logistics", "Rotterdam"]];
  const projects = [["Storefront rebuild", 0], ["Checkout rework", 0], ["Brand refresh", 1], ["Catalogue migration", 2], ["Booking flow", 4], ["Client portal", 5], ["Subscription launch", 3], ["Performance pass", 1]];
  const meetings = [["Kickoff — Storefront rebuild", 0], ["Weekly check-in — Checkout rework", 1], ["Scope review — Brand refresh", 2], ["Handover — Booking flow", 4], ["Budget conversation — Client portal", 5]];
  const invoices = [["Fatura 4471", 0, "£6,200"], ["Fatura 4472", 1, "£2,400"], ["Fatura 4468", 2, "£1,800"], ["Fatura 4475", 4, "£4,150"], ["Fatura 4477", 5, "£980"]];
  const people = [["Aisha Rahman", 0], ["Bruno Cortez", 1], ["Elin Berg", 2], ["Femi Achebe", 3], ["Priya Iqbal", 4], ["Tomas Falk", 5]];
  const notes = [["Operating principles", "The spine note. Most other notes point back here.", ["positioning"]], ["Client health board", "One line per client, updated when something changes.", ["clients"]], ["Money map", "Where revenue comes from and when it lands.", ["cashflow"]], ["Deposit policy", "Half up front, the rest on handover. The deposit is what buys the slot in the calendar — a project without one is a conversation, and the calendar stays open.", ["cashflow"]], ["Pricing ladder", "Five offerings, five ceilings. Say the number first; the conversation after it is the real brief.", ["pricing"]], ["Late payment, what works", "Chase on the day, not the week. Name the invoice and the amount in the first line.", ["cashflow"]], ["Scope creep early-warning signs", "Three: a new name on the call, a deadline that moves toward you, and the word 'quick'.", ["process"]], ["Where the margin actually goes", "Hosting, the two hours before every call, and the fortnight after handover nobody quotes for.", ["costs"]], ["Capacity, realistically", "Two projects, one care plan, nothing else. The third project is always paid for by the first two.", ["planning"]], ["The two-week rule", "Nothing goes on the board that cannot start within a fortnight.", ["process"]]];
  const refs = [["Contrato padrão 2026", "reference"], ["Tabela de preços — julho", "reference"]];
  const nodes = [];
  const edges = [];
  const bodies = {};
  const add = (id, title, type, rel, body, meta) => {
    nodes.push({
      id,
      title,
      type,
      rel
    });
    bodies[id] = {
      body: body || "",
      meta: meta || {}
    };
  };
  const link = (a, b) => edges.push({
    source: a,
    target: b
  });
  const slug = s => s.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
  clients.forEach(([name, trade, city], i) => {
    const id = "demo/clients/" + slug(name) + ".md";
    add(id, name, "client", "clients/" + slug(name) + ".md", name + " — " + trade + ", " + city + ". Two projects live, one care plan. Paid on time twice, late once.", {
      trade: trade,
      city: city,
      since: "2024"
    });
  });
  projects.forEach(([name, ci], i) => {
    const id = "demo/projects/" + slug(name) + ".md";
    add(id, name, "project", "projects/" + slug(name) + ".md", name + " for " + clients[ci][0] + ". Fixed price, two-week discovery, handover in six.", {
      client: clients[ci][0],
      status: i % 3 === 0 ? "live" : "closed"
    });
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  meetings.forEach(([name, pi]) => {
    const id = "demo/meetings/" + slug(name) + ".md";
    add(id, name, "meeting", "meetings/" + slug(name) + ".md", "Present: the client and me. Decisions taken are in the project note; this is what was said.", {
      when: "2026-07-2" + (pi + 1)
    });
    link(id, "demo/projects/" + slug(projects[pi][0]) + ".md");
  });
  invoices.forEach(([name, ci, amount]) => {
    const id = "demo/invoices/" + slug(name) + ".pdf";
    add(id, name, "invoice", "invoices/" + slug(name) + ".pdf", "", {
      amount: amount,
      due: "2026-08-1" + (ci + 1),
      client: clients[ci][0]
    });
    bodies[id].warning = "no text extracted — scanned PDF";
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  people.forEach(([name, ci]) => {
    const id = "demo/people/" + slug(name) + ".md";
    add(id, name, "person", "people/" + slug(name) + ".md", name + " is the contact at " + clients[ci][0] + ". Answers email, not calls.", {
      at: clients[ci][0]
    });
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  notes.forEach(([title, body, tags], i) => {
    const id = "demo/notes/" + slug(title) + ".md";
    add(id, title, "note", "notes/" + slug(title) + ".md", body, {
      tags: tags.join(", ")
    });
    if (i > 0) link(id, "demo/notes/operating-principles.md");
  });
  refs.forEach(([title]) => {
    const id = "demo/reference/" + slug(title) + ".pdf";
    add(id, title, "reference", "reference/" + slug(title) + ".pdf", "Assinado em janeiro. Cláusula 7 é a que importa: 30 dias, sem exceção.", {
      pages: "4"
    });
    link(id, "demo/notes/deposit-policy.md");
  });
  link("demo/notes/deposit-policy.md", "demo/notes/money-map.md");
  link("demo/notes/late-payment-what-works.md", "demo/notes/money-map.md");
  link("demo/notes/client-health-board.md", "demo/clients/harrow-vane.md");
  link("demo/notes/client-health-board.md", "demo/clients/orlin-freight.md");
  link("demo/notes/client-health-board.md", "demo/clients/bramble-bakery.md");
  link("demo/notes/money-map.md", "demo/invoices/fatura-4471.pdf");
  link("demo/notes/money-map.md", "demo/invoices/fatura-4475.pdf");
  link("demo/notes/pricing-ladder.md", "demo/projects/storefront-rebuild.md");
  link("demo/notes/capacity-realistically.md", "demo/projects/booking-flow.md");
  link("demo/notes/scope-creep-early-warning-signs.md", "demo/projects/checkout-rework.md");
  link("demo/notes/deposit-policy.md", "demo/clients/harrow-vane.md");
  link("demo/notes/where-the-margin-actually-goes.md", "demo/notes/money-map.md");
  const degree = {};
  edges.forEach(e => {
    degree[e.source] = (degree[e.source] || 0) + 1;
    degree[e.target] = (degree[e.target] || 0) + 1;
  });
  nodes.forEach(n => {
    n.degree = degree[n.id] || 0;
  });
  const counts = {};
  nodes.forEach(n => {
    counts[n.type] = (counts[n.type] || 0) + 1;
  });
  const hubs = nodes.slice().sort((a, b) => b.degree - a.degree).slice(0, 5).map(n => n.id);
  window.VAULT = {
    nodes: nodes,
    edges: edges,
    bodies: bodies,
    counts: counts,
    hubs: hubs,
    stats: [["mode", "demo"], ["notes", String(nodes.length)], ["links", String(edges.length)], ["indexed in", "0.4s"], ["model", "sonnet-4.6"], ["listen", "missing"], ["speak", "this machine"]],
    brains: [{
      id: "claude-sonnet-4-6",
      label: "Sonnet 4.6",
      note: "rápido, o padrão",
      current: true
    }, {
      id: "claude-opus-4-1",
      label: "Opus 4.1",
      note: "mais caro, para relatórios",
      current: false
    }, {
      id: "claude-haiku-4-5",
      label: "Haiku 4.5",
      note: "para buscas curtas",
      current: false
    }],
    tools: [{
      name: "drive",
      label: "Google Drive",
      authenticated: false,
      needs: "GOOGLE_OAUTH_TOKEN",
      on: false
    }, {
      name: "gmail",
      label: "Gmail",
      authenticated: false,
      needs: "GOOGLE_OAUTH_TOKEN",
      on: false
    }, {
      name: "fetch",
      label: "Fetch (web)",
      authenticated: true,
      on: false
    }],
    skills: [{
      name: "Cobrança",
      description: "cobrar fornecedor, boleto vencido, atraso, inadimplência…",
      always: false
    }, {
      name: "Fechamento",
      description: "",
      problem: "sem description — nunca casa"
    }],
    edits: [{
      id: "e2",
      when: "04/08/2026, 11:02",
      path: "demo/notes/client-health-board.md",
      action: "escreveu",
      before: 1840,
      after: 1952,
      undone: false
    }, {
      id: "e1",
      when: "03/08/2026, 16:20",
      path: "demo/notes/money-map.md",
      action: "escreveu",
      before: 2210,
      after: 2210,
      undone: true
    }],
    facts: [{
      name: "jatinox-prazo",
      when: "2026-07-30",
      text: "Boleto da Jatinox sempre 28 dias, nunca 30."
    }, {
      name: "sem-backup",
      when: "2026-07-22",
      text: "Não existe backup do Evo-SI. Qualquer escrita precisa de cópia antes."
    }],
    turns: [{
      when: "04/08/2026, 14:22",
      question: "quanto está vencido com a Jatinox?",
      meta: "6 citações · 318 tokens · sonnet-4.6"
    }, {
      when: "03/08/2026, 09:07",
      question: "o que muda no prazo se eu antecipar?",
      meta: "2 citações · 1.204 tokens · sonnet-4.6"
    }, {
      when: "02/08/2026, 17:55",
      question: "quais projetos estão sem depósito?",
      meta: "0 citações · 96 tokens · sonnet-4.6"
    }],
    answers: {
      ask: {
        kind: "ask",
        meta: "sonnet-4.6 · read 14 notes · 2 remembered · cobranca · JARVIS.md · 4.1s",
        body: "Metade na assinatura, metade na entrega [demo/notes/deposit-policy.md]. O depósito é o que compra a vaga na agenda — sem ele o projeto é uma conversa e a agenda continua aberta.\n\nA Harrow & Vane é a única cliente com projeto aberto e sem depósito registrado [demo/clients/harrow-vane.md]. A fatura 4471, de £6,200, vence em 11/08 [demo/invoices/fatura-4471.pdf].\n\nO que falta para responder direito sobre hoje: um export de contas a receber com data de hoje. O dado acima é de 28/07.",
        citations: ["demo/notes/deposit-policy.md", "demo/clients/harrow-vane.md", "demo/invoices/fatura-4471.pdf"]
      },
      brief: {
        kind: "brief",
        meta: "sonnet-4.6 · read 22 notes · JARVIS.md · 6.3s",
        body: "· Fatura 4471 (£6,200, Harrow & Vane) vence em 11/08 e não tem depósito lançado.\n· Checkout rework ganhou um nome novo na chamada de segunda — sinal de escopo [demo/notes/scope-creep-early-warning-signs.md].\n· Booking flow entregou; a janela de suporte fecha em 14 dias.\n· Capacidade: dois projetos e um care plan já estão ocupados. O terceiro entra sem margem [demo/notes/capacity-realistically.md].\n· Nada mais mudou desde 28/07.",
        citations: ["demo/notes/scope-creep-early-warning-signs.md", "demo/notes/capacity-realistically.md"]
      }
    }
  };
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "design_handoff_jarvis_console_v2/ui_kits/jarvis-console/vault.js", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/CenterStage.jsx
try { (() => {
/* Centre stage — greeting, the reactor with the vault's four headline
   figures pinned around it, and the ask bar. Everything the operator types
   happens here; the rails only report. */
const {
  Reactor,
  Icon,
  AnswerBlock,
  Cite,
  ResultRow,
  Button
} = window.JARVISDesignSystem_ad8200;
const readout = (label, value, pos) => /*#__PURE__*/React.createElement("div", {
  key: label,
  style: {
    position: "absolute",
    maxWidth: "46%",
    ...pos
  }
}, /*#__PURE__*/React.createElement("div", {
  style: {
    fontSize: "8px",
    letterSpacing: "0.16em",
    textTransform: "uppercase",
    color: "var(--text-label)",
    whiteSpace: "nowrap"
  }
}, label), /*#__PURE__*/React.createElement("div", {
  style: {
    fontSize: "15px",
    color: "var(--accent)",
    textShadow: "var(--hud-glow-soft)",
    marginTop: "4px",
    fontVariantNumeric: "tabular-nums",
    whiteSpace: "nowrap"
  }
}, value));
function CenterStage({
  greeting,
  phase,
  level,
  query,
  onQuery,
  onSubmit,
  results,
  onOpen,
  answer,
  onCite,
  onClear,
  vaultStats
}) {
  const inputRef = React.useRef(null);
  const stageRef = React.useRef(null);
  const [focus, setFocus] = React.useState(false);
  const [dial, setDial] = React.useState(0);
  const busy = phase === "thinking";
  const showResults = !busy && !answer && query.trim().length > 1 && results.length > 0;

  /* The reactor is the one thing here that must never collide with a rail or
     the greeting, so it is measured rather than fixed — and re-measured after
     every render, because opening the answer panel steals the stage's height
     without any window resize. */
  const fit = React.useCallback(() => {
    const el = stageRef.current;
    if (!el) return;
    const next = Math.max(0, Math.min(420, el.clientWidth - 24, el.clientHeight - 8));
    setDial(d => Math.abs(d - next) < 2 ? d : next);
  }, []);
  React.useLayoutEffect(fit);
  React.useEffect(() => {
    const el = stageRef.current;
    if (!el || typeof ResizeObserver === "undefined") return;
    const ro = new ResizeObserver(fit);
    ro.observe(el);
    window.addEventListener("resize", fit);
    return () => {
      ro.disconnect();
      window.removeEventListener("resize", fit);
    };
  }, [fit]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 2,
      top: 0,
      bottom: 0,
      left: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      right: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      pointerEvents: "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "58px",
      display: "flex",
      gap: "13px",
      alignItems: "flex-start",
      pointerEvents: "auto"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "26px",
      height: "26px",
      flex: "none",
      border: "1px solid var(--accent-line)",
      borderRadius: "var(--r)",
      background: "var(--accent-soft)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "9px",
      height: "9px",
      background: "var(--accent)",
      boxShadow: "var(--hud-glow)"
    }
  })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "19px",
      letterSpacing: "0.02em",
      color: "var(--accent)",
      textShadow: "var(--hud-glow-soft)"
    }
  }, greeting), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-base)",
      color: "var(--text-body)",
      marginTop: "3px"
    }
  }, "Pergunte qualquer coisa sobre o que est\xE1 no vault."))), /*#__PURE__*/React.createElement("div", {
    ref: stageRef,
    style: {
      flex: 1,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: "100%",
      minHeight: 0,
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: dial,
      height: dial,
      flex: "none"
    }
  }, /*#__PURE__*/React.createElement(Reactor, {
    size: dial,
    state: phase === "idle" ? "idle" : "thinking",
    level: level,
    style: {
      position: "absolute",
      inset: 0
    }
  }), dial >= 260 ? /*#__PURE__*/React.createElement(React.Fragment, null, readout("Índice", vaultStats.notes + " notas", {
    left: 0,
    top: "19%"
  }), readout("Cobertura", "93%", {
    right: 0,
    top: "19%",
    textAlign: "right"
  }), readout("Vínculos", vaultStats.links, {
    left: 0,
    bottom: "19%"
  }), readout("Varredura", "0,4s", {
    right: 0,
    bottom: "19%",
    textAlign: "right"
  })) : null)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "min(var(--ask-w), 100%)",
      paddingBottom: "26px",
      pointerEvents: "auto"
    }
  }, answer ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: "12px",
      maxHeight: "34vh",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      background: "var(--surface-panel)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid var(--border-hairline)",
      borderTopColor: "var(--border-lit)",
      borderRadius: "var(--r)",
      padding: "var(--pad)"
    }
  }, /*#__PURE__*/React.createElement(AnswerBlock, {
    kind: answer.kind,
    meta: answer.meta,
    body: answer.body
  }, answer.citations.map(id => /*#__PURE__*/React.createElement(Cite, {
    key: id,
    type: answer.types[id],
    title: answer.titles[id],
    onClick: () => onCite(id)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "12px",
      display: "flex",
      gap: "6px"
    }
  }, /*#__PURE__*/React.createElement(Button, {
    size: "ghost",
    onClick: onClear
  }, "nova conversa"))) : null, showResults ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: "12px",
      maxHeight: "30vh",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent",
      background: "var(--surface-panel)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid var(--border-hairline)",
      borderTopColor: "var(--border-lit)",
      borderRadius: "var(--r)",
      padding: "10px var(--pad)"
    }
  }, results.map(r => /*#__PURE__*/React.createElement(ResultRow, {
    key: r.id,
    type: r.type,
    title: r.title,
    where: r.rel,
    snippet: r.snippet,
    onClick: () => onOpen(r.id)
  }))) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      marginBottom: "10px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "7px",
      fontSize: "var(--size-tick)",
      letterSpacing: "0.16em",
      textTransform: "uppercase",
      color: busy ? "var(--accent)" : "var(--text-label)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "5px",
      height: "5px",
      borderRadius: "50%",
      background: busy ? "var(--accent)" : "var(--good)",
      boxShadow: "var(--hud-glow-soft)"
    }
  }), busy ? "pensando…" : "sistemas operacionais")), /*#__PURE__*/React.createElement("form", {
    onSubmit: e => {
      e.preventDefault();
      onSubmit();
    },
    style: {
      display: "flex",
      alignItems: "center",
      gap: "10px",
      height: "var(--ask-h)",
      padding: "0 12px 0 14px",
      background: "var(--surface-raised)",
      backdropFilter: "var(--blur-panel)",
      WebkitBackdropFilter: "var(--blur-panel)",
      border: "1px solid " + (focus ? "var(--accent-line)" : "var(--border-hairline)"),
      borderRadius: "var(--r)",
      transition: "border-color var(--t-field) linear"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "mic",
    size: 14,
    style: {
      color: "var(--ink-3)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: "1px",
      height: "18px",
      background: "var(--hairline)",
      flex: "none"
    }
  }), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    value: query,
    onChange: e => onQuery(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    placeholder: "O que vamos construir hoje?",
    "aria-label": "Pergunta",
    disabled: busy,
    style: {
      flex: 1,
      minWidth: 0,
      background: "transparent",
      border: 0,
      outline: "none",
      color: "var(--text-strong)",
      fontFamily: "var(--font-mono)",
      fontSize: "13.5px",
      letterSpacing: "0.01em"
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "submit",
    disabled: busy || !query.trim(),
    "aria-label": "Enviar",
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      width: "28px",
      height: "28px",
      background: "transparent",
      border: "1px solid " + (query.trim() && !busy ? "var(--accent-line)" : "var(--control-border)"),
      borderRadius: "var(--r)",
      color: query.trim() && !busy ? "var(--accent)" : "var(--control-fg-disabled)",
      cursor: query.trim() && !busy ? "pointer" : "default",
      transition: "var(--transition-control)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "arrow-right",
    size: 14
  })))));
}
Object.assign(window, {
  CenterStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/CenterStage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/GraphStage.jsx
try { (() => {
/* GraphStage — the canvas graph, ported from Jarvis/ui/graph.js.
   Same forces, same colours, same label placement, same dim/glow rules;
   the spatial-grid optimisation is dropped because 44 nodes do not need it. */
const {
  useRef,
  useEffect
} = React;
const REPULSION = 2000,
  CUTOFF = 240,
  SPRING_LEN = 58,
  SPRING_K = 0.036;
const GRAVITY = 0.0105,
  DAMPING = 0.83,
  ALPHA_DECAY = 0.014,
  BREATH = 0.0065;
const BREATH_MS = 11000,
  PRESETTLE = 450,
  R_MIN = 3.4,
  R_MAX = 17;
const LABEL_MIN_R = 6.5,
  LABEL_PAD = 4,
  LABEL_CAP = 26,
  DIM = 0.1;
const PULSE_EVERY = 3400,
  PULSE_MS = 1500;
const INSETS = {
  left: 300,
  right: 300,
  top: 130,
  bottom: 150
};
const TYPES = ["client", "project", "meeting", "invoice", "person", "note", "reference"];
function hash(str) {
  let h = 2166136261;
  for (let i = 0; i < str.length; i++) {
    h ^= str.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return (h >>> 0) / 4294967296;
}
const clamp = (v, lo, hi) => v < lo ? lo : v > hi ? hi : v;
function GraphStage({
  nodes,
  edges,
  hidden,
  focused,
  pathIds,
  onSelect,
  onHover,
  opacity = 1,
  sparseLabels = false
}) {
  const ref = useRef(null);
  const sim = useRef(null);
  const props = useRef({});
  props.current = {
    hidden,
    focused,
    pathIds,
    onSelect,
    onHover,
    sparseLabels
  };
  useEffect(() => {
    const canvas = ref.current;
    const ctx = canvas.getContext("2d", {
      alpha: true
    });
    const css = getComputedStyle(document.documentElement);
    const colour = {
      other: css.getPropertyValue("--t-other").trim()
    };
    TYPES.forEach(t => {
      colour[t] = css.getPropertyValue("--t-" + t).trim();
    });
    const accent = css.getPropertyValue("--accent").trim();
    const accentRgb = (css.getPropertyValue("--accent-rgb").trim() || "60,232,143").replace(/\s/g, "");
    const plateC = css.getPropertyValue("--plate").trim() || "rgba(4,9,7,.74)";
    const ink3 = css.getPropertyValue("--ink-3").trim();
    const colourFor = t => colour[t] || colour.other;

    /* Nodes are drawn as lit spheres, not flat dots: a wide halo, a body whose
       highlight sits up and to the left, and a cross flare on the hubs. Hue is
       still taxonomy and nothing else — only the rendering changed. */
    const rgbOf = hex => {
      const h = hex.replace("#", "");
      const v = h.length === 3 ? h.split("").map(c => c + c).join("") : h;
      return [parseInt(v.slice(0, 2), 16), parseInt(v.slice(2, 4), 16), parseInt(v.slice(4, 6), 16)];
    };
    const mix = (hex, target, k) => {
      const [r, g, b] = rgbOf(hex);
      return "rgb(" + Math.round(r + (target - r) * k) + "," + Math.round(g + (target - g) * k) + "," + Math.round(b + (target - b) * k) + ")";
    };
    const soft = (hex, a) => {
      const [r, g, b] = rgbOf(hex);
      return "rgba(" + r + "," + g + "," + b + "," + a + ")";
    };
    const size = () => ({
      w: canvas.clientWidth || 1,
      h: canvas.clientHeight || 1
    });
    const view = () => {
      const {
        w,
        h
      } = size();
      const x = INSETS.left,
        y = INSETS.top;
      const vw = Math.max(160, w - INSETS.left - INSETS.right);
      const vh = Math.max(160, h - INSETS.top - INSETS.bottom);
      return {
        x,
        y,
        w: vw,
        h: vh,
        cx: x + vw / 2,
        cy: y + vh / 2
      };
    };
    const v = view();
    const n = Math.max(1, nodes.length);
    const spread = Math.min(v.w, v.h) * 0.42;
    const pts = nodes.map((raw, i) => {
      const seed = hash(raw.id);
      const angle = i * 2.399963 + seed * 0.9;
      const radius = Math.sqrt((i + 0.5) / n) * spread;
      return {
        ...raw,
        x: v.cx + Math.cos(angle) * radius,
        y: v.cy + Math.sin(angle) * radius,
        vx: 0,
        vy: 0,
        fixed: false,
        r: clamp(R_MIN + Math.sqrt(raw.degree || 0) * 2.35, R_MIN, R_MAX)
      };
    });
    const byId = new Map(pts.map(p => [p.id, p]));
    const links = edges.map(e => ({
      a: byId.get(e.source),
      b: byId.get(e.target)
    })).filter(e => e.a && e.b);
    const state = {
      pts,
      links,
      byId,
      alpha: 1,
      scale: 1,
      tx: 0,
      ty: 0,
      hovered: null,
      pulses: [],
      lastPulse: 0
    };
    sim.current = state;
    const visible = p => !(props.current.hidden || new Set()).has(p.type);
    const edgeKey = (a, b) => a < b ? a + "\u0000" + b : b + "\u0000" + a;
    function tick(stepScale) {
      const active = state.pts.filter(visible);
      if (!active.length) return;
      state.alpha += (BREATH - state.alpha) * ALPHA_DECAY;
      const a = state.alpha * (stepScale || 1);
      const sway = 1 + Math.sin(performance.now() / BREATH_MS * Math.PI * 2) * 0.03;
      for (let i = 0; i < active.length; i++) {
        for (let j = i + 1; j < active.length; j++) {
          const p = active[i],
            q = active[j];
          let dx = p.x - q.x,
            dy = p.y - q.y,
            d2 = dx * dx + dy * dy;
          if (d2 > CUTOFF * CUTOFF) continue;
          if (d2 < 0.01) {
            dx = (hash(p.id) - 0.5) * 0.5;
            dy = (hash(q.id) - 0.5) * 0.5;
            d2 = dx * dx + dy * dy + 0.01;
          }
          const d = Math.sqrt(d2);
          const force = REPULSION * a * (p.r * q.r / 36) / d2;
          p.vx += dx / d * force;
          p.vy += dy / d * force;
          q.vx -= dx / d * force;
          q.vy -= dy / d * force;
        }
      }
      const rest = SPRING_LEN * sway;
      for (const {
        a: p,
        b: q
      } of state.links) {
        if (!visible(p) || !visible(q)) continue;
        const dx = q.x - p.x,
          dy = q.y - p.y;
        const d = Math.hypot(dx, dy) || 0.01;
        const f = (d - rest) * SPRING_K * a;
        p.vx += dx / d * f;
        p.vy += dy / d * f;
        q.vx -= dx / d * f;
        q.vy -= dy / d * f;
      }
      const c = view();
      for (const p of active) {
        p.vx += (c.cx - p.x) * GRAVITY * a;
        p.vy += (c.cy - p.y) * GRAVITY * a;
        if (p.fixed) {
          p.vx = 0;
          p.vy = 0;
          continue;
        }
        p.vx *= DAMPING;
        p.vy *= DAMPING;
        p.x += p.vx;
        p.y += p.vy;
      }
      for (let pass = 0; pass < 2; pass++) {
        for (let i = 0; i < active.length; i++) {
          for (let j = i + 1; j < active.length; j++) {
            const p = active[i],
              q = active[j];
            const min = p.r + q.r + 2.5;
            const dx = q.x - p.x,
              dy = q.y - p.y,
              d2 = dx * dx + dy * dy;
            if (d2 >= min * min || d2 === 0) continue;
            const d = Math.sqrt(d2),
              shift = (min - d) / d * 0.5;
            if (!q.fixed) {
              q.x += dx * shift;
              q.y += dy * shift;
            }
            if (!p.fixed) {
              p.x -= dx * shift;
              p.y -= dy * shift;
            }
          }
        }
      }
    }
    function fit(margin) {
      const shown = state.pts.filter(visible);
      if (!shown.length) return;
      const q = (vals, p) => vals[clamp(Math.round((vals.length - 1) * p), 0, vals.length - 1)];
      const xs = shown.map(p => p.x).sort((a, b) => a - b);
      const ys = shown.map(p => p.y).sort((a, b) => a - b);
      const pad = Math.max(...shown.map(p => p.r));
      const minX = q(xs, 0.01) - pad,
        maxX = q(xs, 0.99) + pad;
      const minY = q(ys, 0.01) - pad,
        maxY = q(ys, 0.99) + pad;
      const c = view();
      const sx = (c.w - margin * 2) / Math.max(1, maxX - minX);
      const sy = (c.h - margin * 2) / Math.max(1, maxY - minY);
      state.scale = clamp(Math.min(sx, sy), 0.15, 2.4);
      state.tx = c.cx - (minX + maxX) / 2 * state.scale;
      state.ty = c.cy - (minY + maxY) / 2 * state.scale;
    }
    function draw() {
      const {
        w,
        h
      } = size();
      const dpr = Math.min(window.devicePixelRatio || 1, 2);
      if (canvas.width !== Math.round(w * dpr)) {
        canvas.width = Math.round(w * dpr);
        canvas.height = Math.round(h * dpr);
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      ctx.clearRect(0, 0, w, h);
      ctx.save();
      ctx.translate(state.tx, state.ty);
      ctx.scale(state.scale, state.scale);
      const path = props.current.pathIds || new Set();
      const pathEdges = new Set();
      const list = Array.from(path);
      for (let i = 0; i < list.length - 1; i++) pathEdges.add(edgeKey(list[i], list[i + 1]));
      const spotlight = state.hovered || props.current.focused;
      const lit = new Set();
      if (spotlight) {
        for (const {
          a,
          b
        } of state.links) {
          if (a.id === spotlight) lit.add(b.id);
          if (b.id === spotlight) lit.add(a.id);
        }
      }
      const isLit = id => !spotlight || id === spotlight || lit.has(id);
      for (const {
        a,
        b
      } of state.links) {
        if (!visible(a) || !visible(b)) continue;
        const onPath = pathEdges.has(edgeKey(a.id, b.id));
        const touched = spotlight && (a.id === spotlight || b.id === spotlight);
        let alpha = 0.14,
          rgb = accentRgb;
        if (onPath) {
          alpha = 0.95;
          rgb = accentRgb;
        } else if (touched) {
          alpha = 0.40;
          rgb = accentRgb;
        } else if (spotlight) {
          alpha = 0.14 * DIM;
        }
        ctx.strokeStyle = "rgba(" + rgb + "," + alpha + ")";
        ctx.lineWidth = (onPath ? 1.6 : 1) / state.scale;
        ctx.beginPath();
        ctx.moveTo(a.x, a.y);
        ctx.lineTo(b.x, b.y);
        ctx.stroke();
      }
      for (const pulse of state.pulses) {
        const {
          a,
          b
        } = pulse.edge;
        if (!visible(a) || !visible(b)) continue;
        const fade = Math.sin(pulse.t * Math.PI);
        ctx.fillStyle = "rgba(" + accentRgb + "," + 0.5 * fade * (spotlight ? DIM : 1) + ")";
        ctx.beginPath();
        ctx.arc(a.x + (b.x - a.x) * pulse.t, a.y + (b.y - a.y) * pulse.t, 1.9 / state.scale, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.globalCompositeOperation = "lighter";
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        const dim = isLit(p.id) || onPath ? 1 : DIM;
        const r = p.r * (hot ? 1.32 : 1);
        const col = colourFor(p.type);
        const halo = ctx.createRadialGradient(p.x, p.y, r * 0.5, p.x, p.y, r * (hot ? 3.0 : 2.1));
        halo.addColorStop(0, soft(col, 0.20 * dim));
        halo.addColorStop(0.42, soft(col, 0.06 * dim));
        halo.addColorStop(1, soft(col, 0));
        ctx.fillStyle = halo;
        ctx.beginPath();
        ctx.arc(p.x, p.y, r * (hot ? 3.0 : 2.1), 0, Math.PI * 2);
        ctx.fill();

        /* the four-point flare the biggest hubs carry */
        if (p.r > 11 || hot || focus) {
          const reach = r * (hot ? 4.2 : 3.2);
          ctx.lineWidth = Math.max(0.5, r * 0.10) / state.scale;
          for (const [dx, dy] of [[1, 0], [0, 1]]) {
            const flare = ctx.createLinearGradient(p.x - dx * reach, p.y - dy * reach, p.x + dx * reach, p.y + dy * reach);
            flare.addColorStop(0, soft(col, 0));
            flare.addColorStop(0.5, soft(col, 0.18 * dim));
            flare.addColorStop(1, soft(col, 0));
            ctx.strokeStyle = flare;
            ctx.beginPath();
            ctx.moveTo(p.x - dx * reach, p.y - dy * reach);
            ctx.lineTo(p.x + dx * reach, p.y + dy * reach);
            ctx.stroke();
          }
        }
      }
      ctx.globalCompositeOperation = "source-over";
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        ctx.globalAlpha = isLit(p.id) || onPath ? 1 : DIM;
        const r = p.r * (hot ? 1.32 : 1);
        const col = colourFor(p.type);
        const body = ctx.createRadialGradient(p.x - r * 0.34, p.y - r * 0.38, r * 0.08, p.x, p.y, r);
        body.addColorStop(0, mix(col, 255, 0.78));
        body.addColorStop(0.38, mix(col, 255, 0.16));
        body.addColorStop(1, mix(col, 0, 0.34));
        ctx.fillStyle = body;
        ctx.beginPath();
        ctx.arc(p.x, p.y, r, 0, Math.PI * 2);
        ctx.fill();
        if (focus || onPath) {
          ctx.strokeStyle = accent;
          ctx.lineWidth = (focus ? 2 : 1.4) / state.scale;
          ctx.beginPath();
          ctx.arc(p.x, p.y, r + 4 / state.scale, 0, Math.PI * 2);
          ctx.stroke();
        }
        ctx.globalAlpha = 1;
      }
      ctx.restore();
      labels(spotlight, isLit, path);
    }
    function labels(spotlight, isLit, path) {
      const {
        w,
        h
      } = size();
      ctx.font = "11px " + getComputedStyle(document.body).getPropertyValue("--font-mono");
      ctx.textBaseline = "middle";
      const candidates = state.pts.filter(visible).sort((a, b) => b.r - a.r);
      const obstacles = [];
      for (const p of candidates) {
        const sr = p.r * state.scale;
        if (sr < 6) continue;
        obstacles.push({
          x: p.x * state.scale + state.tx - sr,
          y: p.y * state.scale + state.ty - sr,
          w: sr * 2,
          h: sr * 2
        });
      }
      const hits = box => obstacles.some(o => box.x < o.x + o.w && box.x + box.w > o.x && box.y < o.y + o.h && box.y + box.h > o.y);
      const always = new Set(candidates.slice(0, 5).map(p => p.id));
      let drawn = 0;
      for (const p of candidates) {
        const hot = p.id === state.hovered;
        const focus = p.id === props.current.focused;
        const onPath = path.has(p.id);
        const forced = hot || focus || onPath || !props.current.sparseLabels && always.has(p.id);
        if (!forced && props.current.sparseLabels) continue;
        if (!forced && (p.r * state.scale < LABEL_MIN_R || drawn >= LABEL_CAP)) continue;
        const sx = p.x * state.scale + state.tx,
          sy = p.y * state.scale + state.ty;
        if (sx < -120 || sx > w + 120 || sy < -30 || sy > h + 30) continue;
        const text = p.title.length > 30 ? p.title.slice(0, 29) + "…" : p.title;
        const tw = ctx.measureText(text).width;
        const sr = p.r * state.scale,
          off = sr + 8;
        const slots = [[sx + off, sy], [sx - off - tw, sy], [sx - tw / 2, sy + off + 5], [sx - tw / 2, sy - off - 5], [sx + off * 0.72, sy - off * 0.72 - 4], [sx - off * 0.72 - tw, sy - off * 0.72 - 4], [sx + off * 0.72, sy + off * 0.72 + 4], [sx - off * 0.72 - tw, sy + off * 0.72 + 4]];
        let box = null;
        for (const [x, y] of slots) {
          const trial = {
            x: x - LABEL_PAD,
            y: y - 8,
            w: tw + LABEL_PAD * 2,
            h: 16
          };
          if (!hits(trial)) {
            box = trial;
            break;
          }
        }
        if (!box) {
          if (!forced) continue;
          box = {
            x: sx + sr + 7 - LABEL_PAD,
            y: sy - 8,
            w: tw + LABEL_PAD * 2,
            h: 16
          };
        }
        obstacles.push(box);
        drawn++;
        ctx.globalAlpha = spotlight && !isLit(p.id) && !onPath ? DIM : 1;
        ctx.fillStyle = plateC;
        ctx.fillRect(box.x, box.y, box.w, box.h);
        ctx.fillStyle = hot || focus || onPath ? accent : ink3;
        ctx.fillText(text, box.x + LABEL_PAD, box.y + 8.5);
        ctx.globalAlpha = 1;
      }
    }
    for (let i = 0; i < PRESETTLE; i++) tick(1);
    fit(46);
    draw();

    /* The product drives this with requestAnimationFrame. Here it runs on the
       same 33ms timer the reactor uses, for the same reason the reactor gives:
       RAF stops dead when the frame is not on screen, and this kit is often
       previewed inside one that isn't. */
    let last = performance.now();
    const frame = () => {
      const now = performance.now();
      const dt = Math.min(64, now - last);
      last = now;
      tick(1);
      if (now - state.lastPulse > PULSE_EVERY && state.links.length) {
        const vis = state.links.filter(e => visible(e.a) && visible(e.b));
        if (vis.length) state.pulses.push({
          edge: vis[Math.floor(Math.random() * vis.length)],
          t: 0
        });
        state.lastPulse = now;
      }
      for (const p of state.pulses) p.t += dt / PULSE_MS;
      state.pulses = state.pulses.filter(p => p.t < 1);
      draw();
    };
    const loop = setInterval(frame, 33);
    const nodeAt = (px, py) => {
      const x = (px - state.tx) / state.scale,
        y = (py - state.ty) / state.scale;
      let best = null,
        bestD = Infinity;
      for (const p of state.pts) {
        if (!visible(p)) continue;
        const d = Math.hypot(p.x - x, p.y - y);
        const reach = Math.max(p.r + 5 / state.scale, 9 / state.scale);
        if (d < reach && d < bestD) {
          best = p;
          bestD = d;
        }
      }
      return best;
    };
    let panning = false,
      dragging = null,
      lastX = 0,
      lastY = 0,
      moved = 0;
    const down = ev => {
      canvas.setPointerCapture(ev.pointerId);
      lastX = ev.offsetX;
      lastY = ev.offsetY;
      moved = 0;
      const p = nodeAt(ev.offsetX, ev.offsetY);
      if (p) {
        dragging = p;
        p.fixed = true;
      } else {
        panning = true;
        canvas.style.cursor = "grabbing";
      }
    };
    const move = ev => {
      const dx = ev.offsetX - lastX,
        dy = ev.offsetY - lastY;
      lastX = ev.offsetX;
      lastY = ev.offsetY;
      moved += Math.abs(dx) + Math.abs(dy);
      if (dragging) {
        dragging.x += dx / state.scale;
        dragging.y += dy / state.scale;
        state.alpha = Math.max(state.alpha, 0.12);
        return;
      }
      if (panning) {
        state.tx += dx;
        state.ty += dy;
        return;
      }
      const p = nodeAt(ev.offsetX, ev.offsetY);
      const id = p ? p.id : null;
      if (id !== state.hovered) {
        state.hovered = id;
        canvas.style.cursor = id ? "pointer" : "grab";
        if (props.current.onHover) props.current.onHover(p);
      }
    };
    const up = ev => {
      const wasClick = moved < 5;
      const p = nodeAt(ev.offsetX, ev.offsetY);
      if (dragging) {
        dragging.fixed = false;
        dragging = null;
      }
      if (panning) {
        panning = false;
        canvas.style.cursor = "grab";
      }
      try {
        canvas.releasePointerCapture(ev.pointerId);
      } catch (e) {/* gone */}
      if (!wasClick) return;
      if (props.current.onSelect) props.current.onSelect(p || null, ev.shiftKey);
    };
    const wheel = ev => {
      ev.preventDefault();
      const factor = Math.exp(-ev.deltaY * 0.0016);
      const next = clamp(state.scale * factor, 0.15, 6);
      const k = next / state.scale;
      state.tx = ev.offsetX - (ev.offsetX - state.tx) * k;
      state.ty = ev.offsetY - (ev.offsetY - state.ty) * k;
      state.scale = next;
    };
    canvas.addEventListener("pointerdown", down);
    canvas.addEventListener("pointermove", move);
    canvas.addEventListener("pointerup", up);
    canvas.addEventListener("wheel", wheel, {
      passive: false
    });
    const onResize = () => fit(46);
    window.addEventListener("resize", onResize);
    return () => {
      clearInterval(loop);
      canvas.removeEventListener("pointerdown", down);
      canvas.removeEventListener("pointermove", move);
      canvas.removeEventListener("pointerup", up);
      canvas.removeEventListener("wheel", wheel);
      window.removeEventListener("resize", onResize);
    };
  }, []);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: ref,
    "aria-label": "Knowledge graph. Use the inspector panel to read notes.",
    style: {
      position: "fixed",
      inset: 0,
      width: "100%",
      height: "100%",
      display: "block",
      touchAction: "none",
      cursor: "grab",
      opacity
    }
  });
}
Object.assign(window, {
  GraphStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/GraphStage.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/JarvisConsole.jsx
try { (() => {
/* JARVIS console — v2. Same product as v1 (one vault, one graph, one ask
   bar); the shell is the instrument HUD instead of three plates.

   State lives here and nowhere else: the rails report, the stage asks, the
   overlays read. */
const {
  useState,
  useEffect,
  useRef,
  useMemo,
  useCallback
} = React;
const V = window.VAULT,
  T = window.TELEMETRY;
const BY_ID = new Map(V.nodes.map(n => [n.id, n]));
const NEIGH = new Map(V.nodes.map(n => [n.id, []]));
V.edges.forEach(e => {
  NEIGH.get(e.source).push(e.target);
  NEIGH.get(e.target).push(e.source);
});
function shortestPath(a, b) {
  if (!a || !b || a === b) return null;
  const prev = new Map([[a, null]]);
  const queue = [a];
  while (queue.length) {
    const cur = queue.shift();
    if (cur === b) break;
    for (const next of NEIGH.get(cur) || []) {
      if (prev.has(next)) continue;
      prev.set(next, cur);
      queue.push(next);
    }
  }
  if (!prev.has(b)) return null;
  const out = [];
  for (let cur = b; cur !== null; cur = prev.get(cur)) out.unshift(cur);
  return out;
}
const clock = () => new Date().toLocaleTimeString("pt-BR", {
  hour12: false
});
const hhmm = () => new Date().toLocaleTimeString("pt-BR", {
  hour: "2-digit",
  minute: "2-digit"
});
const greetingFor = h => h < 5 ? "BOA MADRUGADA, EDSON" : h < 12 ? "BOM DIA, EDSON" : h < 18 ? "BOA TARDE, EDSON" : "BOA NOITE, EDSON";
const drift = v => Math.max(4, Math.min(97, Math.round(v + (Math.random() - 0.5) * 7)));
function JarvisConsoleV2() {
  const [metrics, setMetrics] = useState(T.system);
  const [feed, setFeed] = useState(T.feed);
  const [log, setLog] = useState(T.log);
  const [brain, setBrain] = useState(V.brains.find(b => b.current).id);
  const [tools, setTools] = useState(V.tools);
  const [edits, setEdits] = useState(V.edits);
  const [quickState, setQuickState] = useState({
    silencio: false,
    analisar: false
  });
  const [openPanel, setOpenPanel] = useState(null);
  const [focused, setFocused] = useState(null);
  const [anchor, setAnchor] = useState(null);
  const [pathIds, setPathIds] = useState(null);
  const [query, setQuery] = useState("");
  const [phase, setPhase] = useState("idle");
  const [answer, setAnswer] = useState(null);
  const [level, setLevel] = useState(0.5);
  const timer = useRef(null);
  const push = useCallback((tag, text) => setLog(l => [...l.slice(-40), {
    at: clock(),
    tag,
    text
  }]), []);
  const note = useCallback((what, tail, tone) => setFeed(f => [{
    at: hhmm(),
    what,
    tail,
    tone
  }, ...f].slice(0, 7)), []);
  useEffect(() => {
    const id = setInterval(() => {
      setMetrics(m => m.map(x => ({
        ...x,
        value: drift(x.value)
      })));
      setLevel(l => Math.max(0.25, Math.min(1, l + (Math.random() - 0.5) * 0.2)));
    }, 2400);
    return () => clearInterval(id);
  }, []);
  useEffect(() => () => clearTimeout(timer.current), []);
  const results = useMemo(() => {
    const q = query.trim().toLowerCase();
    if (q.length < 2) return [];
    const out = [];
    for (const n of V.nodes) {
      const body = (V.bodies[n.id] || {}).body || "";
      const inTitle = n.title.toLowerCase().includes(q);
      const at = body.toLowerCase().indexOf(q);
      if (!inTitle && at < 0) continue;
      out.push({
        id: n.id,
        title: n.title,
        type: n.type,
        rel: n.rel,
        snippet: at >= 0 ? "…" + body.slice(Math.max(0, at - 34), at + 78).trim() + "…" : undefined
      });
      if (out.length === 6) break;
    }
    return out;
  }, [query]);
  const open = useCallback(id => {
    setFocused(id);
    setAnchor(id);
    setPathIds(null);
    setOpenPanel(null);
  }, []);
  const onSelect = useCallback((node, shift) => {
    if (!node) {
      setFocused(null);
      setPathIds(null);
      return;
    }
    if (shift && anchor && anchor !== node.id) {
      const p = shortestPath(anchor, node.id);
      setFocused(node.id);
      setPathIds(p ? new Set(p) : null);
      push(p ? "ok" : "warn", p ? "caminho traçado — " + (p.length - 1) + " passos" : "nenhum caminho entre as duas notas");
      return;
    }
    open(node.id);
  }, [anchor, open, push]);
  const ask = useCallback(() => {
    const q = query.trim();
    if (!q) return;
    setPhase("thinking");
    setAnswer(null);
    push("info", "consulta: " + q);
    timer.current = setTimeout(() => {
      const a = V.answers.ask;
      const titles = {},
        types = {};
      a.citations.forEach(id => {
        const n = BY_ID.get(id);
        titles[id] = n ? n.title : id;
        types[id] = n ? n.type : "other";
      });
      setAnswer({
        ...a,
        titles,
        types
      });
      setPhase("idle");
      push("ok", "resposta pronta · " + a.citations.length + " citações · 4,1s");
      note("Resposta gerada", a.citations.length + " citações", "accent");
    }, 1500);
  }, [query, push, note]);
  const onQuick = useCallback(q => {
    if (q.disabled) return;
    if (q.blocked) {
      push("err", q.note + " — nada foi ligado");
      note("Ação recusada", q.label, "warn");
      return;
    }
    if (q.id === "reindexar") {
      push("sys", "reindexando " + V.nodes.length + " notas…");
      note("Índice atualizado", "em curso");
      timer.current = setTimeout(() => {
        push("ok", "índice pronto — " + V.nodes.length + " notas, " + V.edges.length + " vínculos");
      }, 1200);
      return;
    }
    setQuickState(s => {
      const next = {
        ...s,
        [q.id]: !s[q.id]
      };
      push("info", q.label.toLowerCase() + ": " + (next[q.id] ? "ligado" : "desligado"));
      return next;
    });
  }, [push, note]);
  const onTool = useCallback(name => setTools(ts => ts.map(t => {
    if (t.name !== name) return t;
    push("info", t.label + ": " + (t.on ? "desligada" : "ligada"));
    return {
      ...t,
      on: !t.on
    };
  })), [push]);
  const onUndo = useCallback(id => setEdits(es => es.map(e => {
    if (e.id !== id || e.undone) return e;
    push("ok", "desfeito — " + e.path);
    note("Alteração desfeita", e.path.split("/").pop());
    return {
      ...e,
      undone: true
    };
  })), [push, note]);
  const focusedNote = focused ? {
    ...BY_ID.get(focused),
    ...(V.bodies[focused] || {})
  } : null;
  const links = focused ? (NEIGH.get(focused) || []).map(id => ({
    ...BY_ID.get(id),
    dir: "→"
  })) : [];
  const pathNodes = pathIds ? Array.from(pathIds).map(id => BY_ID.get(id)) : null;
  const hubs = V.hubs.map(id => BY_ID.get(id));
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(GraphStage, {
    nodes: V.nodes,
    edges: V.edges,
    hidden: new Set(),
    focused: focused,
    pathIds: pathIds,
    onSelect: onSelect,
    opacity: 0.68,
    sparseLabels: true
  }), /*#__PURE__*/React.createElement(RailLeft, {
    metrics: metrics,
    brains: V.brains,
    brain: brain,
    onBrain: id => {
      setBrain(id);
      push("info", "cérebro: " + id);
    },
    memory: T.memory,
    network: T.network,
    feed: feed
  }), /*#__PURE__*/React.createElement(CenterStage, {
    greeting: greetingFor(new Date().getHours()),
    phase: phase,
    level: level,
    query: query,
    onQuery: v => {
      setQuery(v);
      if (answer) setAnswer(null);
    },
    onSubmit: ask,
    results: results,
    onOpen: open,
    answer: answer,
    onCite: open,
    onClear: () => {
      setAnswer(null);
      setQuery("");
      push("sys", "nova conversa");
    },
    vaultStats: {
      notes: V.nodes.length,
      links: V.edges.length
    }
  }), /*#__PURE__*/React.createElement(RailRight, {
    weather: T.weather,
    jobs: T.jobs,
    quick: T.quick,
    quickState: quickState,
    onQuick: onQuick,
    shortcuts: T.shortcuts,
    onShortcut: id => setOpenPanel(p => {
      const next = p === id ? null : id;
      if (next) {
        setFocused(null);
        setPathIds(null);
      }
      return next;
    }),
    openPanel: openPanel,
    log: log,
    busy: phase === "thinking"
  }), /*#__PURE__*/React.createElement(Inspector, {
    note: focusedNote,
    links: links,
    path: pathNodes,
    hubs: hubs,
    onOpen: open,
    onClose: () => {
      setFocused(null);
      setPathIds(null);
    }
  }), /*#__PURE__*/React.createElement(SidePanel, {
    id: openPanel,
    vault: V,
    brain: brain,
    onBrain: id => {
      setBrain(id);
      push("info", "cérebro: " + id);
    },
    tools: tools,
    onTool: onTool,
    edits: edits,
    onUndo: onUndo,
    log: log,
    onClose: () => setOpenPanel(null)
  }));
}
Object.assign(window, {
  JarvisConsoleV2
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/JarvisConsole.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/Overlays.jsx
try { (() => {
/* The two overlays. Both are plates — the one place v2 still uses v1's
   translucent panel recipe, because they sit on top of the graph rather
   than on the field. */
const {
  HudFrame,
  MetaGrid,
  Row,
  BrainRow,
  Button,
  Eyebrow,
  PanelNote,
  OpenLink,
  TerminalLog,
  Icon
} = window.JARVISDesignSystem_ad8200;
const PLATE = {
  position: "fixed",
  zIndex: 6,
  top: "var(--rail-gutter)",
  background: "var(--surface-panel)",
  backdropFilter: "var(--blur-panel)",
  WebkitBackdropFilter: "var(--blur-panel)",
  border: "1px solid var(--border-hairline)",
  borderTopColor: "var(--border-lit)",
  borderRadius: "var(--r)",
  padding: "var(--pad)",
  maxHeight: "min(72vh, calc(100vh - var(--rail-gutter) * 2))",
  overflowY: "auto",
  overscrollBehavior: "contain",
  scrollbarWidth: "thin",
  scrollbarColor: "var(--ink-3) transparent",
  fontFamily: "var(--font-mono)",
  fontSize: "var(--size-base)",
  color: "var(--text-body)"
};
function CloseX({
  onClick
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    "aria-label": "Fechar",
    style: {
      position: "absolute",
      top: "10px",
      right: "10px",
      width: "20px",
      height: "20px",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      color: "var(--control-fg)",
      cursor: "pointer",
      transition: "var(--transition-control)"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "x",
    size: 11
  }));
}
function Inspector({
  note,
  links,
  path,
  hubs,
  onOpen,
  onClose
}) {
  if (!note) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...PLATE,
      left: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      width: "334px"
    }
  }, /*#__PURE__*/React.createElement(CloseX, {
    onClick: onClose
  }), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Inspector",
    right: note.type
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "9px",
      alignItems: "baseline",
      paddingRight: "22px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "var(--swatch)",
      height: "var(--swatch)",
      borderRadius: "50%",
      flex: "none",
      background: "var(--t-" + note.type + ", var(--t-other))",
      transform: "translateY(2px)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "var(--size-title)",
      color: "var(--text-strong)",
      lineHeight: 1.25
    }
  }, note.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "6px",
      fontSize: "var(--size-tick)",
      color: "var(--ink-3)"
    }
  }, note.rel), note.meta && Object.keys(note.meta).length ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "11px"
    }
  }, /*#__PURE__*/React.createElement(MetaGrid, {
    rows: Object.entries(note.meta)
  })) : null, note.warning ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "10px",
      color: "var(--warn)"
    }
  }, /*#__PURE__*/React.createElement(PanelNote, null, note.warning)) : null, note.body ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "12px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--size-prose)",
      lineHeight: 1.55,
      color: "var(--text-strong)",
      maxHeight: "var(--scroll-note)",
      overflowY: "auto",
      textWrap: "pretty"
    }
  }, note.body) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "12px"
    }
  }, /*#__PURE__*/React.createElement(OpenLink, {
    href: "file:///vault/" + note.rel,
    title: "abre fora do JARVIS"
  }, "Abrir o arquivo"))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "V\xEDnculos",
    right: String(links.length)
  }, links.length ? links.map(l => /*#__PURE__*/React.createElement(Row, {
    key: l.id,
    type: l.type,
    label: l.title,
    dir: l.dir,
    onClick: () => onOpen(l.id)
  })) : /*#__PURE__*/React.createElement(PanelNote, null, "Nenhum v\xEDnculo."))), path && path.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Caminho mais curto",
    right: path.length - 1 + " passos"
  }, path.map((p, i) => /*#__PURE__*/React.createElement(Row, {
    key: p.id,
    type: p.type,
    label: p.title,
    step: i + 1,
    onClick: () => onOpen(p.id)
  })))) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "16px"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Maiores hubs",
    foot: true
  }, hubs.map(h => /*#__PURE__*/React.createElement(Row, {
    key: h.id,
    type: h.type,
    label: h.title,
    tail: h.degree,
    onClick: () => onOpen(h.id)
  })))));
}
function SidePanel({
  id,
  vault,
  brain,
  onBrain,
  tools,
  onTool,
  edits,
  onUndo,
  log,
  onClose
}) {
  if (!id) return null;
  const wrap = (label, right, children) => /*#__PURE__*/React.createElement("div", {
    style: {
      ...PLATE,
      right: "calc(var(--rail-w) + var(--rail-gutter) * 2)",
      width: "300px"
    }
  }, /*#__PURE__*/React.createElement(CloseX, {
    onClick: onClose
  }), /*#__PURE__*/React.createElement(HudFrame, {
    label: label,
    right: right,
    foot: true
  }, children));
  if (id === "cerebro") {
    return wrap("Cérebro", "modelo", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, vault.brains.map(b => /*#__PURE__*/React.createElement(BrainRow, {
      key: b.id,
      name: b.label,
      note: b.note,
      pressed: b.id === brain,
      onClick: () => onBrain(b.id)
    }))));
  }
  if (id === "ferramentas") {
    return wrap("Ferramentas", "fora do vault", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, tools.map(t => /*#__PURE__*/React.createElement(BrainRow, {
      key: t.name,
      name: t.label,
      note: t.authenticated ? t.on ? "ligada" : "desligada" : "falta " + t.needs,
      pressed: t.on,
      disabled: !t.authenticated,
      onClick: () => t.authenticated && onTool(t.name)
    }))));
  }
  if (id === "alteracoes") {
    return wrap("Alterações", edits.length + " no disco", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "9px"
      }
    }, /*#__PURE__*/React.createElement(PanelNote, null, "C\xF3pias em .jarvis/undo. Nada \xE9 sobrescrito sem uma."), edits.map(e => /*#__PURE__*/React.createElement("div", {
      key: e.id,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "4px",
        paddingBottom: "8px",
        borderBottom: "1px solid var(--hairline)"
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        justifyContent: "space-between",
        gap: "8px",
        fontSize: "var(--size-tick)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--text-body)"
      }
    }, e.action), /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--ink-3)"
      }
    }, e.when)), /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: "var(--size-tick)",
        color: "var(--text-strong)",
        wordBreak: "break-all"
      }
    }, e.path), /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        gap: "8px"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: "var(--size-tick)",
        color: "var(--ink-3)"
      }
    }, e.before, " \u2192 ", e.after, " bytes"), /*#__PURE__*/React.createElement(Button, {
      size: "ghost",
      disabled: e.undone,
      title: e.undone ? "já desfeito" : undefined,
      onClick: () => onUndo(e.id)
    }, e.undone ? "desfeito" : "desfazer"))))));
  }
  if (id === "habilidades") {
    return wrap("Habilidades", "como você trabalha", /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: "2px"
      }
    }, vault.skills.map(s => /*#__PURE__*/React.createElement(BrainRow, {
      key: s.name,
      name: s.name,
      note: s.problem ? "⚠ " + s.problem : s.description,
      pressed: s.always,
      disabled: !!s.problem
    }))));
  }
  return wrap("Terminal", "sessão", /*#__PURE__*/React.createElement(TerminalLog, {
    lines: log,
    height: 280
  }));
}
Object.assign(window, {
  Inspector,
  SidePanel
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/Overlays.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/RailLeft.jsx
try { (() => {
/* Left rail — the machine's own vital signs, top to bottom: load, the core
   that is answering, what it is holding in memory, the vault's shape, and
   the last things that happened. */
const {
  HudFrame,
  Meter,
  RadialGauge,
  StatList,
  BrainRow
} = window.JARVISDesignSystem_ad8200;
const kv = (k, v, tone) => /*#__PURE__*/React.createElement("div", {
  key: k,
  style: {
    display: "flex",
    justifyContent: "space-between",
    gap: "8px",
    padding: "2px 0",
    fontSize: "var(--size-tick)"
  }
}, /*#__PURE__*/React.createElement("span", {
  style: {
    color: "var(--text-label)",
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  }
}, k), /*#__PURE__*/React.createElement("span", {
  style: {
    color: tone || "var(--text-strong)",
    fontVariantNumeric: "tabular-nums"
  }
}, v));
function RailLeft({
  metrics,
  brains,
  brain,
  onBrain,
  memory,
  network,
  feed,
  onOpenFeed
}) {
  const [openBrains, setOpenBrains] = React.useState(false);
  const current = brains.find(b => b.id === brain) || brains[0];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 4,
      left: "var(--rail-gutter)",
      top: "var(--rail-gutter)",
      bottom: "var(--rail-gutter)",
      width: "var(--rail-w)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--rail-gap)",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Status do sistema",
    right: "local"
  }, metrics.map(m => /*#__PURE__*/React.createElement(Meter, {
    key: m.key,
    label: m.label,
    value: m.value,
    tone: m.value > 88 ? "warn" : "accent"
  }))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "N\xFAcleo de IA",
    right: openBrains ? "trocar" : null
  }, /*#__PURE__*/React.createElement("div", null, kv("modelo", current.label, "var(--accent)"), kv("modo", "ask"), kv("raciocínio", "87%"), kv("resposta", "4,1s")), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setOpenBrains(v => !v),
    style: {
      marginTop: "7px",
      width: "100%",
      padding: "4px 6px",
      background: "transparent",
      border: "1px solid var(--control-border)",
      borderRadius: "var(--r)",
      color: "var(--control-fg)",
      fontFamily: "var(--font-mono)",
      fontSize: "8px",
      letterSpacing: "0.14em",
      textTransform: "uppercase",
      cursor: "pointer",
      transition: "var(--transition-control)"
    }
  }, openBrains ? "fechar cérebro" : "trocar cérebro"), openBrains ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "7px",
      display: "flex",
      flexDirection: "column",
      gap: "2px"
    }
  }, brains.map(b => /*#__PURE__*/React.createElement(BrainRow, {
    key: b.id,
    name: b.label,
    note: b.note,
    pressed: b.id === brain,
    onClick: () => {
      onBrain(b.id);
      setOpenBrains(false);
    }
  }))) : null), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Banco de mem\xF3ria"
  }, /*#__PURE__*/React.createElement(RadialGauge, {
    value: memory.pct,
    label: "mem\xF3ria",
    caption: /*#__PURE__*/React.createElement(StatList, {
      rows: memory.rows
    })
  })), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Rede"
  }, /*#__PURE__*/React.createElement(StatList, {
    rows: network
  })), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Feed ao vivo",
    foot: true
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "1px"
    }
  }, feed.map((f, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    onClick: () => onOpenFeed && onOpenFeed(f),
    style: {
      display: "flex",
      gap: "8px",
      alignItems: "baseline",
      width: "100%",
      textAlign: "left",
      padding: "var(--row-pad-y) var(--row-pad-x)",
      margin: "0 calc(var(--row-pad-x) * -1)",
      background: "transparent",
      border: 0,
      borderRadius: "var(--r)",
      cursor: "pointer",
      fontFamily: "var(--font-mono)",
      fontSize: "var(--size-tick)",
      transition: "var(--transition-row)"
    },
    onMouseEnter: e => {
      e.currentTarget.style.background = "var(--wash)";
    },
    onMouseLeave: e => {
      e.currentTarget.style.background = "transparent";
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--ink-3)",
      flex: "none"
    }
  }, f.at), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)",
      flex: 1,
      minWidth: 0,
      whiteSpace: "nowrap",
      overflow: "hidden",
      textOverflow: "ellipsis"
    }
  }, f.what), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "none",
      color: f.tone === "warn" ? "var(--warn)" : f.tone === "accent" ? "var(--accent)" : "var(--ink-3)",
      maxWidth: "96px",
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, f.tail))))));
}
Object.assign(window, {
  RailLeft
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/RailLeft.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/RailRight.jsx
try { (() => {
/* Right rail — the world outside the vault (weather, scheduled work), the
   things you can press, and the log. */
const {
  HudFrame,
  Tile,
  TerminalLog,
  Icon
} = window.JARVISDesignSystem_ad8200;
function RailRight({
  weather,
  jobs,
  quick,
  quickState,
  onQuick,
  shortcuts,
  onShortcut,
  openPanel,
  log,
  busy
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      zIndex: 4,
      right: "var(--rail-gutter)",
      top: "var(--rail-gutter)",
      bottom: "var(--rail-gutter)",
      width: "var(--rail-w)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--rail-gap)",
      overflowY: "auto",
      overscrollBehavior: "contain",
      scrollbarWidth: "thin",
      scrollbarColor: "var(--ink-3) transparent"
    }
  }, /*#__PURE__*/React.createElement(HudFrame, {
    label: "Clima",
    right: weather.city
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "12px",
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: "none",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: "4px",
      paddingTop: "2px"
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "cloud-rain",
    size: 26,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: "20px",
      color: "var(--text-strong)",
      textShadow: "var(--hud-glow-soft)",
      lineHeight: 1
    }
  }, weather.temp, "\xB0")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, weather.rows.map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k,
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "8px",
      fontSize: "var(--size-tick)",
      padding: "1px 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-label)"
    }
  }, k), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--text-body)"
    }
  }, v))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "8px",
      fontSize: "var(--size-tick)",
      color: "var(--text-label)",
      textTransform: "uppercase",
      letterSpacing: "0.1em"
    }
  }, weather.sky), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "7px",
      display: "grid",
      gridTemplateColumns: "repeat(5,minmax(0,1fr))",
      gap: "4px"
    }
  }, weather.week.map(([d, t]) => /*#__PURE__*/React.createElement("div", {
    key: d,
    style: {
      textAlign: "center",
      fontSize: "8px",
      color: "var(--ink-3)",
      letterSpacing: "0.06em"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textTransform: "uppercase"
    }
  }, d), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--text-body)",
      marginTop: "3px"
    }
  }, t))))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Tarefas agendadas"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(4,minmax(0,1fr))",
      gap: "4px"
    }
  }, jobs.map(([n, label], i) => /*#__PURE__*/React.createElement("div", {
    key: label,
    style: {
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "17px",
      color: i === 3 ? "var(--crit)" : "var(--accent)",
      textShadow: i === 3 ? "none" : "var(--hud-glow-soft)",
      lineHeight: 1.1
    }
  }, n), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: "7.5px",
      letterSpacing: "0.1em",
      textTransform: "uppercase",
      color: "var(--text-label)",
      marginTop: "3px"
    }
  }, label))))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "A\xE7\xF5es r\xE1pidas"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(5,minmax(0,1fr))",
      gap: "var(--tile-gap)"
    }
  }, quick.map(q => /*#__PURE__*/React.createElement(Tile, {
    key: q.id,
    icon: q.icon,
    label: q.label,
    active: !!quickState[q.id],
    disabled: q.disabled,
    title: q.title || q.note,
    onClick: () => onQuick(q)
  })))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Atalhos"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3,minmax(0,1fr))",
      gap: "var(--tile-gap)"
    }
  }, shortcuts.map(s => /*#__PURE__*/React.createElement(Tile, {
    key: s.id,
    icon: s.icon,
    label: s.label,
    active: openPanel === s.id,
    onClick: () => onShortcut(s.id)
  })))), /*#__PURE__*/React.createElement(HudFrame, {
    label: "Terminal",
    right: busy ? "ocupado" : null,
    foot: true
  }, /*#__PURE__*/React.createElement(TerminalLog, {
    lines: log,
    busy: busy,
    height: 132
  })));
}
Object.assign(window, {
  RailRight
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/RailRight.jsx", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/telemetry.js
try { (() => {
/* Telemetry — the machine-side readouts v2 added to the console. Fixed
   values so every load draws the same picture; the drift in ConsoleV2 walks
   them a few points either way, which is all the movement the source's
   motion rules allow. */
(function () {
  window.TELEMETRY = {
    system: [{
      key: "cpu",
      label: "CPU",
      value: 17
    }, {
      key: "ram",
      label: "RAM",
      value: 39
    }, {
      key: "gpu",
      label: "GPU",
      value: 35
    }, {
      key: "dsk",
      label: "Disco",
      value: 52
    }],
    core: [["modelo", "sonnet-4.6"], ["modo", "ask"], ["raciocínio", "87%"], ["resposta", "4,1s"]],
    memory: {
      pct: 62,
      rows: [["contexto ativo", "32,4 GB"], ["longo prazo", "128,7 GB"], ["curto prazo", "18,6 GB"]]
    },
    network: [["nós do vault", String(window.VAULT.nodes.length)], ["vínculos", String(window.VAULT.edges.length)], ["entrada", "2,4 MB/s"], ["saída", "1,8 MB/s"]],
    feed: [{
      at: "22:41",
      what: "Índice atualizado",
      tail: "concluído"
    }, {
      at: "22:40",
      what: "Arquivo lido",
      tail: "fatura-4471.pdf"
    }, {
      at: "22:38",
      what: "Ferramenta negada",
      tail: "Google Drive",
      tone: "warn"
    }, {
      at: "22:35",
      what: "Nota escrita",
      tail: "client-health-board",
      tone: "accent"
    }, {
      at: "22:33",
      what: "Sessão aberta",
      tail: "edson@evo.local"
    }],
    weather: {
      city: "Rio do Sul, SC",
      temp: "18",
      sky: "chuva fraca",
      rows: [["sensação", "16°C"], ["umidade", "88%"], ["vento", "11 km/h"], ["ar", "bom"]],
      week: [["ter", "19/12"], ["qua", "21/13"], ["qui", "23/14"], ["sex", "20/15"], ["sáb", "18/12"]]
    },
    jobs: [["3", "rodando"], ["2", "agendadas"], ["5", "concluídas"], ["1", "falha"]],
    quick: [{
      id: "voz",
      icon: "mic",
      label: "Voz",
      note: "falta ELEVENLABS_API_KEY",
      blocked: true
    }, {
      id: "silencio",
      icon: "volume-x",
      label: "Silêncio"
    }, {
      id: "reindexar",
      icon: "scan-line",
      label: "Reindexar",
      action: true
    }, {
      id: "analisar",
      icon: "file-search",
      label: "Analisar"
    }, {
      id: "treinar",
      icon: "brain-circuit",
      label: "Treinar",
      disabled: true,
      title: "a etapa 5 liga isto"
    }],
    shortcuts: [{
      id: "terminal",
      icon: "square-terminal",
      label: "Terminal"
    }, {
      id: "cerebro",
      icon: "database",
      label: "Cérebro"
    }, {
      id: "ferramentas",
      icon: "network",
      label: "Ferramentas"
    }, {
      id: "alteracoes",
      icon: "workflow",
      label: "Alterações"
    }, {
      id: "habilidades",
      icon: "settings",
      label: "Habilidades"
    }],
    log: [{
      at: "22:36:58",
      tag: "sys",
      text: "índice aberto — " + window.VAULT.nodes.length + " notas, " + window.VAULT.edges.length + " vínculos"
    }, {
      at: "22:37:02",
      tag: "ok",
      text: "backend conectado — tudo operacional"
    }, {
      at: "22:37:44",
      tag: "info",
      text: "cérebro: sonnet-4.6"
    }, {
      at: "22:38:10",
      tag: "err",
      text: "falta ELEVENLABS_API_KEY — voz desligada"
    }]
  };
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/telemetry.js", error: String((e && e.message) || e) }); }

// ui_kits/jarvis-console/vault.js
try { (() => {
/* Demo vault — the profile block from Jarvis/data/generate.py, shrunk to 44
   notes so the graph reads at card size. Same shapes, same seed idea: fixed
   data, identical picture every load. */
(function () {
  const clients = [["Harrow & Vane", "furniture retail", "Leeds"], ["Northsound Audio", "hardware", "Glasgow"], ["Pellworth Legal", "professional services", "London"], ["Bramble Bakery", "food & drink", "Bristol"], ["Kestrel Fitness", "health", "Manchester"], ["Orlin Freight", "logistics", "Rotterdam"]];
  const projects = [["Storefront rebuild", 0], ["Checkout rework", 0], ["Brand refresh", 1], ["Catalogue migration", 2], ["Booking flow", 4], ["Client portal", 5], ["Subscription launch", 3], ["Performance pass", 1]];
  const meetings = [["Kickoff — Storefront rebuild", 0], ["Weekly check-in — Checkout rework", 1], ["Scope review — Brand refresh", 2], ["Handover — Booking flow", 4], ["Budget conversation — Client portal", 5]];
  const invoices = [["Fatura 4471", 0, "£6,200"], ["Fatura 4472", 1, "£2,400"], ["Fatura 4468", 2, "£1,800"], ["Fatura 4475", 4, "£4,150"], ["Fatura 4477", 5, "£980"]];
  const people = [["Aisha Rahman", 0], ["Bruno Cortez", 1], ["Elin Berg", 2], ["Femi Achebe", 3], ["Priya Iqbal", 4], ["Tomas Falk", 5]];
  const notes = [["Operating principles", "The spine note. Most other notes point back here.", ["positioning"]], ["Client health board", "One line per client, updated when something changes.", ["clients"]], ["Money map", "Where revenue comes from and when it lands.", ["cashflow"]], ["Deposit policy", "Half up front, the rest on handover. The deposit is what buys the slot in the calendar — a project without one is a conversation, and the calendar stays open.", ["cashflow"]], ["Pricing ladder", "Five offerings, five ceilings. Say the number first; the conversation after it is the real brief.", ["pricing"]], ["Late payment, what works", "Chase on the day, not the week. Name the invoice and the amount in the first line.", ["cashflow"]], ["Scope creep early-warning signs", "Three: a new name on the call, a deadline that moves toward you, and the word 'quick'.", ["process"]], ["Where the margin actually goes", "Hosting, the two hours before every call, and the fortnight after handover nobody quotes for.", ["costs"]], ["Capacity, realistically", "Two projects, one care plan, nothing else. The third project is always paid for by the first two.", ["planning"]], ["The two-week rule", "Nothing goes on the board that cannot start within a fortnight.", ["process"]]];
  const refs = [["Contrato padrão 2026", "reference"], ["Tabela de preços — julho", "reference"]];
  const nodes = [];
  const edges = [];
  const bodies = {};
  const add = (id, title, type, rel, body, meta) => {
    nodes.push({
      id,
      title,
      type,
      rel
    });
    bodies[id] = {
      body: body || "",
      meta: meta || {}
    };
  };
  const link = (a, b) => edges.push({
    source: a,
    target: b
  });
  const slug = s => s.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "");
  clients.forEach(([name, trade, city], i) => {
    const id = "demo/clients/" + slug(name) + ".md";
    add(id, name, "client", "clients/" + slug(name) + ".md", name + " — " + trade + ", " + city + ". Two projects live, one care plan. Paid on time twice, late once.", {
      trade: trade,
      city: city,
      since: "2024"
    });
  });
  projects.forEach(([name, ci], i) => {
    const id = "demo/projects/" + slug(name) + ".md";
    add(id, name, "project", "projects/" + slug(name) + ".md", name + " for " + clients[ci][0] + ". Fixed price, two-week discovery, handover in six.", {
      client: clients[ci][0],
      status: i % 3 === 0 ? "live" : "closed"
    });
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  meetings.forEach(([name, pi]) => {
    const id = "demo/meetings/" + slug(name) + ".md";
    add(id, name, "meeting", "meetings/" + slug(name) + ".md", "Present: the client and me. Decisions taken are in the project note; this is what was said.", {
      when: "2026-07-2" + (pi + 1)
    });
    link(id, "demo/projects/" + slug(projects[pi][0]) + ".md");
  });
  invoices.forEach(([name, ci, amount]) => {
    const id = "demo/invoices/" + slug(name) + ".pdf";
    add(id, name, "invoice", "invoices/" + slug(name) + ".pdf", "", {
      amount: amount,
      due: "2026-08-1" + (ci + 1),
      client: clients[ci][0]
    });
    bodies[id].warning = "no text extracted — scanned PDF";
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  people.forEach(([name, ci]) => {
    const id = "demo/people/" + slug(name) + ".md";
    add(id, name, "person", "people/" + slug(name) + ".md", name + " is the contact at " + clients[ci][0] + ". Answers email, not calls.", {
      at: clients[ci][0]
    });
    link(id, "demo/clients/" + slug(clients[ci][0]) + ".md");
  });
  notes.forEach(([title, body, tags], i) => {
    const id = "demo/notes/" + slug(title) + ".md";
    add(id, title, "note", "notes/" + slug(title) + ".md", body, {
      tags: tags.join(", ")
    });
    if (i > 0) link(id, "demo/notes/operating-principles.md");
  });
  refs.forEach(([title]) => {
    const id = "demo/reference/" + slug(title) + ".pdf";
    add(id, title, "reference", "reference/" + slug(title) + ".pdf", "Assinado em janeiro. Cláusula 7 é a que importa: 30 dias, sem exceção.", {
      pages: "4"
    });
    link(id, "demo/notes/deposit-policy.md");
  });
  link("demo/notes/deposit-policy.md", "demo/notes/money-map.md");
  link("demo/notes/late-payment-what-works.md", "demo/notes/money-map.md");
  link("demo/notes/client-health-board.md", "demo/clients/harrow-vane.md");
  link("demo/notes/client-health-board.md", "demo/clients/orlin-freight.md");
  link("demo/notes/client-health-board.md", "demo/clients/bramble-bakery.md");
  link("demo/notes/money-map.md", "demo/invoices/fatura-4471.pdf");
  link("demo/notes/money-map.md", "demo/invoices/fatura-4475.pdf");
  link("demo/notes/pricing-ladder.md", "demo/projects/storefront-rebuild.md");
  link("demo/notes/capacity-realistically.md", "demo/projects/booking-flow.md");
  link("demo/notes/scope-creep-early-warning-signs.md", "demo/projects/checkout-rework.md");
  link("demo/notes/deposit-policy.md", "demo/clients/harrow-vane.md");
  link("demo/notes/where-the-margin-actually-goes.md", "demo/notes/money-map.md");
  const degree = {};
  edges.forEach(e => {
    degree[e.source] = (degree[e.source] || 0) + 1;
    degree[e.target] = (degree[e.target] || 0) + 1;
  });
  nodes.forEach(n => {
    n.degree = degree[n.id] || 0;
  });
  const counts = {};
  nodes.forEach(n => {
    counts[n.type] = (counts[n.type] || 0) + 1;
  });
  const hubs = nodes.slice().sort((a, b) => b.degree - a.degree).slice(0, 5).map(n => n.id);
  window.VAULT = {
    nodes: nodes,
    edges: edges,
    bodies: bodies,
    counts: counts,
    hubs: hubs,
    stats: [["mode", "demo"], ["notes", String(nodes.length)], ["links", String(edges.length)], ["indexed in", "0.4s"], ["model", "sonnet-4.6"], ["listen", "missing"], ["speak", "this machine"]],
    brains: [{
      id: "claude-sonnet-4-6",
      label: "Sonnet 4.6",
      note: "rápido, o padrão",
      current: true
    }, {
      id: "claude-opus-4-1",
      label: "Opus 4.1",
      note: "mais caro, para relatórios",
      current: false
    }, {
      id: "claude-haiku-4-5",
      label: "Haiku 4.5",
      note: "para buscas curtas",
      current: false
    }],
    tools: [{
      name: "drive",
      label: "Google Drive",
      authenticated: false,
      needs: "GOOGLE_OAUTH_TOKEN",
      on: false
    }, {
      name: "gmail",
      label: "Gmail",
      authenticated: false,
      needs: "GOOGLE_OAUTH_TOKEN",
      on: false
    }, {
      name: "fetch",
      label: "Fetch (web)",
      authenticated: true,
      on: false
    }],
    skills: [{
      name: "Cobrança",
      description: "cobrar fornecedor, boleto vencido, atraso, inadimplência…",
      always: false
    }, {
      name: "Fechamento",
      description: "",
      problem: "sem description — nunca casa"
    }],
    edits: [{
      id: "e2",
      when: "04/08/2026, 11:02",
      path: "demo/notes/client-health-board.md",
      action: "escreveu",
      before: 1840,
      after: 1952,
      undone: false
    }, {
      id: "e1",
      when: "03/08/2026, 16:20",
      path: "demo/notes/money-map.md",
      action: "escreveu",
      before: 2210,
      after: 2210,
      undone: true
    }],
    facts: [{
      name: "jatinox-prazo",
      when: "2026-07-30",
      text: "Boleto da Jatinox sempre 28 dias, nunca 30."
    }, {
      name: "sem-backup",
      when: "2026-07-22",
      text: "Não existe backup do Evo-SI. Qualquer escrita precisa de cópia antes."
    }],
    turns: [{
      when: "04/08/2026, 14:22",
      question: "quanto está vencido com a Jatinox?",
      meta: "6 citações · 318 tokens · sonnet-4.6"
    }, {
      when: "03/08/2026, 09:07",
      question: "o que muda no prazo se eu antecipar?",
      meta: "2 citações · 1.204 tokens · sonnet-4.6"
    }, {
      when: "02/08/2026, 17:55",
      question: "quais projetos estão sem depósito?",
      meta: "0 citações · 96 tokens · sonnet-4.6"
    }],
    answers: {
      ask: {
        kind: "ask",
        meta: "sonnet-4.6 · read 14 notes · 2 remembered · cobranca · JARVIS.md · 4.1s",
        body: "Metade na assinatura, metade na entrega [demo/notes/deposit-policy.md]. O depósito é o que compra a vaga na agenda — sem ele o projeto é uma conversa e a agenda continua aberta.\n\nA Harrow & Vane é a única cliente com projeto aberto e sem depósito registrado [demo/clients/harrow-vane.md]. A fatura 4471, de £6,200, vence em 11/08 [demo/invoices/fatura-4471.pdf].\n\nO que falta para responder direito sobre hoje: um export de contas a receber com data de hoje. O dado acima é de 28/07.",
        citations: ["demo/notes/deposit-policy.md", "demo/clients/harrow-vane.md", "demo/invoices/fatura-4471.pdf"]
      },
      brief: {
        kind: "brief",
        meta: "sonnet-4.6 · read 22 notes · JARVIS.md · 6.3s",
        body: "· Fatura 4471 (£6,200, Harrow & Vane) vence em 11/08 e não tem depósito lançado.\n· Checkout rework ganhou um nome novo na chamada de segunda — sinal de escopo [demo/notes/scope-creep-early-warning-signs.md].\n· Booking flow entregou; a janela de suporte fecha em 14 dias.\n· Capacidade: dois projetos e um care plan já estão ocupados. O terceiro entra sem margem [demo/notes/capacity-realistically.md].\n· Nada mais mudou desde 28/07.",
        citations: ["demo/notes/scope-creep-early-warning-signs.md", "demo/notes/capacity-realistically.md"]
      }
    }
  };
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/jarvis-console/vault.js", error: String((e && e.message) || e) }); }

__ds_ns.AnswerBlock = __ds_scope.AnswerBlock;

__ds_ns.Cite = __ds_scope.Cite;

__ds_ns.FactRow = __ds_scope.FactRow;

__ds_ns.ResultRow = __ds_scope.ResultRow;

__ds_ns.TurnRow = __ds_scope.TurnRow;

__ds_ns.Alert = __ds_scope.Alert;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.OpenLink = __ds_scope.OpenLink;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.PanelHead = __ds_scope.PanelHead;

__ds_ns.PanelNote = __ds_scope.PanelNote;

__ds_ns.AskField = __ds_scope.AskField;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.SearchInput = __ds_scope.SearchInput;

__ds_ns.ThreadBar = __ds_scope.ThreadBar;

__ds_ns.HudFrame = __ds_scope.HudFrame;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.Meter = __ds_scope.Meter;

__ds_ns.RadialGauge = __ds_scope.RadialGauge;

__ds_ns.Reactor = __ds_scope.Reactor;

__ds_ns.TerminalLog = __ds_scope.TerminalLog;

__ds_ns.Tile = __ds_scope.Tile;

__ds_ns.BrainRow = __ds_scope.BrainRow;

__ds_ns.MetaGrid = __ds_scope.MetaGrid;

__ds_ns.Row = __ds_scope.Row;

__ds_ns.StatList = __ds_scope.StatList;

__ds_ns.Swatch = __ds_scope.Swatch;

__ds_ns.TypeRow = __ds_scope.TypeRow;

__ds_ns.ReactorDial = __ds_scope.ReactorDial;

})();
